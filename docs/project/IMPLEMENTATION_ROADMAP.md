# План реализации AI_TEAM

**Принцип**: Сначала "материнская плата и провода", потом "процессор и память", потом "софт и логика"

**Метафора**: Мы собираем компьютер — начинаем с материнской платы (MindBus), потом добавляем компоненты один за одним.

**Стратегия**: Сначала практика с реальным кодом → потом корректировка спецификаций → потом сложные компоненты (Orchestrator)

---

## 🎯 Этап 0: Концепция и спецификации (✅ ЗАВЕРШЁН)

**Статус**: ✅ Готово (обновлено 2025-12-15)

**Что сделано**:
- ✅ Основная концепция системы ([README.md](../../README.md))
- ✅ Ключевые принципы (Observability, Resilience, Ready-Made Solutions First)
- ✅ **SSOT спецификации** ([docs/SSOT/](../SSOT/)):
  - MindBus Protocol v1.0 Final
  - MESSAGE_FORMAT v1.1.2 (оценка 96/100 🏆)
  - NODE_PASSPORT_SPEC v1.0
  - NODE_REGISTRY_SPEC v1.0
  - PROCESS_CARD_SPEC v1.0
- ✅ SSOT Audit (2025-12-15) — общая оценка 88/100
- ✅ QUESTIONS.md очищен и обновлён (удалено 50+ решённых вопросов)

**Технологический стек** (определён на основе SSOT):
- **Transport**: RabbitMQ (AMQP 0-9-1) — из [MindBus Protocol v1.0](../SSOT/mindbus_protocol_v1.md)
- **Язык**: Python 3.11+ — для Pydantic, async/await, typing
- **Envelope**: CloudEvents v1.0 (JSON) — из MindBus Protocol
- **Валидация**: Pydantic — для SSOT schemas
- **Конфиги**: YAML — согласно принципу Zero Hardcoding ([CLAUDE.md](../../CLAUDE.md))
- **Логирование**: Python logging + W3C Trace Context
- **Error Codes**: google.rpc.Code (17 стандартных кодов)

**Результат**: Понимание "ЧТО", "ЗАЧЕМ" и "НА ЧЁМ" мы строим

---

## 🔌 Этап 1: Материнская плата (MindBus + Monitor + AI Agent)

**Статус**: ✅ ПОЛНОСТЬЮ ЗАВЕРШЁН

**Цель**: Создать "провода" и убедиться, что "по ним бегут сигналы" с реальным AI

**Философия**: Проверяем инфраструктуру на практике ПЕРЕД созданием сложных компонентов (Orchestrator)

**Результаты (2025-12-16)**:
- ✅ MindBus Core работает (59/59 тестов пройдено)
- ✅ SimpleAIAgent работает с OpenAI
- ✅ Web Demo для тестирования (http://localhost:8080)
- ✅ Все 5 типов сообщений протестированы
- ✅ Node Registry + Agent Registration (92 теста)
- ✅ Storage Service (34 теста)
- ✅ Orchestrator MVP (46 тестов)
- ✅ CLI интерфейс
- ✅ E2E интеграционные тесты (18/18)
- ✅ **Всего: 117 тестов пройдено**

---

### ⚠️ НАПОМИНАНИЕ: Ready-Made Solutions First

> **КРИТИЧЕСКОЕ ПРАВИЛО** (см. [CLAUDE.md](../../CLAUDE.md) раздел 0)
>
> Перед написанием ЛЮБОГО кода — проверь: есть ли готовое решение?

**Чеклист "Анти-велосипед"** перед каждым шагом:
- [ ] Есть ли готовая библиотека для этой задачи?
- [ ] Есть ли стандартный протокол/формат?
- [ ] Покрывает ли готовое решение ≥80% требований?
- [ ] Если нет — обоснование в `docs/concepts/COMPONENT_decision.md`

**Что мы НЕ пишем сами** (уже выбрано и зафиксировано):

| Задача | ❌ НЕ ПИШЕМ | ✅ ИСПОЛЬЗУЕМ | Экономия |
|--------|-------------|---------------|----------|
| Message broker | Custom transport | **RabbitMQ** (AMQP 0-9-1) | 7-10 недель |
| Message format | Custom JSON schema | **CloudEvents v1.0** | 2-3 недели |
| Validation | Custom validators | **Pydantic** | 1-2 недели |
| Error codes | Custom error taxonomy | **google.rpc.Code** (17 кодов) | 1 неделя |
| Distributed tracing | Custom correlation | **W3C Trace Context** | 1 неделя |
| Config format | Custom parser | **YAML** | 3-5 дней |
| API description | Custom docs | **AsyncAPI 3.0.0** | 1 неделя |

**Почему это важно**:
> Без этого принципа MindBus занял бы 7-10 недель разработки.
> С этим принципом — 1-2 недели интеграции готовых решений.
> **Экономия: 80-90% времени.**

---

### Шаг 1.1: Определить формат Message (✅ ЗАВЕРШЁН)

**Задача**: Зафиксировать структуру сообщения на концептуальном уровне

**Документ**: ✅ [docs/SSOT/MESSAGE_FORMAT_v1.1.md](../SSOT/MESSAGE_FORMAT_v1.1.md) (версия v1.1.2)

**Что определено**:
- ✅ Структура `data` field для CloudEvents сообщений
- ✅ Типы сообщений для MVP: `COMMAND`, `RESULT`, `ERROR`, `EVENT`, `CONTROL` (5 типов)
- ✅ ERROR как отдельный тип (RFC 7807 pattern)
- ✅ Стандартные коды ошибок (google.rpc.Code - 17 кодов)
- ✅ Интеграция с NODE_PASSPORT через `requirements` field
- ✅ Интеграция с PROCESS_CARD через `context` field
- ✅ Идемпотентность и retry semantics (Stripe API pattern)
- ✅ AsyncAPI 3.0.0 совместимость
- ✅ Обязательные и опциональные поля для каждого типа
- ✅ Pydantic схемы для валидации
- ✅ Полные примеры всех типов сообщений
- ✅ Критические исправления из SSOT Audit (RESULT status = только SUCCESS)

**Критерий готовности**: ✅ Есть утверждённая спецификация v1.1.2 с примерами и оценкой 96/100

**Время**: Завершено 2025-12-15

---

### Шаг 1.2: Реализовать MindBus Core v0.1 (✅ ЗАВЕРШЁН)

**Задача**: Написать минимальную рабочую шину с валидацией по SSOT

**Статус**: ✅ Завершено 2025-12-16

---

#### 📦 Часть A: Установка готовых решений (30 минут)

> **Принцип**: Сначала ставим готовое, потом пишем свой "клей"

**A1. Запуск RabbitMQ (готовый сервер сообщений)**:
```bash
# Одна команда — и почтовый сервер для сообщений работает
docker run -d --name rabbitmq \
  -p 5672:5672 \
  -p 15672:15672 \
  rabbitmq:3-management

# Проверка: откройте http://localhost:15672 (guest/guest)
```

**A2. Установка Python библиотек (готовые кубики)**:
```bash
# Создаём виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или: venv\Scripts\activate  # Windows

# Устанавливаем готовые библиотеки
pip install pika              # работа с RabbitMQ (~50,000 строк готового кода)
pip install cloudevents       # формат сообщений (~10,000 строк готового кода)
pip install pydantic          # валидация данных (~30,000 строк готового кода)
pip install pyyaml            # чтение конфигов (~5,000 строк готового кода)

# Сохраняем зависимости
pip freeze > requirements.txt
```

**A3. Создание структуры проекта**:
```bash
mkdir -p src/mindbus
mkdir -p config
mkdir -p tests
touch src/__init__.py
touch src/mindbus/__init__.py
```

**Результат части A**:
- ✅ RabbitMQ работает (0 строк нашего кода)
- ✅ Библиотеки установлены (0 строк нашего кода)
- ✅ Структура папок готова

---

#### 🔧 Часть B: Написание "клея" (4-6 часов)

> **Принцип**: Мы пишем только то, что соединяет готовые библиотеки

**B1. Pydantic модели** — `src/mindbus/models.py` (~100 строк):
- Переводим SSOT схемы из [MESSAGE_FORMAT_v1.1.md](../SSOT/MESSAGE_FORMAT_v1.1.md) в Python классы
- Pydantic сам делает валидацию — мы только описываем структуру

**B2. MindBus Core** — `src/mindbus/core.py` (~150 строк):
- Подключение к RabbitMQ (используем готовую библиотеку `pika`)
- Обёртка сообщений в CloudEvents (используем готовую библиотеку `cloudevents`)
- Валидация через Pydantic (автоматически)

**B3. Конфигурация** — `config/mindbus.yaml` (~20 строк):
- Адрес RabbitMQ, credentials, названия очередей

**B4. Тесты** — `tests/test_mindbus.py` (~50 строк):
- Проверяем: отправка работает, валидация работает

---

#### 📊 Соотношение готового и нашего кода

| Компонент | Готовый код | Наш код | Мы пишем |
|-----------|-------------|---------|----------|
| RabbitMQ + pika | ~50,000 строк | 0 | 0% |
| CloudEvents SDK | ~10,000 строк | 0 | 0% |
| Pydantic | ~30,000 строк | 0 | 0% |
| **Наш "клей"** | — | ~300 строк | 100% |
| **ИТОГО** | ~90,000 строк | ~300 строк | **0.3%** |

---

#### 📋 Функционал MindBus Core

- `send_message(message)` — отправить сообщение в шину
  - **Валидация**: проверить message по Pydantic схемам из [MESSAGE_FORMAT_v1.1.md](../SSOT/MESSAGE_FORMAT_v1.1.md)
  - **Отклонение**: если невалидно → вернуть ошибку отправителю (google.rpc.Code.INVALID_ARGUMENT)
  - **Логирование**: W3C Trace Context для всех сообщений
- `subscribe(filter, callback)` — подписаться на сообщения
  - Фильтр по `type` (COMMAND, RESULT, ERROR, EVENT, CONTROL)
  - Фильтр по `to` (routing по получателю)
- Доставка сообщений всем подписчикам
- Обработка приоритетов (priority field 1-10)

---

#### 📁 Структура проекта (создаётся на этом шаге)

```
src/
  mindbus/
    __init__.py
    core.py          # MindBus Core — наш "клей" (~150 строк)
    models.py        # Pydantic models из SSOT (~100 строк)
config/
  mindbus.yaml       # Конфигурация RabbitMQ (~20 строк)
tests/
  test_mindbus.py    # Тесты (~50 строк)
requirements.txt     # Зависимости (pika, cloudevents, pydantic, pyyaml)
```

---

#### ✅ Критерий готовности

- ✅ RabbitMQ запущен и доступен
- ✅ Можно отправить валидное сообщение и получить его в callback
- ✅ Невалидное сообщение отклоняется с ошибкой и понятным сообщением
- ✅ trace_id проходит через все сообщения

**Время**: 6-8 часов
- Часть A (установка готового): 30 минут
- Часть B (написание клея): 4-6 часов
- Тестирование и отладка: 1-2 часа

---

### Шаг 1.3: Реализовать Monitor v0.1 (✅ ЗАВЕРШЁН)

**Задача**: Создать "индикатор питания" — видим, что по шине что-то летает

**Статус**: ✅ Завершено 2025-12-16 (интегрировано в web_demo.py)

**Функционал**:
- Подписывается на **ВСЕ** сообщения (без фильтров)
- Выводит в консоль с цветовой кодировкой:
  - `[timestamp] type: from→to (trace_id: xxx)`
  - COMMAND — синий, RESULT — зелёный, ERROR — красный, EVENT — жёлтый, CONTROL — пурпурный
- Опционально: сохраняет в файл для последующего анализа (JSON Lines format)
- Показывает валидационные ошибки (если MindBus отклонил сообщение)

**Структура**:
```
src/
  monitor/
    __init__.py
    monitor.py       # Monitor implementation
    formatters.py    # Output formatting (console, file)
config/
  monitor.yaml       # Monitor configuration
```

**Критерий готовности**: При отправке сообщения видим его в логе Monitor с правильным цветом

**Время**: 2-3 часа

---

### Шаг 1.4: Реализовать DummyAgent v0.1 (✅ ЗАВЕРШЁН)

**Задача**: Создать агента-заглушку, который умеет принимать COMMAND и отвечать RESULT

**Статус**: ✅ Завершено 2025-12-16

**Функционал**:
- Подписывается на сообщения: `type=COMMAND` и `to=dummy_agent`
- При получении COMMAND:
  - Ждёт `config["dummy_agent"]["mock_delay_seconds"]` (имитация работы)
  - Отправляет RESULT согласно [MESSAGE_FORMAT_v1.1.md](../SSOT/MESSAGE_FORMAT_v1.1.md) — тип RESULT
  - Копирует `trace_id` из входящего сообщения
  - Использует Pydantic модель `ResultData` из спецификации
  - Заполняет `execution_time_ms` (фактическое время выполнения)

**Структура**:
```
src/
  agents/
    __init__.py
    dummy_agent.py   # DummyAgent implementation
    base_agent.py    # Base class for all agents
config/
  agents/
    dummy_agent.yaml # DummyAgent configuration
```

**Пример конфигурации** (`config/agents/dummy_agent.yaml`):
```yaml
dummy_agent:
  name: "dummy_agent"
  type: "agent"
  mock_delay_seconds: 1.0    # Задержка для имитации работы
  success_rate: 1.0          # Всегда успешно для начала
  capabilities:
    - "test.echo"            # Тестовая capability
```

**Критерий готовности**: Отправили COMMAND → через `mock_delay_seconds` получили RESULT с правильной структурой

**Время**: 2-3 часа

---

### Шаг 1.5: Сквозной тест с заглушкой (✅ ЗАВЕРШЁН)

**Задача**: Убедиться, что "провода работают" с заглушкой

**Статус**: ✅ Завершено 2025-12-16

**Сценарий**:
1. Запустить RabbitMQ (через Docker)
2. Запустить MindBus Core
3. Запустить Monitor
4. Запустить DummyAgent
5. Отправить вручную (через скрипт `tests/send_test_command.py`) COMMAND для `dummy_agent`
6. Посмотреть в Monitor:
   - Видим COMMAND: `user→dummy_agent` (синий)
   - Видим RESULT: `dummy_agent→user` (зелёный)
7. Убедиться, что `trace_id` один и тот же в обоих сообщениях
8. Отправить невалидное сообщение → видим ERROR в Monitor (красный)

**Тестовый скрипт**:
```
tests/
  send_test_command.py       # Отправка тестовой команды
  test_e2e_dummy.py          # E2E тест с DummyAgent
```

**Критерий успеха**:
✅ "Мы можем кинуть сообщение и увидеть в Monitor, как оно прошло через систему"

**Время**: 1-2 часа

---

### Шаг 1.6: Подключить реального AI-агента (✅ ЗАВЕРШЁН)

**Задача**: Заменить DummyAgent на SimpleAIAgent с реальным LLM

**Статус**: ✅ Завершено 2025-12-16

**Почему важно**: Нужен **практический опыт** работы с реальным AI ПЕРЕД созданием ORCHESTRATOR_SPEC

**Функционал**:
- Подписывается на сообщения: `type=COMMAND` и `to=simple_ai_agent`
- При получении COMMAND с `action: "generate_text"`:
  - Извлекает `payload.prompt`
  - Вызывает OpenAI/Anthropic API (из переменных окружения)
  - Возвращает RESULT с `output: {text: "..."}` согласно [MESSAGE_FORMAT](../SSOT/MESSAGE_FORMAT_v1.1.md)
  - Заполняет `metrics: {model: "gpt-4", tokens: 150, cost: 0.003}`
  - Правильно обрабатывает ошибки → отправляет ERROR (не RESULT с ошибкой!)

**Структура**:
```
src/
  agents/
    simple_ai_agent.py       # SimpleAIAgent implementation
config/
  agents/
    simple_ai_agent.yaml     # Configuration
.env.example                 # Example env vars (OPENAI_API_KEY, etc.)
```

**Пример конфигурации** (`config/agents/simple_ai_agent.yaml`):
```yaml
simple_ai_agent:
  name: "simple_ai_agent"
  type: "agent"
  capabilities:
    - "generate_text"
  llm:
    provider: "openai"       # or "anthropic"
    model: "gpt-4o-mini"
    temperature: 0.7
    max_tokens: 500
```

**Критерий готовности**:
- ✅ Отправили COMMAND "напиши стих про кота" → получили реальный стих от LLM
- ✅ Отправили невалидный prompt → получили ERROR (не RESULT)
- ✅ Метрики заполнены корректно (model, tokens, cost)

**Время**: 3-4 часа

---

### Шаг 1.7: Ретроспектива Этапа 1

**Задача**: Проанализировать практический опыт и зафиксировать находки

**Вопросы для проработки**:
1. **MESSAGE_FORMAT**: Достаточно ли типов сообщений? Нужны ли изменения?
2. **Валидация**: Как работает Pydantic? Нужны ли улучшения в схемах?
3. **RabbitMQ**: Какие проблемы обнаружили? Как работают приоритеты?
4. **CloudEvents**: Все ли поля используются правильно?
5. **Trace Context**: Правильно ли проходит trace_id через систему?
6. **Errors**: Как обрабатываются ошибки? Достаточно ли google.rpc.Code?
7. **Что узнали о реальном LLM**: Какие ошибки возможны? Как долго выполняется?

**Результат**:
- Обновлённый [QUESTIONS.md](../../QUESTIONS.md) с новыми находками (если есть)
- Список потенциальных изменений для MESSAGE_FORMAT v1.2 (если нужны)
- **Главное**: Понимание того, что учесть при создании ORCHESTRATOR_SPEC

**Документ**: `docs/audits/ETAP_1_RETROSPECTIVE.md`

**Критерий готовности**: ✅ Зафиксирован практический опыт, понятны требования к Orchestrator

**Время**: 2 часа

**ВАЖНО**: **ТОЛЬКО ПОСЛЕ** этого шага переходить к Этапу 1.8 (Концепции для Orchestrator)

---

## 🧩 Этап 1.8: Node Registry + Agent Registration (подготовка к Этапу 2)

**Статус**: ✅ ЗАВЕРШЁН (2025-12-16)

**Цель**: Реализовать систему регистрации агентов — prerequisite для Orchestrator

**Философия**: Orchestrator не может работать без знания о том, какие агенты есть и что они умеют

**Спецификации**:
- [NODE_PASSPORT_SPEC_v1.0.md](../SSOT/NODE_PASSPORT_SPEC_v1.0.md) — формат паспорта узла
- [NODE_REGISTRY_SPEC_v1.0.md](../SSOT/NODE_REGISTRY_SPEC_v1.0.md) — спецификация реестра

---

### Шаг 1.8.1: Реализовать Node Registry (MVP — in-memory)

**Задача**: Создать реестр узлов системы

**Структура**:
```
src/
  registry/
    __init__.py
    node_registry.py     # Реестр узлов (in-memory)
    models.py            # Pydantic модели для NODE_PASSPORT
config/
  registry.yaml          # Конфигурация (TTL, heartbeat interval)
```

**Функционал**:
- `register_node(passport)` — зарегистрировать узел
- `update_heartbeat(node_id)` — обновить "живость" узла
- `find_nodes(selector)` — найти узлы по capabilities (label matching)
- `get_alive_nodes()` — получить список живых узлов
- `remove_dead_nodes()` — удалить узлы с истёкшим TTL

**Конфигурация** (`config/registry.yaml`):
```yaml
registry:
  heartbeat_interval_seconds: 10   # Агент отправляет heartbeat каждые 10s
  ttl_seconds: 30                  # TTL = 3x interval (буфер для сбоев сети)
  cleanup_interval_seconds: 5      # Проверка мёртвых узлов каждые 5s
```

**Критерий готовности**: Можно зарегистрировать узел и найти его по capabilities

**Время**: 3-4 часа

---

### Шаг 1.8.2: Научить агентов регистрироваться

**Задача**: При старте агент отправляет свой NODE_PASSPORT в Registry

**Изменения**:
- `src/agents/base_agent.py` — добавить метод `register()` и `send_heartbeat()`
- `src/agents/simple_ai_agent.py` — указать capabilities в конфиге

**Жизненный цикл агента**:
```
1. Агент стартует
2. Создаёт NODE_PASSPORT из своего конфига
3. Отправляет EVENT: node.registered
4. Каждые N сек отправляет EVENT: node.heartbeat
5. При остановке отправляет EVENT: node.deregistered
```

**Пример конфига агента** (`config/agents/simple_ai_agent.yaml`):
```yaml
simple_ai_agent:
  name: "simple_ai_agent"
  node_type: "agent"
  capabilities:
    - name: "generate_text"
      version: "1.0"
      parameters:
        models: ["gpt-4o-mini"]
        max_tokens: 4000
  labels:
    capability.generate_text: "true"
    capability.model.gpt-4o-mini: "true"
```

**Критерий готовности**: При старте SimpleAIAgent появляется в Registry

**Время**: 2-3 часа

---

### Шаг 1.8.3: Registry Service (слушает регистрации)

**Задача**: Сервис, который слушает MindBus и обновляет Registry

**Структура**:
```
src/
  registry/
    registry_service.py  # Слушает node.* events
```

**Подписки**:
- `evt.node_registered` → добавить в Registry
- `evt.node_heartbeat` → обновить last_seen
- `evt.node_deregistered` → удалить из Registry

**Функционал**:
- Фоновый процесс проверяет TTL и удаляет мёртвые узлы
- Логирует регистрации/удаления

**Критерий готовности**: Registry автоматически обновляется при старте/остановке агентов

**Время**: 2-3 часа

---

### Шаг 1.8.4: Тесты + Обновление Web Demo

**Задача**: Проверить работу системы регистрации

**Тесты** (`tests/test_node_registry.py`):
- Регистрация узла
- Heartbeat обновляет last_seen
- Поиск по capabilities (label selector)
- Удаление при истечении TTL
- Graceful deregistration

**Web Demo**:
- Добавить страницу `/nodes` — список зарегистрированных узлов
- Показывать: name, capabilities, status, last_seen

**Критерий готовности**: Все тесты проходят, в Web Demo видны зарегистрированные агенты

**Время**: 2 часа

---

### Итого Шаг 1.8

| Подшаг | Время | Результат |
|--------|-------|-----------|
| 1.8.1 Node Registry | 3-4 ч | Реестр узлов |
| 1.8.2 Agent Registration | 2-3 ч | Агенты регистрируются |
| 1.8.3 Registry Service | 2-3 ч | Автоматическое обновление |
| 1.8.4 Тесты + Demo | 2 ч | Проверка работы |
| **ИТОГО** | **~10 ч** | Готовая система регистрации |

**После завершения**: Orchestrator сможет находить агентов по capabilities

---

## 🧠 Этап 2: Добавляем мозг (Orchestrator)

**Статус**: 🔄 ТЕКУЩИЙ ЭТАП (Шаг 2.1 завершён, Шаги 2.2-2.3 в работе)

**Цель**: Реализовать Orchestrator согласно утверждённой спецификации

**Предусловие**: ✅ Завершён Этап 1.8.4 — есть ORCHESTRATOR_SPEC_v1.0.md

**Результаты (2025-12-16)**:
- ✅ Шаг 2.1: Orchestrator v0.1 (базовый) — ГОТОВ
  - IntegratedOrchestrator с полной интеграцией
  - Sync mode (локальные агенты) работает
  - Storage интеграция работает
  - CLI интерфейс работает
  - 46 тестов + 18 E2E тестов
- ✅ Шаг 2.2: API Gateway — ГОТОВ (22 теста)
  - FastAPI сервер на порту 8000
  - Endpoints: /tasks, /process, /cards, /status
- ✅ Шаг 2.3: Сквозной тест — ГОТОВ (9 E2E API тестов)

---

### Шаг 2.1: Реализовать Orchestrator v0.1 (базовый)

**Задача**: Реализовать Orchestrator согласно [ORCHESTRATOR_SPEC_v1.0.md](../SSOT/ORCHESTRATOR_SPEC_v1.0.md)

**Функционал** (минимальный для v0.1):
- **Coordinator**:
  - Принимает Task (структура будет определена в ORCHESTRATOR_SPEC)
  - Находит агента по capabilities (Agent Registration flow)
  - Отправляет COMMAND агенту
  - Ждёт RESULT
- **QualityController**:
  - Проверяет RESULT (пока без Reviewer — просто проверка на SUCCESS)
- **ResourceManager**:
  - Отслеживает `time_budget` (таймер на задачу)
  - Если превышен → отправляет CONTROL.STOP
- Публикует EVENT `task_completed` при завершении

**Структура**:
```
src/
  orchestrator/
    __init__.py
    orchestrator.py          # Main orchestrator
    planner.py               # Planner module (stub for now)
    coordinator.py           # Coordinator module
    quality_controller.py    # QualityController module (basic)
    resource_manager.py      # ResourceManager module
    models.py                # Pydantic models (Task, etc.)
config/
  orchestrator.yaml          # Configuration
```

**Критерий готовности**:
- ✅ Человек создаёт Task → Orchestrator находит агента → отправляет COMMAND → получает RESULT → завершает Task
- ✅ Если агент не найден → ERROR
- ✅ Если превышен time_budget → Task отменён

**Время**: 8-10 часов (сложнее чем в старом плане, но правильнее)

---

### Шаг 2.2: API Gateway (точка входа для человека)

**Задача**: Создать HTTP API для создания задач

**Функционал**:
- HTTP эндпоинт: `POST /tasks` — создать задачу
  - Body: `{"description": "Напиши стих про кота", "time_budget_seconds": 60}`
- Генерирует Task и передаёт Orchestrator
- Возвращает `task_id` человеку

**Технологии**: FastAPI (async Python web framework)

**Структура**:
```
src/
  api_gateway/
    __init__.py
    gateway.py               # FastAPI app
    routes/
      tasks.py               # /tasks endpoints
config/
  api_gateway.yaml           # Configuration (host, port)
```

**Критерий готовности**: ✅ Можно через curl создать задачу и получить task_id

**Время**: 2-3 часа

---

### Шаг 2.3: Сквозной тест Этапа 2

**Задача**: Убедиться что "мозг работает"

**Сценарий**:
1. Запустить всю систему: RabbitMQ, MindBus, Monitor, SimpleAIAgent, Orchestrator, API Gateway
2. Человек отправляет POST запрос: `{"description": "Напиши стих про кота", "time_budget_seconds": 30}`
3. API Gateway создаёт Task
4. Orchestrator:
   - Находит SimpleAIAgent по capability "generate_text"
   - Отправляет COMMAND
   - Получает RESULT
   - Проверяет качество (basic check)
   - Завершает Task
5. Monitor показывает всю цепочку:
   - EVENT (task created)
   - COMMAND (orchestrator → simple_ai_agent)
   - RESULT (simple_ai_agent → orchestrator)
   - EVENT (task completed)
6. Человек получает task_id и может проверить результат

**Тестовый скрипт**:
```
tests/
  test_e2e_orchestrator.py   # E2E тест с Orchestrator
  send_task_request.sh       # curl скрипт для ручного теста
```

**Критерий успеха Этапа 2**:
✅ "У нас есть сквозной поток: Человек → Task → Orchestrator → Agent → Результат"

**Время**: 2-3 часа

---

## 📋 Этап 3: Process Cards (многошаговые процессы)

**Статус**: ✅ ЗАВЕРШЁН (2025-12-16)

**Цель**: Дать Orchestrator возможность выполнять многошаговые процессы по инструкциям

**Предусловие**: ✅ Есть работающий Orchestrator v0.1

**Результаты (2025-12-16)**:
- ✅ Шаг 3.0: Примеры Process Cards — 5 карточек
  - simple_text_generation (2 шага)
  - article_generation (7 шагов, условия)
  - code_feature (8 шагов, multi-agent)
  - research_report (7 шагов, retry)
  - content_pipeline (качество + итерации)
- ✅ Шаг 3.1: Planner — встроен в IntegratedOrchestrator
- ✅ Шаг 3.2: Выполнение Process Cards — работает с DummyAgent

---

### Шаг 3.0: Создать примеры Process Cards

**Задача**: Создать 2-3 минимальных примера для понимания структуры

**Файлы**:
- `examples/process_cards/simple_text_task.yaml` — Research → Write → Review
- `examples/process_cards/code_feature.yaml` — Design → Code → Test → Review

**Формат**: Согласно [PROCESS_CARD_SPEC_v1.0.md](../SSOT/PROCESS_CARD_SPEC_v1.0.md)

**Связано**: [QUESTIONS.md](../../QUESTIONS.md) задача 2.1

**Критерий готовности**: ✅ Есть понятные примеры, можно использовать как шаблоны

**Время**: 2-3 часа

---

### Шаг 3.1: Реализовать Planner (интерпретатор Process Cards)

**Задача**: Orchestrator.Planner читает Process Card и планирует выполнение

**Функционал**:
- Читает Process Card из файла (YAML)
- Валидирует по [PROCESS_CARD_SPEC_v1.0.md](../SSOT/PROCESS_CARD_SPEC_v1.0.md)
- Создаёт execution plan (список шагов с зависимостями)
- Передаёт Coordinator для выполнения

**Критерий готовности**: Process Card с 3 последовательными шагами превращается в execution plan

**Время**: 4-5 часов

---

### Шаг 3.2: Реализовать выполнение Process Cards

**Задача**: Coordinator выполняет шаги Process Card по очереди

**Функционал**:
- Получает execution plan от Planner
- Выполняет шаги:
  - Отправляет COMMAND нужному агенту (по capabilities из шага)
  - Ждёт RESULT
  - QualityController проверяет качество
  - Если OK → переход к следующему шагу
  - Если FAIL → retry или ошибка
- Обрабатывает context (передаёт результаты предыдущих шагов)
- Завершает Task когда все шаги выполнены

**Критерий готовности**: Process Card "Research → Write → Review" выполняется автоматически

**Время**: 6-8 часов

---

### Шаг 3.3: Сквозной тест Этапа 3

**Сценарий**:
- Process Card: "Сгенерируй текст → Проверь качество → Сохрани результат"
- Orchestrator:
  - Читает Process Card
  - Координирует 3 агента (Writer, Critic, Storage)
  - Проходит все шаги
- Monitor показывает весь процесс
- Человек получает финальный результат

**Критерий успеха Этапа 3**:
✅ "Система выполняет сложные многошаговые процессы по инструкциям"

**Время**: 2-3 часа

---

## 🎭 Этап 4: Специализированные агенты (Творческая команда)

**Статус**: ✅ ЗАВЕРШЁН (2025-12-18)

**Цель**: Добавить специализированных агентов для разных типов задач

### Результаты

**Творческая команда** — 5 агентов для креативных проектов:

| Агент | Роль | Temperature | Capabilities |
|-------|------|-------------|--------------|
| 🔍 **Исследователь** | Сбор информации, анализ рынка | 0.3 | research_topic, analyze_market, fact_check |
| 💡 **Генератор идей** | Креативные концепции | 0.9 | generate_ideas, brainstorm, combine_concepts |
| 🎯 **Критик** | Оценка качества | 0.4 | review_content, evaluate_idea, compare_options |
| ✨ **Редактор** | Финальная доработка | 0.5 | edit_content, polish_style, restructure |
| ✒️ **Пушкин** (Writer) | Создание текстов | 0.7 | write_article, generate_outline |

### Архитектура

**CreativeAgent** — базовый класс ([src/agents/creative_agent.py](../../src/agents/creative_agent.py)):
- Наследует от BaseAgent (MindBus интеграция)
- LangGraph Agent Loop: UNDERSTAND → RESEARCH → EXECUTE → CRITIQUE → FINISH
- LiteLLM для унифицированных LLM вызовов
- Самокритика и итеративное улучшение (до 3 итераций)
- Конфиг определяет "личность" через system_prompt

**Конфигурации** ([config/agents/](../../config/agents/)):
- researcher_agent.yaml
- ideator_agent.yaml
- critic_agent.yaml
- editor_agent.yaml
- writer_agent.yaml

**Process Card** ([config/process_cards/creative_project.yaml](../../config/process_cards/creative_project.yaml)):
- 7-шаговый workflow для творческих проектов
- Поддержка: scenario, business_plan, book_plot, product_concept

### Тестирование

**Unit тесты** ([tests/test_creative_team.py](../../tests/test_creative_team.py)):
- 45 тестов — все пройдены
- Покрытие: конфиги, инстанциация, team factory, capabilities, prompts

**Интеграционные тесты с реальным API** (OpenAI GPT-4o):
- ✅ Каждый агент индивидуально
- ✅ Полная цепочка агентов (90 сек, 10 LLM вызовов)
- ✅ Коммуникация через RabbitMQ (MindBus)

**Web Monitor** ([src/web/monitor.py](../../src/web/monitor.py)):
- Все агенты видны в UI (http://localhost:8080)
- Отправка команд агентам
- Получение ответов в реальном времени
- Статусы: online/offline/working

### Файлы

```
src/agents/
├── creative_agent.py      # CreativeAgent + 4 concrete classes
├── base_agent.py          # BaseAgent (MindBus, Tools, Registry)
└── writer_agent.py        # Пушкин (отдельная реализация)

config/agents/
├── researcher_agent.yaml
├── ideator_agent.yaml
├── critic_agent.yaml
├── editor_agent.yaml
└── writer_agent.yaml

config/process_cards/
└── creative_project.yaml  # 7-step creative workflow

tests/
└── test_creative_team.py  # 45 unit tests
```

**Критерий успеха Этапа 4**: ✅ Выполнен
- "Orchestrator может координировать разных специалистов для сложных задач"
- Все агенты работают индивидуально и в цепочке
- Коммуникация через MindBus подтверждена

**Время**: ~12 часов (2025-12-18)

---

## 💾 Этап 5: Storage (хранение данных)

**Статус**: ✅ ЗАВЕРШЁН (спецификация утверждена 2025-12-19)

**Цель**: Добавить постоянное хранение для задач, артефактов, истории

### 📝 ОБЯЗАТЕЛЬНО ПЕРЕД НАЧАЛОМ: Проверь хранилище идей!

> **КРИТИЧНО**: Перед началом реализации — загляни в готовые наработки!

**Уже проработано**:
- ✅ [STORAGE_SPEC_v1.0.md](../SSOT/STORAGE_SPEC_v1.0.md) — **УТВЕРЖДЁННАЯ SSOT спецификация**
- ✅ [STORAGE_ARCHITECTURE_DISCUSSION](../concepts/drafts/STORAGE_ARCHITECTURE_DISCUSSION_2025-12-19.md) — протокол обсуждения (оценка 8.8/10)

**Ключевые решения уже приняты**:
- Трёхуровневая архитектура: Agent State → Process State → Artifacts
- Технологии: LangGraph Checkpointer + SQLite + fsspec
- "Pointer, not Payload" — файлы не через MindBus
- Artifact Manifest v1.0 с AI Context
- Degradation behavior (retry + buffer)

### Шаг 5.1: Реализовать Storage Adapter

**Функционал**:
- Принимает COMMAND `store_artifact`
- Сохраняет данные (начать с файловой системы)
- Возвращает RESULT с `artifact_id`

**Критерий успеха Этапа 5**:
✅ "Результаты задач сохраняются и доступны для просмотра"

**Время**: 4-5 часов

---

## 📊 Этап 6: Улучшенный Monitor (Web UI)

**Статус**: ⏭️ Будущий этап

**Цель**: Создать удобный интерфейс для наблюдения за системой

### 📝 ОБЯЗАТЕЛЬНО ПЕРЕД НАЧАЛОМ: Проверь хранилище идей!

> **КРИТИЧНО**: Перед началом реализации — загляни в готовые наработки!

| Черновик | Что полезного |
|----------|---------------|
| [AGENT_HUMAN_NAMES_v0.1](../concepts/drafts/AGENT_HUMAN_NAMES_v0.1.md) | Display Identity — как показывать агентов человеку |

**Ключевые идеи из черновика**:
- Два слоя идентификации: UID (системный) + displayName (для человека)
- "Перекличка" агентов: "Кто в системе — представьтесь"
- Резолв имён: "Спроси у Елены" → Монитор находит UID → COMMAND
- Display Identity = функция Монитора, НЕ оркестратора

### Шаг 6.1: Web-интерфейс для Monitor

**Функционал**:
- Показывает live stream сообщений (WebSockets)
- Фильтр по `trace_id` (показать всю цепочку одной задачи)
- Визуализация потоков сообщений

**Критерий успеха Этапа 6**:
✅ "Можно в браузере видеть, как система работает в реальном времени"

**Время**: 8-10 часов

---

## 🔄 Этап 7: Цикл улучшения (Quality Assessment)

**Статус**: ⏭️ Будущий этап

**Цель**: Реализовать полный цикл улучшения с Critic Agent

### 📝 ОБЯЗАТЕЛЬНО ПЕРЕД НАЧАЛОМ: Проверь хранилище идей!

> **КРИТИЧНО**: Перед началом реализации — загляни в готовые наработки!
> Здесь есть **БРИЛЛИАНТЫ** — не изобретай заново!

| Черновик | Что полезного |
|----------|---------------|
| [ORCHESTRATOR_COGNITIVE_STACK_v0.1](../concepts/drafts/ORCHESTRATOR_COGNITIVE_STACK_v0.1.md) | "Triangle of Quality" — паттерн качества |
| [ORCHESTRATOR_BRAIN_DYNAMICS_v0.1](../concepts/drafts/ORCHESTRATOR_BRAIN_DYNAMICS_v0.1.md) | Quality Loop — цикл обратной связи |

**💎 Ключевые идеи из черновиков**:

1. **Triangle of Quality** (разные LLM для разных ролей):
   | Роль | LLM | Функция |
   |------|-----|---------|
   | Исполнитель | GPT-4o | Создаёт контент |
   | Критик | Claude Sonnet | Ищет слабые места |
   | Арбитр | o1-preview или Человек | Принимает решение |

2. **State-Machine для задач**:
   ```
   PENDING → RESEARCHING → EXECUTING → CRITIQUE → REFACTORING → FINALIZED
   ```

3. **Quality Loop** (Feedback Cycle):
   ```
   Dispatch → Ingest → Evaluate → Decision Tree
     ↓
     Quality >= Threshold → Merge в SSOT
     Quality < Threshold AND Attempts < Max → Retry
     Quality < Threshold AND Attempts >= Max → Эскалация
   ```

4. **Post-mortem отчёты** — автоматическое улучшение Process Cards

### Шаг 7.1: Интегрировать Critic Agent с QualityController

**Функционал**:
- QualityController использует Critic Agent для оценки
- Если FAIL → retry с улучшенным промптом
- Если PASS → переход к следующему шагу
- Учёт `iteration_budget`

**Критерий успеха Этапа 7**:
✅ "Система автоматически улучшает результаты на основе критики"

**Время**: 6-8 часов

---

## 🔐 Этап 8: Безопасность и контроль

**Статус**: ⏭️ Будущий этап

**Цель**: Реализовать SECURITY_CONTROLS и Human-in-the-loop

### 📝 ОБЯЗАТЕЛЬНО ПЕРЕД НАЧАЛОМ: Проверь хранилище идей!

> **КРИТИЧНО**: Перед началом реализации — загляни в готовые наработки!
> Здесь есть **БРИЛЛИАНТЫ** — не изобретай заново!

| Черновик | Что полезного |
|----------|---------------|
| [ORCHESTRATOR_CONCEPT_v0.2](../concepts/drafts/ORCHESTRATOR_CONCEPT_v0.2.md) | Human-in-the-loop режимы A/B/C |
| [ORCHESTRATOR_COGNITIVE_STACK_v0.1](../concepts/drafts/ORCHESTRATOR_COGNITIVE_STACK_v0.1.md) | Internal Economy (Resource Budget) |

**💎 Ключевые идеи из черновиков**:

1. **Три режима Human-in-the-loop**:
   | Режим | Описание |
   |-------|----------|
   | A — Полная автономия | Команда сама формирует вопросы, сама отвечает, человек только получает итог |
   | B — Частичная | "Есть пожелания, остальное решайте сами" |
   | C — Полный диалог | Команда формирует вопросы, человек утверждает |

2. **Internal Economy (Resource Budget)**:
   > "У меня есть 10 итераций и $5 бюджета. Если качество 96%, а бюджет исчерпан — это жёсткий триггер для эскалации на человека."

3. **Протокол эскалации** — условия, когда ИИ обязан разбудить человека:
   - Превышение бюджета итераций или $
   - Логический тупик (цикл без прогресса)
   - Нарушение этических фильтров

4. **Project Working Rules** — первый документ проекта (режим + quality gates + кто утверждает)

### Шаг 8.1: Реализовать Task Budget enforcement

**Документ**: Использовать концепцию из Step 1.8.3

**Функционал**:
- ResourceManager жёстко отслеживает бюджеты
- Circuit Breaker при превышении
- Rate Limiting для защиты от DoS
- Kill Switch (CONTROL.STOP от человека)

**Время**: 4-5 часов

---

### Шаг 8.2: Human-in-the-loop checkpoints

**Функционал**:
- Process Card может содержать checkpoint (требуется подтверждение человека)
- Orchestrator ставит процесс на паузу
- Отправляет NOTIFICATION человеку
- Ждёт решения (approve/reject/edit)
- Продолжает или отменяет процесс

**Критерий успеха Этапа 8**:
✅ "Человек может контролировать критичные моменты процесса"

**Время**: 5-6 часов

---

## 📈 Дальнейшие этапы (не детализированы)

### Этап 9: Knowledge Base
Векторное хранилище для контекста.

---

### Этап 10: Advanced Process Cards

**Цель**: Циклы, условия, параллельность в Process Cards.

**📝 ОБЯЗАТЕЛЬНО ПЕРЕД НАЧАЛОМ**: Загляни в черновик!

| Черновик | Что полезного |
|----------|---------------|
| [PROCESS_CARD_INTERPRETER_v0.2](../concepts/drafts/PROCESS_CARD_INTERPRETER_v0.2.md) | Движок интерпретатора — как CPU |

**💎 Ключевые идеи**:
- **IR/AST**: Компиляция карточки в граф шагов
- **Expression engine**: Безопасный интерпретатор условий (не произвольный код!)
- **Статический анализ**: Детекция циклов, ошибок ссылок, лимитов
- **Версионирование**: Политика schema-per-version

---

### Этап 11: Метрики и аналитика

**📝 ОБЯЗАТЕЛЬНО ПЕРЕД НАЧАЛОМ**: Загляни в черновик!

| Черновик | Что полезного |
|----------|---------------|
| [ORCHESTRATOR_BRAIN_DYNAMICS_v0.1](../concepts/drafts/ORCHESTRATOR_BRAIN_DYNAMICS_v0.1.md) | Self-Reflection |

**💎 Ключевые идеи**:
- **Причинно-следственные связи**: "Шаг 5 занял 10 итераций, потому что в Шаге 2 была неопределённость"
- **Post-mortem отчёты**: Автоматически предлагает правки в Process Card
- **Улучшение карточек**: "Повысить требования к этапу X, т.к. 70% retry rate"

---

### Этап 12: Масштабирование
Распределённая шина, кластеризация.

---

## 🎯 Текущий фокус

**Сейчас мы на**: ✅ Этапы 1-4 ЗАВЕРШЕНЫ

**Следующий шаг**: Этап 5 — Storage (хранение данных)

**Что сделано (2025-12-18)**:
- ✅ Этап 1: MindBus + Registry + Storage + Orchestrator MVP
- ✅ Этап 2: API Gateway + E2E тесты
- ✅ Этап 3: Process Cards (5 примеров)
- ✅ Этап 4: Творческая команда (5 агентов с реальным AI)
- ✅ **Всего: 211 тестов пройдено** (97.7% success rate)

**Что работает**:
- CLI: `./venv/bin/python -m src.cli demo`
- API: `./venv/bin/python scripts/run_api_server.py`
- Process Cards: 6 готовых шаблонов (включая creative_project)
- Web Monitor: `./venv/bin/python -m src.web.monitor` → http://localhost:8080
- Творческая команда: 5 агентов online, коммуникация через RabbitMQ

**Творческая команда запущена**:
```bash
# Запустить монитор
./venv/bin/python -m src.web.monitor

# Запустить агента (в отдельном терминале)
./venv/bin/python -c "from src.agents.creative_agent import ResearcherAgent; ResearcherAgent().start()"
```

**Цель Этапа 5**: Добавить постоянное хранение для артефактов и истории.

---

## 📝 Принципы работы с этим планом

1. **Последовательно**: Не переходим к следующему этапу, пока не завершён текущий
2. **Инкрементально**: Каждый шаг добавляет небольшой кусок функционала
3. **Тестируемо**: Каждый этап завершается сквозным тестом
4. **Гибко**: План может корректироваться по мере получения опыта
5. **Практика → Спецификация**: Сначала проверяем на практике, потом создаём сложные спецификации (Orchestrator)
6. **Ретроспективы**: После каждого этапа анализируем опыт и обновляем документы

---

## 🔗 Связанные документы

### Основные
- [README.md](../../README.md) — концепция проекта
- [CLAUDE.md](../../CLAUDE.md) — правила разработки
- [QUESTIONS.md](../../QUESTIONS.md) — открытые вопросы и задачи
- [docs/SSOT/](../SSOT/) — канонические спецификации
- [docs/audits/SSOT_AUDIT_2025-12-15.md](../audits/SSOT_AUDIT_2025-12-15.md) — аудит спецификаций

### 📝 Черновики для будущих этапов

> **ВНИМАНИЕ**: Черновики НЕ являются частью SSOT и требуют проверки перед интеграцией.
> См. [docs/concepts/drafts/README.md](../concepts/drafts/README.md)

| Черновик | Релевантные этапы | Ключевые идеи |
|----------|-------------------|---------------|
| [ORCHESTRATOR_COGNITIVE_STACK_v0.1](../concepts/drafts/ORCHESTRATOR_COGNITIVE_STACK_v0.1.md) | Этап 7, 8 | State-Machine, Resource Budget, Triangle of Quality |
| [ORCHESTRATOR_BRAIN_DYNAMICS_v0.1](../concepts/drafts/ORCHESTRATOR_BRAIN_DYNAMICS_v0.1.md) | Этап 7, 11 | Quality Loop, Self-Reflection, Post-mortem |
| [ORCHESTRATOR_CONCEPT_v0.2](../concepts/drafts/ORCHESTRATOR_CONCEPT_v0.2.md) | Этап 8 | Human-in-the-loop режимы A/B/C, Протокол эскалации |
| [PROCESS_CARD_INTERPRETER_v0.2](../concepts/drafts/PROCESS_CARD_INTERPRETER_v0.2.md) | Этап 10 | IR/AST, Expression engine, Static analysis |
| [AGENT_HUMAN_NAMES_v0.1](../concepts/drafts/AGENT_HUMAN_NAMES_v0.1.md) | Этап 4, 6 | Display Identity через Монитор |

---

**Последнее обновление**: 2025-12-19

**Версия**: 2.3 (добавлены напоминалки "📝 ОБЯЗАТЕЛЬНО ПЕРЕД НАЧАЛОМ" для всех этапов)
