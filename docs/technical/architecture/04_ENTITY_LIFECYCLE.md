# Жизненный цикл сущностей (Entity Lifecycle)

**Версия**: 1.0
**Дата**: 2025-12-14
**Статус**: Проектирование

---

## 📋 Оглавление

1. [Введение](#введение)
2. [Жизненный цикл Task (Задача)](#жизненный-цикл-task-задача)
3. [Жизненный цикл Agent (Агент)](#жизненный-цикл-agent-агент)
4. [Жизненный цикл Process Card](#жизненный-цикл-process-card)
5. [Жизненный цикл Message (Сообщение)](#жизненный-цикл-message-сообщение)
6. [Жизненный цикл Artifact (Артефакт)](#жизненный-цикл-artifact-артефакт)
7. [Взаимосвязи между сущностями](#взаимосвязи-между-сущностями)

---

## Введение

Этот документ описывает **жизненный цикл ключевых сущностей** системы AI_TEAM — от создания до завершения/удаления.

**Ключевые сущности**:
- **Task** — задача, которую выполняет система
- **Agent** — AI-агент, исполнитель задач
- **Process Card** — карточка процесса, инструкция по выполнению
- **Message** — сообщение в MindBus
- **Artifact** — результат работы (файл, текст, данные)

---

## Жизненный цикл Task (Задача)

### Диаграмма состояний

```
┌─────────┐
│  START  │
└────┬────┘
     │
     │ Человек ставит задачу
     ▼
┌──────────┐
│ CREATED  │ ◄─────────────────────┐
└────┬─────┘                       │
     │                             │
     │ Orchestrator анализирует    │ Возврат на доработку
     ▼                             │
┌──────────┐                       │
│ PLANNED  │                       │
└────┬─────┘                       │
     │                             │
     │ Запуск выполнения           │
     ▼                             │
┌──────────────┐                   │
│ IN_PROGRESS  │                   │
└────┬─────────┘                   │
     │                             │
     │ Агенты закончили работу     │
     ▼                             │
┌──────────────┐                   │
│QUALITY_CHECK │                   │
└────┬─────────┘                   │
     │                             │
     ├─► Качество НЕ ОК ───────────┘
     │
     │ Качество ОК
     ▼
┌──────────┐
│COMPLETED │
└────┬─────┘
     │
     ▼
┌─────────┐
│   END   │
└─────────┘

Альтернативный путь при ошибке:
IN_PROGRESS ──► ERROR ──► FAILED ──► END
```

---

### Состояния Task

#### 1. CREATED (Создана)

**Что происходит**:
- Человек отправляет запрос через API
- API Gateway создаёт Task с уникальным ID
- Генерируется trace_id для трассировки
- Task сохраняется в Operational Storage
- Отправляется EVENT `task_created` в MindBus

**Данные Task**:
```yaml
Task:
  id: "task_abc123"
  status: "CREATED"
  description: "Напиши статью о трендах AI"
  created_by: "user_john"
  created_at: "2025-12-14T10:00:00Z"
  constraints:
    max_cost: 1.0
    max_time: 300
  context:
    trace_id: "trace_xyz789"
```

**Переходы**:
- → `PLANNED` — когда Orchestrator проанализировал задачу
- → `FAILED` — если задача некорректна (редко)

---

#### 2. PLANNED (Спланирована)

**Что происходит**:
- Orchestrator получил EVENT `task_created`
- Проанализировал описание задачи
- Выбрал или создал Process Card
- Составил план выполнения (шаги, агенты, зависимости)
- Обновил Task

**Данные Task**:
```yaml
Task:
  id: "task_abc123"
  status: "PLANNED"
  process_card_id: "pc_article_v1.2.0"
  plan:
    steps:
      - id: "step_1"
        agent_role: "Writer"
        action: "generate_article"
        status: "PENDING"
  planned_at: "2025-12-14T10:00:01Z"
```

**Переходы**:
- → `IN_PROGRESS` — когда начато выполнение первого шага
- → `FAILED` — если невозможно спланировать (нет подходящих агентов)

---

#### 3. IN_PROGRESS (Выполняется)

**Что происходит**:
- Orchestrator отправляет COMMAND агентам
- Агенты обрабатывают подзадачи
- Orchestrator отслеживает прогресс
- Обновляется статус каждого шага
- Промежуточные результаты сохраняются в контекст

**Данные Task**:
```yaml
Task:
  id: "task_abc123"
  status: "IN_PROGRESS"
  plan:
    steps:
      - id: "step_1"
        status: "COMPLETED"  # ✓
        result: { artifact_id: "..." }
      - id: "step_2"
        status: "IN_PROGRESS"  # ⏳
        agent_id: "writer_agent_1"
  progress: 0.5  # 50%
  started_at: "2025-12-14T10:00:02Z"
```

**Переходы**:
- → `QUALITY_CHECK` — все шаги завершены, проверка качества
- → `ERROR` — критическая ошибка при выполнении
- → `CANCELLED` — человек отменил задачу

---

#### 4. QUALITY_CHECK (Проверка качества)

**Что происходит**:
- Orchestrator получил результаты от всех агентов
- Quality Controller оценивает результаты
- Сравнивает с критериями из Process Card
- Принимает решение: принять или доработать

**Данные Task**:
```yaml
Task:
  id: "task_abc123"
  status: "QUALITY_CHECK"
  quality_evaluation:
    criteria:
      - name: "Отсутствие ошибок"
        status: "PASS"
      - name: "Соответствие теме"
        status: "PASS"
      - name: "Качество текста"
        status: "FAIL"  # ❌
        score: 6.5
        threshold: 8.0
    iteration: 1
```

**Переходы**:
- → `COMPLETED` — качество удовлетворительное
- → `REWORK` → `IN_PROGRESS` — нужна доработка
- → `FAILED` — превышен лимит итераций или бюджет

---

#### 5. REWORK (На доработке)

**Что происходит**:
- Quality Controller решил, что нужно улучшение
- Формируется feedback для агента
- Счётчик итераций увеличивается
- Проверяются лимиты (макс. итераций, бюджет)
- Если лимиты не превышены → возврат в IN_PROGRESS

**Данные Task**:
```yaml
Task:
  id: "task_abc123"
  status: "REWORK"
  rework_feedback:
    iteration: 1
    issues:
      - "Повысить профессиональность изложения"
      - "Добавить технические примеры"
    target_step: "step_2"
```

**Переходы**:
- → `IN_PROGRESS` — агент начинает улучшение
- → `FAILED` — лимиты превышены
- → `HUMAN_CHECKPOINT` — требуется решение человека

---

#### 6. HUMAN_CHECKPOINT (Требуется человек)

**Что происходит**:
- Process Card содержит checkpoint
- Orchestrator приостанавливает выполнение
- Отправляет NOTIFICATION человеку (High priority)
- Ждёт ответа с таймаутом
- Человек принимает решение: одобрить / доработать / отменить

**Данные Task**:
```yaml
Task:
  id: "task_abc123"
  status: "HUMAN_CHECKPOINT"
  checkpoint:
    step_id: "review_strategy"
    question: "Одобрить черновик стратегии?"
    options: ["approve", "request_changes", "reject"]
    timeout_at: "2025-12-15T10:00:00Z"  # 24 часа
    paused_at: "2025-12-14T10:05:00Z"
```

**Переходы**:
- → `IN_PROGRESS` — человек одобрил, продолжить
- → `REWORK` — человек запросил изменения
- → `CANCELLED` — человек отменил
- → `TIMEOUT` — человек не ответил вовремя (действие по умолчанию)

---

#### 7. COMPLETED (Завершена успешно)

**Что происходит**:
- Все шаги выполнены
- Качество удовлетворительное
- Результаты сохранены в Storage
- Process Card может быть сохранена в Knowledge Base
- Отправляется EVENT `task_completed`
- Человек получает уведомление

**Данные Task**:
```yaml
Task:
  id: "task_abc123"
  status: "COMPLETED"
  result:
    artifact_id: "artifact_001"
    artifact_uri: "storage://artifacts/task_abc123/article.md"
  metrics:
    total_duration_seconds: 45.5
    total_cost_usd: 0.85
    iterations: 2
    steps_executed: 5
  completed_at: "2025-12-14T10:00:45Z"
```

**Финальное состояние** — дальнейших переходов нет.

---

#### 8. FAILED (Завершена с ошибкой)

**Что происходит**:
- Произошла критическая ошибка
- Превышены лимиты (бюджет, итерации, время)
- Невозможно выполнить задачу
- Отправляется EVENT `task_failed`
- Человек получает уведомление с деталями ошибки

**Данные Task**:
```yaml
Task:
  id: "task_abc123"
  status: "FAILED"
  error:
    code: "BUDGET_EXCEEDED"
    message: "Превышен бюджет: $1.20 > $1.00"
    timestamp: "2025-12-14T10:02:00Z"
    recoverable: false
  metrics:
    total_cost_usd: 1.20
    iterations: 3
  failed_at: "2025-12-14T10:02:00Z"
```

**Финальное состояние** — дальнейших переходов нет.

---

#### 9. CANCELLED (Отменена)

**Что происходит**:
- Человек отправил команду остановки
- Orchestrator останавливает все активные агенты
- Промежуточные результаты сохраняются (опционально)
- Отправляется EVENT `task_cancelled`

**Данные Task**:
```yaml
Task:
  id: "task_abc123"
  status: "CANCELLED"
  cancelled_by: "user_john"
  cancelled_at: "2025-12-14T10:01:30Z"
  cancellation_reason: "Изменились требования"
  partial_results: [...]  # Что успели сделать
```

**Финальное состояние** — дальнейших переходов нет.

---

### Таймлайн жизни Task

```
00:00 │ CREATED          │ Создана человеком
00:01 │ PLANNED          │ План составлен
00:02 │ IN_PROGRESS      │ Writer работает
00:10 │ QUALITY_CHECK    │ Проверка качества
00:11 │ REWORK           │ Качество низкое (6/10)
00:12 │ IN_PROGRESS      │ Writer улучшает
00:25 │ QUALITY_CHECK    │ Повторная проверка
00:26 │ COMPLETED        │ Качество OK (8.5/10) ✓
```

---

## Жизненный цикл Agent (Агент)

### Диаграмма состояний

```
┌─────────┐
│  START  │
└────┬────┘
     │ Запуск агента
     ▼
┌──────────┐
│ STARTING │
└────┬─────┘
     │ Регистрация в системе
     ▼
┌──────────┐
│AVAILABLE │ ◄─────────┐
└────┬─────┘           │
     │                 │ Задача завершена
     │ Получена задача │
     ▼                 │
┌──────────┐           │
│   BUSY   │───────────┘
└────┬─────┘
     │ Все задачи завершены,
     │ но лимит достигнут
     ▼
┌────────────┐
│ OVERLOADED │
└────┬───────┘
     │ Задачи завершились
     │
     ▼
┌──────────┐
│AVAILABLE │
└──────────┘

Альтернативные состояния:
UNAVAILABLE ─ агент временно недоступен (обслуживание, ошибка)
ERROR ─ критическая ошибка
STOPPED ─ агент остановлен
```

---

### Состояния Agent

#### 1. STARTING (Запускается)

**Что происходит**:
- Агент запускается (процесс, контейнер)
- Инициализирует подключение к MindBus
- Загружает конфигурацию
- Готовится к регистрации

**Данные Agent**:
```yaml
Agent:
  id: "writer_agent_1"
  status: "STARTING"
  started_at: "2025-12-14T09:55:00Z"
```

**Переходы**:
- → `AVAILABLE` — успешная регистрация
- → `ERROR` — ошибка при запуске

---

#### 2. AVAILABLE (Доступен)

**Что происходит**:
- Агент отправил EVENT `agent_registered`
- Orchestrator добавил агента в реестр
- Агент подписан на COMMAND в MindBus
- Ожидает задач

**Данные Agent**:
```yaml
Agent:
  id: "writer_agent_1"
  status: "AVAILABLE"
  role: "Writer"
  capabilities: ["generate_article", "improve_text"]
  current_load: 0
  max_concurrent_tasks: 3
  registered_at: "2025-12-14T09:55:05Z"
  last_heartbeat: "2025-12-14T10:00:00Z"
```

**Переходы**:
- → `BUSY` — получена задача
- → `UNAVAILABLE` — временно отключён
- → `STOPPED` — остановка агента

---

#### 3. BUSY (Занят)

**Что происходит**:
- Агент получил COMMAND от Orchestrator
- Обрабатывает задачу (вызов LLM API)
- Может принимать ещё задачи (если load < max)
- Отправляет heartbeat сообщения

**Данные Agent**:
```yaml
Agent:
  id: "writer_agent_1"
  status: "BUSY"
  current_load: 2
  max_concurrent_tasks: 3
  active_tasks:
    - task_id: "task_abc123"
      started_at: "2025-12-14T10:00:02Z"
    - task_id: "task_def456"
      started_at: "2025-12-14T10:00:15Z"
  last_heartbeat: "2025-12-14T10:00:30Z"
```

**Переходы**:
- → `AVAILABLE` — все задачи завершены
- → `OVERLOADED` — достигнут max_concurrent_tasks
- → `ERROR` — критическая ошибка при выполнении

---

#### 4. OVERLOADED (Перегружен)

**Что происходит**:
- Агент работает на максимуме (load == max)
- Не принимает новые задачи
- Orchestrator не будет назначать задачи этому агенту
- Продолжает выполнять текущие задачи

**Данные Agent**:
```yaml
Agent:
  id: "writer_agent_1"
  status: "OVERLOADED"
  current_load: 3
  max_concurrent_tasks: 3
  queue_full_since: "2025-12-14T10:01:00Z"
```

**Переходы**:
- → `BUSY` — одна задача завершилась
- → `AVAILABLE` — все задачи завершены

---

#### 5. UNAVAILABLE (Недоступен)

**Что происходит**:
- Агент временно не работает
- Причины: обновление, обслуживание, потеря связи
- Orchestrator не назначает задачи
- Текущие задачи могут быть переназначены

**Данные Agent**:
```yaml
Agent:
  id: "writer_agent_1"
  status: "UNAVAILABLE"
  unavailable_reason: "Обновление модели"
  unavailable_since: "2025-12-14T10:05:00Z"
  expected_return: "2025-12-14T10:10:00Z"
```

**Переходы**:
- → `AVAILABLE` — агент вернулся в работу
- → `ERROR` — проблема не решена
- → `STOPPED` — агент остановлен

---

#### 6. ERROR (Ошибка)

**Что происходит**:
- Критическая ошибка в агенте
- Отправка EVENT `agent_error`
- Orchestrator исключает агента из реестра
- Логирование ошибки
- Возможно автоматическое перезапуск

**Данные Agent**:
```yaml
Agent:
  id: "writer_agent_1"
  status: "ERROR"
  error:
    code: "LLM_API_UNREACHABLE"
    message: "Не удалось подключиться к OpenAI API"
    timestamp: "2025-12-14T10:03:00Z"
    recoverable: true
  retry_count: 2
```

**Переходы**:
- → `STARTING` — автоматический перезапуск
- → `STOPPED` — ошибка фатальная

---

#### 7. STOPPED (Остановлен)

**Что происходит**:
- Агент штатно остановлен
- Отправляет EVENT `agent_unregistered`
- Завершает текущие задачи (graceful shutdown)
- Orchestrator удаляет из реестра

**Данные Agent**:
```yaml
Agent:
  id: "writer_agent_1"
  status: "STOPPED"
  stopped_at: "2025-12-14T10:10:00Z"
  stopped_reason: "Штатное завершение"
```

**Финальное состояние** — агент завершён.

---

### Heartbeat механизм

Агент отправляет heartbeat каждые N секунд:

```yaml
EVENT:
  event_name: "agent_heartbeat"
  agent:
    id: "writer_agent_1"
    status: "BUSY"
    current_load: 2
    timestamp: "2025-12-14T10:00:30Z"
```

Orchestrator отслеживает:
- Если heartbeat не пришёл > timeout → агент считается UNAVAILABLE
- Автоматическая эскалация или переназначение задач

---

## Жизненный цикл Process Card

### Диаграмма состояний

```
┌─────────┐
│  START  │
└────┬────┘
     │ Создание новой карточки
     ▼
┌──────────┐
│  DRAFT   │
└────┬─────┘
     │ Тестирование и валидация
     ▼
┌──────────┐
│  ACTIVE  │ ◄─────────┐
└────┬─────┘           │ Изменения
     │                 │
     │ Новая версия    │
     ▼                 │
┌──────────┐           │
│DEPRECATED│───────────┘
└────┬─────┘
     │ Удаление старых версий
     ▼
┌──────────┐
│ ARCHIVED │
└──────────┘
```

---

### Состояния Process Card

#### 1. DRAFT (Черновик)

**Что происходит**:
- Orchestrator создал новую Process Card
- Или человек вручную создал
- Ещё не протестирована
- Не используется в production

**Данные**:
```yaml
ProcessCard:
  id: "pc_article_draft_001"
  name: "Создание технической статьи"
  version: "0.1.0-draft"
  status: "DRAFT"
  created_at: "2025-12-14T10:00:00Z"
  created_by: "orchestrator" | "user_john"
  steps: [...]
```

**Переходы**:
- → `ACTIVE` — после тестирования
- → удаление — если не подошла

---

#### 2. ACTIVE (Активная)

**Что происходит**:
- Process Card протестирована и работает
- Используется Orchestrator для задач
- Собирает метрики эффективности
- Может быть улучшена

**Данные**:
```yaml
ProcessCard:
  id: "pc_article_v1"
  name: "Создание технической статьи"
  version: "1.0.0"
  status: "ACTIVE"
  activated_at: "2025-12-14T11:00:00Z"
  metrics:
    total_runs: 45
    success_rate: 0.93  # 93%
    avg_cost_usd: 0.75
    avg_duration_seconds: 120
  last_used_at: "2025-12-14T15:30:00Z"
```

**Переходы**:
- → `DEPRECATED` — создана новая версия
- → `ARCHIVED` — больше не нужна

---

#### 3. DEPRECATED (Устарела)

**Что происходит**:
- Создана новая версия Process Card
- Старая версия помечена устаревшей
- Orchestrator предпочитает новую версию
- Старая версия доступна для обратной совместимости

**Данные**:
```yaml
ProcessCard:
  id: "pc_article_v1"
  version: "1.0.0"
  status: "DEPRECATED"
  deprecated_at: "2025-12-20T10:00:00Z"
  replaced_by: "pc_article_v2"  # ID новой версии
  deprecation_reason: "Добавлен шаг SEO-оптимизации"
```

**Переходы**:
- → `ARCHIVED` — через N дней после deprecation
- → `ACTIVE` — если новая версия оказалась хуже (rollback)

---

#### 4. ARCHIVED (Архивирована)

**Что происходит**:
- Process Card больше не используется
- Сохранена для истории
- Доступна только для чтения
- Может быть восстановлена при необходимости

**Данные**:
```yaml
ProcessCard:
  id: "pc_article_v1"
  version: "1.0.0"
  status: "ARCHIVED"
  archived_at: "2026-01-15T10:00:00Z"
  total_lifetime_runs: 523
```

**Финальное состояние** (но может быть восстановлена).

---

### Версионирование Process Cards

```
ProcessCard "Создание статьи":
  v1.0.0 [DEPRECATED] ─ простая генерация текста
     │
     ├─► v1.1.0 [DEPRECATED] ─ добавлена проверка качества
     │      │
     │      └─► v1.2.0 [ACTIVE] ─ добавлен шаг research
     │
     └─► v2.0.0 [ACTIVE] ─ полностью новый процесс с SEO
```

---

## Жизненный цикл Message (Сообщение)

### Диаграмма потока

```
┌─────────┐
│ CREATED │
└────┬────┘
     │ Компонент создаёт сообщение
     ▼
┌──────────┐
│ QUEUED   │
└────┬─────┘
     │ Помещено в очередь MindBus
     ▼
┌──────────┐
│ ROUTING  │
└────┬─────┘
     │ Определение получателей
     ▼
┌───────────┐
│ DELIVERED │
└────┬──────┘
     │ Доставлено получателям
     ▼
┌───────────┐
│ PROCESSED │
└────┬──────┘
     │ Получатель обработал
     ▼
┌──────────┐
│ ARCHIVED │
└──────────┘
```

---

### Время жизни Message

**Создание** (0 мс):
```yaml
Message:
  id: "msg_001"
  status: "CREATED"
  created_at: "2025-12-14T10:00:00.000Z"
```

**Очередь** (+5 мс):
```yaml
Message:
  id: "msg_001"
  status: "QUEUED"
  queued_at: "2025-12-14T10:00:00.005Z"
  queue_position: 15
  priority: 5
```

**Маршрутизация** (+10 мс):
```yaml
Message:
  id: "msg_001"
  status: "ROUTING"
  routing_started_at: "2025-12-14T10:00:00.010Z"
  target_subscribers: ["orchestrator", "monitor"]
```

**Доставка** (+15 мс):
```yaml
Message:
  id: "msg_001"
  status: "DELIVERED"
  delivered_at: "2025-12-14T10:00:00.015Z"
  delivery_attempts: 1
```

**Обработка** (+50 мс):
```yaml
Message:
  id: "msg_001"
  status: "PROCESSED"
  processed_at: "2025-12-14T10:00:00.050Z"
  processing_time_ms: 35
```

**Архивирование** (+100 мс):
```yaml
Message:
  id: "msg_001"
  status: "ARCHIVED"
  archived_at: "2025-12-14T10:00:00.100Z"
  archived_to: "audit_log"
```

---

### Retry механизм

Если доставка не удалась:

```
ATTEMPT 1 ──► FAILED
   │
   ├─► wait 1s
   │
   ▼
ATTEMPT 2 ──► FAILED
   │
   ├─► wait 2s (exponential backoff)
   │
   ▼
ATTEMPT 3 ──► SUCCESS ─► DELIVERED
```

После N попыток → сообщение в Dead Letter Queue (DLQ).

---

## Жизненный цикл Artifact (Артефакт)

### Диаграмма состояний

```
┌─────────┐
│ CREATED │
└────┬────┘
     │ Агент создал результат
     ▼
┌──────────┐
│  STORED  │
└────┬─────┘
     │ Сохранён в Storage
     ▼
┌──────────┐
│  ACTIVE  │ ◄─────────┐
└────┬─────┘           │ Доступ
     │                 │
     │ Задача завершена│
     ▼                 │
┌───────────┐          │
│ COMPLETED │──────────┘
└────┬──────┘
     │ Retention период истёк
     ▼
┌──────────┐
│ ARCHIVED │
└────┬─────┘
     │ Удаление
     ▼
┌─────────┐
│ DELETED │
└─────────┘
```

---

### Состояния Artifact

#### 1. CREATED

**Что происходит**:
- Агент создал результат (текст, файл)
- Ещё не сохранён в Storage
- Существует только в памяти агента

---

#### 2. STORED

**Что происходит**:
- Отправлен COMMAND `store_artifact`
- Storage Adapter сохранил в DB/S3
- Получен artifact_id
- Создана запись в Artifact Registry

**Данные**:
```yaml
Artifact:
  id: "artifact_001"
  type: "text"
  format: "markdown"
  uri: "storage://artifacts/task_abc/article.md"
  size_bytes: 3420
  checksum: "sha256:a1b2c3..."
  created_by: "writer_agent_1"
  task_id: "task_abc123"
  created_at: "2025-12-14T10:00:11Z"
  status: "STORED"
```

---

#### 3. ACTIVE

**Что происходит**:
- Артефакт используется в системе
- К нему обращаются другие компоненты
- Отслеживается количество доступов

**Данные**:
```yaml
Artifact:
  id: "artifact_001"
  status: "ACTIVE"
  access_count: 5
  last_accessed_at: "2025-12-14T10:05:00Z"
```

---

#### 4. COMPLETED

**Что происходит**:
- Задача завершена
- Артефакт — финальный результат
- Доступен для скачивания человеку
- Начинается retention период

**Данные**:
```yaml
Artifact:
  id: "artifact_001"
  status: "COMPLETED"
  retention_policy: "30_days"
  delete_after: "2025-01-14T10:00:00Z"
```

---

#### 5. ARCHIVED

**Что происходит**:
- Retention период истёк
- Артефакт перемещён в cold storage
- Доступ медленнее, но дешевле
- Может быть восстановлен

---

#### 6. DELETED

**Что происходит**:
- Артефакт удалён из системы
- Физические файлы удалены
- Остаётся только метаданные в логах

---

## Взаимосвязи между сущностями

### Связи через ID

```
┌────────────────────────────────────────────────┐
│                  TRACE_ID                      │
│         "trace_xyz789"                         │
│                                                │
│  Связывает все сущности одной задачи          │
└─────────┬──────────────────────────────────────┘
          │
          ├─► Task: task_abc123
          │    │
          │    ├─► Process Card: pc_article_v1
          │    │
          │    ├─► Messages:
          │    │    • msg_001 (EVENT task_created)
          │    │    • msg_002 (COMMAND generate_article)
          │    │    • msg_003 (RESULT article_generated)
          │    │    • ...
          │    │
          │    ├─► Agents:
          │    │    • writer_agent_1
          │    │    • editor_agent_1
          │    │
          │    └─► Artifacts:
          │         • artifact_001 (статья)
          │         • artifact_002 (research данные)
          │
          └─► Audit Log: все события с trace_id
```

---

### Граф зависимостей

```
Task (task_abc123)
  │
  ├─► использует Process Card (pc_article_v1)
  │     │
  │     └─► определяет шаги и агентов
  │
  ├─► порождает Messages
  │     ├─► msg_001 → msg_002 → msg_003
  │     └─► все с trace_id = trace_xyz789
  │
  ├─► назначается Agents
  │     ├─► writer_agent_1 (выполняет)
  │     └─► editor_agent_1 (проверяет)
  │
  └─► создаёт Artifacts
        ├─► artifact_001 (результат)
        └─► artifact_002 (промежуточные данные)
```

---

## Заключение

Этот документ описывает полный жизненный цикл ключевых сущностей системы:

✅ **Task** — от создания до завершения с учётом циклов улучшения
✅ **Agent** — от запуска до остановки с heartbeat механизмом
✅ **Process Card** — версионирование и эволюция процессов
✅ **Message** — путь сообщения через MindBus
✅ **Artifact** — сохранение и retention результатов

**Все сущности связаны через**:
- `trace_id` — сквозная трассировка
- `task_id` — принадлежность к задаче
- `parent_message_id` — иерархия сообщений

---

## Документация завершена

1. ✅ [Общая схема системы](01_SYSTEM_OVERVIEW.md)
2. ✅ [Путь пользователя](02_USER_JOURNEY.md)
3. ✅ [Потоки данных](03_DATA_FLOW.md)
4. ✅ [Жизненный цикл сущностей](04_ENTITY_LIFECYCLE.md) — этот документ

---

**Статус**: ✅ Готово
**Последнее обновление**: 2025-12-14
