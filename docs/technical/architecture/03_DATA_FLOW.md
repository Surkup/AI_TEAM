# Потоки данных (Data Flow)

**Версия**: 1.0
**Дата**: 2025-12-14
**Статус**: Проектирование

---

## 📋 Оглавление

1. [Введение](#введение)
2. [Основные потоки данных](#основные-потоки-данных)
3. [Поток 1: Создание и выполнение задачи](#поток-1-создание-и-выполнение-задачи)
4. [Поток 2: Коммуникация через MindBus](#поток-2-коммуникация-через-mindbus)
5. [Поток 3: Работа со Storage](#поток-3-работа-со-storage)
6. [Поток 4: Регистрация и обнаружение агентов](#поток-4-регистрация-и-обнаружение-агентов)
7. [Поток 5: Трассировка и мониторинг](#поток-5-трассировка-и-мониторинг)
8. [Форматы данных](#форматы-данных)

---

## Введение

Этот документ описывает **как данные перемещаются** между компонентами системы AI_TEAM.

**Ключевые принципы**:
- Все данные проходят через **MindBus**
- Используется **канонический формат Message**
- Большие данные передаются как **ссылки**, не inline
- Каждое сообщение имеет **trace_id** для трассировки

---

## Основные потоки данных

### Схема основных потоков

```
┌─────────┐
│ Человек │
└────┬────┘
     │
     │ 1. HTTP Request (Task)
     ▼
┌──────────────┐
│ API Gateway  │
└──────┬───────┘
       │
       │ 2. EVENT (task_created)
       ▼
┌──────────────────────────────────────┐
│           MindBus                    │
│  ┌────────────────────────────────┐  │
│  │ Message Queue & Router         │  │
│  │ • Маршрутизация               │  │
│  │ • Приоритеты                  │  │
│  │ • Трассировка                 │  │
│  └────────────────────────────────┘  │
└───┬──────┬──────┬──────┬──────┬──────┘
    │      │      │      │      │
    │ 3a   │ 3b   │ 3c   │ 3d   │ 3e
    ▼      ▼      ▼      ▼      ▼
┌────────┐┌────────┐┌────────┐┌────────┐┌────────┐
│Orches- ││ Agent  ││ Agent  ││Storage ││Monitor │
│trator  ││Writer  ││Research││Adapter ││        │
└───┬────┘└───┬────┘└───┬────┘└───┬────┘└───┬────┘
    │         │         │         │         │
    │ 4. COMMAND       │         │         │
    │────────►│         │         │         │
    │         │ 5. LLM API       │         │
    │         │────────────►      │         │
    │         │         │         │         │
    │         │ 6. RESULT        │         │
    │◄────────│         │         │         │
    │         │         │         │         │
    │ 7. COMMAND (store)          │         │
    │─────────────────────────────►         │
    │         │         │         │         │
    │         │         │         │ 8. Log  │
    │─────────────────────────────────────► │
    │         │         │         │         │
    │ 9. EVENT (task_completed)   │         │
    ▼─────────────────────────────────────► │
```

---

## Поток 1: Создание и выполнение задачи

### Диаграмма потока

```
ЧЕЛОВЕК → API GATEWAY → MINDBUS → ORCHESTRATOR → AGENT → STORAGE
   │           │           │           │           │         │
   │ POST      │ EVENT     │ COMMAND   │ LLM Call  │ Store   │
   │ /tasks    │ task_     │ generate_ │           │ artifact│
   │           │ created   │ article   │           │         │
   │           │           │           │           │         │
   │◄──────────┴───────────┴───────────┴───────────┴─────────┘
   │                  RESULT + NOTIFICATION
```

### Пошаговое описание данных

#### Шаг 1: HTTP Request от человека

**Направление**: `Человек → API Gateway`

**Формат**: HTTP POST
```json
POST /api/v1/tasks
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
Content-Type: application/json

{
  "description": "Напиши статью о трендах AI в 2025",
  "priority": "medium",
  "constraints": {
    "max_cost": 1.0,
    "max_time": 300
  }
}
```

**Ответ от API**:
```json
HTTP 202 Accepted
{
  "task_id": "task_abc123",
  "trace_id": "trace_xyz789",
  "status": "CREATED",
  "created_at": "2025-12-14T10:00:00Z"
}
```

---

#### Шаг 2: EVENT от API Gateway в MindBus

**Направление**: `API Gateway → MindBus`

**Формат**: Message (EVENT)
```yaml
Message:
  id: "msg_001"
  type: "EVENT"
  from: "api_gateway"
  to: "orchestrator"
  created_at: "2025-12-14T10:00:00.123Z"
  priority: 5  # medium

  context:
    trace_id: "trace_xyz789"
    task_id: "task_abc123"
    user_id: "user_john"

  payload:
    event_name: "task_created"
    task:
      id: "task_abc123"
      description: "Напиши статью о трендах AI в 2025"
      status: "CREATED"
      constraints:
        max_cost: 1.0
        max_time: 300
      created_by: "user_john"
      created_at: "2025-12-14T10:00:00Z"
```

---

#### Шаг 3: Orchestrator получает EVENT и отправляет COMMAND

**Направление**: `MindBus → Orchestrator`

Orchestrator получает сообщение `msg_001` через подписку.

**Orchestrator обрабатывает**:
1. Анализирует задачу
2. Выбирает Process Card
3. Определяет нужного агента (Writer)
4. Формирует COMMAND

**Направление**: `Orchestrator → MindBus → Writer Agent`

**Формат**: Message (COMMAND)
```yaml
Message:
  id: "msg_002"
  type: "COMMAND"
  from: "orchestrator"
  to: "writer_agent_1"
  created_at: "2025-12-14T10:00:01.456Z"
  priority: 5

  context:
    trace_id: "trace_xyz789"        # ← Тот же trace_id!
    task_id: "task_abc123"
    process_id: "proc_001"
    parent_message_id: "msg_001"    # Ссылка на EVENT

  payload:
    command: "generate_article"
    input:
      topic: "Тренды AI в 2025"
      length: "средняя (500-800 слов)"
      style: "информационный"
      tone: "профессиональный"
    expected_output:
      kind: "text"
      format: "markdown"
    constraints:
      max_cost: 0.5
      max_time: 120
```

---

#### Шаг 4: Writer Agent вызывает LLM API

**Направление**: `Writer Agent → OpenAI/Anthropic API`

**Формат**: HTTP POST (зависит от провайдера)

**Пример для OpenAI**:
```json
POST https://api.openai.com/v1/chat/completions
Authorization: Bearer sk-...
Content-Type: application/json

{
  "model": "gpt-4",
  "messages": [
    {
      "role": "system",
      "content": "Ты профессиональный технический писатель..."
    },
    {
      "role": "user",
      "content": "Напиши статью о трендах AI в 2025..."
    }
  ],
  "temperature": 0.7,
  "max_tokens": 1500
}
```

**Ответ от LLM**:
```json
{
  "id": "chatcmpl-abc123",
  "object": "chat.completion",
  "created": 1702551234,
  "model": "gpt-4",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "# Тренды искусственного интеллекта в 2025 году\n\n..."
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 120,
    "completion_tokens": 650,
    "total_tokens": 770
  }
}
```

---

#### Шаг 5: Writer Agent отправляет RESULT в MindBus

**Направление**: `Writer Agent → MindBus → Orchestrator`

**Формат**: Message (RESULT)
```yaml
Message:
  id: "msg_003"
  type: "RESULT"
  from: "writer_agent_1"
  to: "orchestrator"
  created_at: "2025-12-14T10:00:10.789Z"
  priority: 5

  context:
    trace_id: "trace_xyz789"        # ← Тот же trace_id!
    task_id: "task_abc123"
    process_id: "proc_001"
    parent_message_id: "msg_002"    # Ссылка на COMMAND

  payload:
    status: "SUCCESS"
    for_command_id: "msg_002"

    output:
      data:
        title: "Тренды искусственного интеллекта в 2025 году"
        content: "# Тренды искусственного интеллекта...(полный текст)"
        format: "markdown"
        word_count: 650

    metrics:
      duration_seconds: 8.5
      cost_usd: 0.12
      tokens_used: 770
      llm_provider: "openai"
      llm_model: "gpt-4"
```

---

#### Шаг 6: Orchestrator отправляет COMMAND на сохранение

**Направление**: `Orchestrator → MindBus → Storage Adapter`

**Формат**: Message (COMMAND)
```yaml
Message:
  id: "msg_004"
  type: "COMMAND"
  from: "orchestrator"
  to: "storage_adapter"
  created_at: "2025-12-14T10:00:11.123Z"
  priority: 5

  context:
    trace_id: "trace_xyz789"
    task_id: "task_abc123"
    parent_message_id: "msg_003"

  payload:
    command: "store_artifact"
    input:
      artifact_type: "text"
      content: "# Тренды искусственного интеллекта...(полный текст)"
      metadata:
        title: "Тренды искусственного интеллекта в 2025 году"
        task_id: "task_abc123"
        created_by: "writer_agent_1"
        format: "markdown"
```

---

#### Шаг 7: Storage возвращает RESULT с artifact_id

**Направление**: `Storage Adapter → MindBus → Orchestrator`

**Формат**: Message (RESULT)
```yaml
Message:
  id: "msg_005"
  type: "RESULT"
  from: "storage_adapter"
  to: "orchestrator"
  created_at: "2025-12-14T10:00:11.456Z"
  priority: 5

  context:
    trace_id: "trace_xyz789"
    task_id: "task_abc123"
    parent_message_id: "msg_004"

  payload:
    status: "SUCCESS"
    for_command_id: "msg_004"

    output:
      artifact_id: "artifact_001"
      artifact_uri: "storage://artifacts/task_abc123/article.md"
      checksum: "sha256:a1b2c3d4..."
      size_bytes: 3420
```

---

#### Шаг 8: Orchestrator отправляет EVENT о завершении

**Направление**: `Orchestrator → MindBus → [Все подписчики]`

**Формат**: Message (EVENT)
```yaml
Message:
  id: "msg_006"
  type: "EVENT"
  from: "orchestrator"
  to: "broadcast"  # Всем подписчикам
  created_at: "2025-12-14T10:00:11.789Z"
  priority: 6  # Выше среднего

  context:
    trace_id: "trace_xyz789"
    task_id: "task_abc123"

  payload:
    event_name: "task_completed"
    task:
      id: "task_abc123"
      status: "COMPLETED"
      result:
        artifact_id: "artifact_001"
        artifact_uri: "storage://artifacts/task_abc123/article.md"
      metrics:
        total_duration_seconds: 11.5
        total_cost_usd: 0.12
      completed_at: "2025-12-14T10:00:11Z"
```

---

#### Шаг 9: API Gateway уведомляет человека

**Направление**: `MindBus → API Gateway → WebSocket → Браузер`

API Gateway подписан на EVENT `task_completed` и отправляет уведомление.

**Формат**: WebSocket message
```json
{
  "type": "notification",
  "event": "task_completed",
  "data": {
    "task_id": "task_abc123",
    "status": "COMPLETED",
    "message": "Статья готова!",
    "artifact": {
      "id": "artifact_001",
      "url": "/api/v1/artifacts/artifact_001",
      "preview": "# Тренды искусственного интеллекта в 2025 году\n\n..."
    },
    "metrics": {
      "duration": "11.5s",
      "cost": "$0.12"
    }
  }
}
```

---

## Поток 2: Коммуникация через MindBus

### Архитектура MindBus

```
┌────────────────────────────────────────────────────────┐
│                      MindBus Core                      │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │           Message Queue (Очередь)                │ │
│  │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐         │ │
│  │  │ Pri 9│  │ Pri 7│  │ Pri 5│  │ Pri 3│  ...    │ │
│  │  │(Critic│  │(High)│  │(Med) │  │(Low) │         │ │
│  │  └──────┘  └──────┘  └──────┘  └──────┘         │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │              Router (Маршрутизатор)              │ │
│  │  • Адресация по полю 'to'                        │ │
│  │  • Фильтрация по 'type'                          │ │
│  │  • Broadcast для 'to=broadcast'                  │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │         Subscription Manager (Подписки)          │ │
│  │  orchestrator: [EVENT:task_created]              │ │
│  │  writer_agent_1: [COMMAND where to=self]         │ │
│  │  monitor: [*]  ← Все сообщения                   │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │            Audit Logger (Журнал)                 │ │
│  │  Логирует ВСЕ сообщения для трассировки          │ │
│  └──────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────┘
```

### Подписка и доставка

#### 1. Регистрация подписки

```python
# Пример: Orchestrator подписывается на события
mindbus.subscribe(
    subscriber_id="orchestrator",
    filters={
        "type": "EVENT",
        "payload.event_name": "task_created"
    },
    callback=orchestrator.handle_task_created
)
```

#### 2. Отправка сообщения

```python
# Пример: API Gateway отправляет событие
message = Message(
    id=generate_uuid(),
    type="EVENT",
    from_="api_gateway",
    to="orchestrator",
    payload={
        "event_name": "task_created",
        "task": {...}
    }
)
mindbus.send(message)
```

#### 3. MindBus обрабатывает

```
MindBus получает сообщение
  │
  ├─► Логирует в Audit Log
  │
  ├─► Помещает в очередь согласно priority
  │
  ├─► Router определяет получателей:
  │   • Фильтрует по 'to'
  │   • Проверяет подписки
  │
  └─► Доставляет всем подписчикам
      • orchestrator.handle_task_created(message)
      • monitor.log_message(message)
```

---

## Поток 3: Работа со Storage

### Сохранение артефакта

```
Agent → COMMAND(store) → MindBus → Storage Adapter → DB/S3
   │                                      │
   │                                      │ Write to disk/DB
   │                                      ▼
   │                                ┌──────────────┐
   │                                │ PostgreSQL   │
   │                                │   +          │
   │                                │ S3 / MinIO   │
   │                                └──────────────┘
   │                                      │
   │◄──── RESULT(artifact_id) ────────────┘
```

#### COMMAND для сохранения

```yaml
COMMAND:
  command: "store_artifact"
  input:
    artifact_type: "text" | "image" | "code" | "data"
    content: <inline для малых данных>
    content_ref: <ссылка для больших данных>
    metadata:
      title: "..."
      task_id: "..."
      tags: [...]
```

#### RESULT с идентификатором

```yaml
RESULT:
  output:
    artifact_id: "artifact_001"
    artifact_uri: "storage://artifacts/task_abc/file.md"
    checksum: "sha256:..."
    size_bytes: 3420
    storage_backend: "s3"
```

---

### Получение артефакта

```
Agent → QUERY(get_artifact) → MindBus → Storage Adapter → DB/S3
   │                                           │
   │                                           │ Read from disk/DB
   │                                           ▼
   │◄──── RESULT(artifact_data) ───────────────┘
```

#### QUERY для получения

```yaml
QUERY:
  query: "get_artifact"
  filters:
    artifact_id: "artifact_001"
  options:
    include_content: true  # или только метаданные
    format: "raw" | "base64"
```

#### RESULT с данными

```yaml
RESULT:
  output:
    artifact:
      id: "artifact_001"
      type: "text"
      content: "# Заголовок\n\n..." # или base64
      metadata: {...}
      created_at: "2025-12-14T10:00:00Z"
```

---

## Поток 4: Регистрация и обнаружение агентов

### Регистрация агента при запуске

```
Agent Start → EVENT(agent_registered) → MindBus → Orchestrator
                                                        │
                                                        ▼
                                                  ┌──────────────┐
                                                  │ Agent Registry│
                                                  │ (Реестр)     │
                                                  └──────────────┘
```

#### EVENT от агента

```yaml
EVENT:
  event_name: "agent_registered"
  agent:
    id: "writer_agent_1"
    role: "Writer"
    specialization: "Технические статьи"
    capabilities:
      - "generate_article"
      - "improve_text"
      - "translate_text"
    status: "AVAILABLE"
    max_concurrent_tasks: 3
    cost_per_1k_tokens: 0.002
```

---

### Orchestrator обновляет реестр

```
Agent Registry:
  agents:
    - id: "writer_agent_1"
      role: "Writer"
      status: "AVAILABLE"
      current_load: 0/3
      capabilities: [...]

    - id: "researcher_agent_1"
      role: "Researcher"
      status: "BUSY"
      current_load: 2/5
      capabilities: [...]
```

---

### Orchestrator выбирает агента для задачи

```
Orchestrator получает задачу "Создать статью"
  │
  ├─► Определяет нужную роль: "Writer"
  │
  ├─► Запрашивает реестр:
  │   • Роль: "Writer"
  │   • Статус: "AVAILABLE" или "BUSY" (если load < max)
  │   • Capability: "generate_article"
  │
  ├─► Получает список кандидатов:
  │   [writer_agent_1, writer_agent_2]
  │
  ├─► Выбирает по критериям:
  │   • Меньше загружен
  │   • Дешевле
  │   • Быстрее отвечает
  │
  └─► Выбран: writer_agent_1
      Отправляет COMMAND
```

---

## Поток 5: Трассировка и мониторинг

### Сквозная трассировка через trace_id

```
Человек → задача
  │
  ├─► Генерируется trace_id: "trace_xyz789"
  │
  └─► Все сообщения содержат этот trace_id

┌─────────────────────────────────────────────────┐
│ msg_001: EVENT task_created                     │
│   trace_id: trace_xyz789                        │
└────────────┬────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────┐
│ msg_002: COMMAND generate_article               │
│   trace_id: trace_xyz789                        │
│   parent_message_id: msg_001                    │
└────────────┬────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────┐
│ msg_003: RESULT article_generated               │
│   trace_id: trace_xyz789                        │
│   parent_message_id: msg_002                    │
└────────────┬────────────────────────────────────┘
             │
             ▼
         ... и так далее
```

---

### Monitor собирает все сообщения

```
Monitor подписан на ВСЕ сообщения (filter: *)
  │
  ├─► Получает каждое сообщение
  │
  ├─► Логирует в Audit Log
  │
  └─► Индексирует по trace_id для быстрого поиска

Audit Log:
  trace_xyz789:
    - msg_001: [timestamp] EVENT task_created
    - msg_002: [timestamp] COMMAND generate_article
    - msg_003: [timestamp] RESULT article_generated
    - msg_004: [timestamp] COMMAND store_artifact
    - msg_005: [timestamp] RESULT artifact_stored
    - msg_006: [timestamp] EVENT task_completed
```

---

### Визуализация потока

```
Monitor предоставляет API:
  GET /api/traces/trace_xyz789

Возвращает:
  {
    "trace_id": "trace_xyz789",
    "task_id": "task_abc123",
    "started_at": "2025-12-14T10:00:00Z",
    "completed_at": "2025-12-14T10:00:11Z",
    "duration_seconds": 11.5,
    "messages": [
      {
        "id": "msg_001",
        "type": "EVENT",
        "from": "api_gateway",
        "to": "orchestrator",
        "timestamp": "10:00:00.123"
      },
      {
        "id": "msg_002",
        "type": "COMMAND",
        "from": "orchestrator",
        "to": "writer_agent_1",
        "timestamp": "10:00:01.456"
      },
      ...
    ],
    "flow_diagram": "ASCII схема потока"
  }
```

---

## Форматы данных

### Канонический Message

```typescript
interface Message {
  // Обязательные поля
  id: string;                    // UUID
  type: MessageType;             // COMMAND | RESULT | EVENT | ...
  from: string;                  // Отправитель
  to: string;                    // Получатель
  created_at: string;            // ISO 8601 timestamp
  priority: number;              // 1-10

  // Контекст трассировки
  context: {
    trace_id: string;            // UUID сквозной трассировки
    task_id?: string;            // ID задачи
    process_id?: string;         // ID процесса
    step_id?: string;            // ID шага
    parent_message_id?: string;  // Ссылка на родительское сообщение
  };

  // Полезная нагрузка (зависит от type)
  payload: object;
}
```

---

### Payload для COMMAND

```typescript
interface CommandPayload {
  command: string;               // Имя команды
  input: {
    data?: object;               // Малые данные inline
    refs?: string[];             // Ссылки на артефакты
  };
  expected_output?: {
    kind: string;                // text | file | data | ...
    format?: string;             // markdown | json | ...
  };
  constraints?: {
    max_cost?: number;
    max_time?: number;
    quality_target?: string;
  };
}
```

---

### Payload для RESULT

```typescript
interface ResultPayload {
  status: "SUCCESS" | "FAILED" | "PARTIAL";
  for_command_id: string;        // Ссылка на команду

  output?: {
    data?: object;               // Результат inline
    refs?: string[];             // Ссылки на артефакты
  };

  metrics?: {
    duration_seconds?: number;
    cost_usd?: number;
    tokens_used?: number;
  };

  error?: {
    code: string;
    message: string;
    recoverable: boolean;
  };
}
```

---

### Payload для EVENT

```typescript
interface EventPayload {
  event_name: string;            // task_created | agent_registered | ...

  entity?: {
    kind: "TASK" | "AGENT" | "PROCESS" | ...;
    id: string;
  };

  state?: {
    from?: string;               // Предыдущее состояние
    to: string;                  // Новое состояние
  };

  details: object;               // Дополнительные данные
}
```

---

## Принципы работы с данными

### 1. **Ссылки вместо данных**

**Плохо** (передача больших данных inline):
```yaml
RESULT:
  output:
    data:
      image: "data:image/png;base64,iVBORw0KGgoAAAANS..." # 2 MB!
```

**Хорошо** (ссылка на Storage):
```yaml
RESULT:
  output:
    refs:
      - "storage://artifacts/task_abc/image.png"
```

---

### 2. **Идемпотентность**

Агент проверяет, не обработал ли он уже это сообщение:

```python
def handle_command(message):
    # Проверка дубликата
    if self.processed_messages.contains(message.id):
        # Возвращаем кэшированный результат
        return self.get_cached_result(message.id)

    # Обработка
    result = self.process_command(message)

    # Кэшируем результат
    self.cache_result(message.id, result)

    return result
```

---

### 3. **Валидация по SSOT**

Каждый компонент валидирует входящие данные:

```python
def handle_message(message):
    # Валидация структуры Message
    validate_message_schema(message)

    # Валидация payload согласно type
    if message.type == "COMMAND":
        validate_command_payload(message.payload)

    # Обработка
    ...
```

---

## Следующие шаги

1. ✅ Общая схема системы
2. ✅ Путь пользователя
3. ✅ Потоки данных (этот документ)
4. ⏭️ [Жизненный цикл сущностей](04_ENTITY_LIFECYCLE.md)

---

**Статус**: ✅ Готово
**Последнее обновление**: 2025-12-14
