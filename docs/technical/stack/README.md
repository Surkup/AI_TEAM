# Технический стек AI_TEAM

**Статус:** ✅ УТВЕРЖДЕНО
**Последнее обновление:** 2025-12-20
**Версия:** 2.1 (Добавлен Orchestrator на базе Temporal + LangGraph)

---

## Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     User Interface                           │
│                    (Web / API / CLI)                         │
└───────────────────────┬─────────────────────────────────────┘
                        │
              ┌─────────▼─────────┐
              │   API Gateway     │
              │    (FastAPI)      │
              └─────────┬─────────┘
                        │
    ┌───────────────────┼───────────────────┐
    │                   │                   │
┌───▼────────────────┐  ┌▼─────────────┐  ┌▼───────────────┐
│    ORCHESTRATOR    │  │Node Registry │  │    Storage     │
│ ┌────────────────┐ │  │ (etcd/Consul)│  │ (SQLite/MinIO) │
│ │   Temporal     │ │  └──────────────┘  └────────────────┘
│ │ (Durable Exec) │ │
│ ├────────────────┤ │
│ │   LangGraph    │ │
│ │ (AI Planning)  │ │
│ └────────────────┘ │
└───┬────────────────┘
    │
    │ Commands/Results via MindBus (RabbitMQ + CloudEvents)
    │
┌───▼──────────────────────────────────────────────┐
│         MindBus (RabbitMQ + AMQP 0-9-1)          │
│  Topic Exchange + Priority Queues                │
│  CloudEvents v1.0 message format                 │
└───┬──────────────────────────────────────────────┘
    │
    │ Subscribe to queues
    │
┌───┴────────────────────────────────────────────────┐
│                    Nodes (Agents)                   │
│  Каждый с Node Passport, регистрируется в Registry │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │  Writer  │  │  Critic  │  │  Editor  │   ...   │
│  └──────────┘  └──────────┘  └──────────┘         │
│                                                     │
│  ┌────────────────────────────────────┐            │
│  │    LLM Integration (LiteLLM)       │            │
│  │  OpenAI │ Anthropic │ Google       │            │
│  └────────────────────────────────────┘            │
└─────────────────────────────────────────────────────┘
```

**Ключевой принцип Orchestrator v2.1:**
> LangGraph (AI-логика) работает ВНУТРИ Temporal Activity (надёжное выполнение)

См: [ADR-001: Temporal + LangGraph](../../concepts/ADR-001_TEMPORAL_LANGGRAPH.md)

---

## Полный стек (Stack Overview)

| Компонент | Технология | Для чего | Файл с деталями |
|-----------|-----------|----------|----------------|
| **Язык программирования** | Python 3.11+ | Основной язык разработки | [01_programming_language.md](01_programming_language.md) |
| **MindBus (Шина данных)** | RabbitMQ (AMQP 0-9-1) + CloudEvents v1.0 | Коммуникация между компонентами | [02_mindbus.md](02_mindbus.md) + [docs/concepts/mindbus_protocol_v1.md](../../concepts/mindbus_protocol_v1.md) |
| **Node Management** | Node Passport + Node Registry (etcd/Consul) | Регистрация и поиск узлов по capabilities | [03_node_management.md](03_node_management.md) + [docs/SSOT/](../../SSOT/) |
| **Интеграция с LLM** | LiteLLM | Вызовы AI-моделей | [04_llm_integration.md](04_llm_integration.md) |
| **База данных & Хранилище** | SQLite + fsspec (MVP) → PostgreSQL + MinIO (Production) | Задачи, состояние, артефакты | [05_database_storage.md](05_database_storage.md) + [STORAGE_SPEC_v1.0.md](../../SSOT/STORAGE_SPEC_v1.0.md) |
| **Orchestrator (Workflow Engine)** | **Temporal** (Execution) + **LangGraph** (Intelligence) | Durable execution, AI-планирование, subprocess | [06_orchestrator.md](06_orchestrator.md) + [ORCHESTRATOR_SPEC_v2.1](../../SSOT/ORCHESTRATOR_SPEC_v2.1.md) |
| **Process Cards** | YAML DSL (GitHub Actions-подобный) | Декларативное описание процессов | [07_process_cards.md](07_process_cards.md) + [docs/SSOT/PROCESS_CARD_SPEC_v1.0.md](../../SSOT/PROCESS_CARD_SPEC_v1.0.md) |
| **API фреймворк** | FastAPI | HTTP API для пользователей | [08_api_framework.md](08_api_framework.md) |
| **Мониторинг** | OpenTelemetry + Prometheus + Grafana + Temporal UI | Наблюдаемость системы | [09_monitoring_observability.md](09_monitoring_observability.md) |
| **Развертывание** | Docker Compose → Kubernetes | Запуск и масштабирование | [10_deployment.md](10_deployment.md) |
| **Конфигурация** | YAML + Pydantic | Настройки системы | [11_configuration_management.md](11_configuration_management.md) |
| **Тестирование** | pytest + Ruff + mypy | Качество кода | [12_testing_quality.md](12_testing_quality.md) |

---

## Ключевые решения (TL;DR)

### 1. Python 3.11+ — стандарт для AI/ML
- ✅ Все LLM SDK написаны для Python
- ✅ Быстрая разработка MVP
- ✅ Type hints + Pydantic = SSOT
- ✅ Async support из коробки

### 2. RabbitMQ + AMQP 0-9-1 — MindBus
- ✅ ISO/IEC 19464:2014 (международный стандарт)
- ✅ 15+ лет в production (банки, телеком)
- ✅ Priority Queues для управления важностью
- ✅ CloudEvents v1.0 для формата сообщений
- 🔄 **Модульность**: Можно заменить на NATS/Kafka если нужны другие паттерны

### 3. Node Passport + Node Registry — управление узлами
- ✅ Kubernetes-inspired: metadata/spec/status
- ✅ Capability-based routing (оркестратор находит узлы по "что умеют")
- ✅ etcd/Consul для Service Discovery
- 🔄 **Модульность**: Независимые узлы, легко добавлять новые

### 4. Process Cards — декларативные процессы
- ✅ YAML DSL (GitHub Actions-подобный синтаксис)
- ✅ "Dumb Card, Smart Orchestrator" философия
- ✅ Карточка описывает ЧТО, оркестратор решает КАК
- 🔄 **Модульность**: Процессы независимы от исполнения

### 5. LiteLLM — один интерфейс для всех AI-провайдеров
- ✅ OpenAI, Anthropic, Google, локальные модели — одним API
- ✅ Автоматический подсчет стоимости
- ✅ Fallback и retry из коробки
- ✅ Кеширование через Redis
- 🔄 **Модульность**: Легко добавить новые модели

### 6. Temporal + LangGraph — двухслойный Orchestrator (ADR-001)

**Архитектурное решение**: [ADR-001](../../concepts/ADR-001_TEMPORAL_LANGGRAPH.md)

**Два слоя:**

| Слой | Технология | Назначение |
|------|------------|------------|
| **Execution Layer** | Temporal | Durable execution, event sourcing, retry, subprocess |
| **Intelligence Layer** | LangGraph | AI-планирование, Meeting Protocol, Quality Loop |

**Ключевой принцип:**
```
LangGraph ВНУТРИ Temporal Activity (не наоборот!)
```

**Что даёт Temporal:**
- ✅ **Durable Execution** — workflow переживает рестарты
- ✅ **Event History** — полный audit trail
- ✅ **Child Workflows** — наши subprocesses
- ✅ **Retry + Saga** — автоматические повторы и compensation

**Что даёт LangGraph:**
- ✅ **Meeting Protocol** — коллаборативное планирование агентов
- ✅ **Quality Loop** — циклы улучшения качества
- ✅ **Conflict Resolution** — разрешение конфликтов

**Экономия**: ~15-21 неделя разработки (вместо custom state machine)

См: [ORCHESTRATOR_SPEC_v2.1](../../SSOT/ORCHESTRATOR_SPEC_v2.1.md)

### 7. Трёхуровневое хранилище — Ready-Made First
- ✅ **Agent State**: LangGraph Checkpointer (0 строк своего кода)
- ✅ **Process State**: SQLite + SQLAlchemy (MVP) → PostgreSQL (Production)
- ✅ **Artifacts**: fsspec + SQLite (MVP) → MinIO + PostgreSQL (Production)
- ✅ Storage Service как участник MindBus
- ✅ Artifact Manifest v1.0 с AI Context
- 🔄 **Модульность**: SQLite → PostgreSQL, Local FS → S3/MinIO (одна строка конфига)

---

## Соответствие принципам проекта

### ✅ Готовые решения первичны (Ready-Made Solutions First)
- **RabbitMQ** вместо custom протокола (ISO/IEC стандарт, 15+ лет production)
- **CloudEvents** вместо custom формата (CNCF стандарт)
- **YAML** для Process Cards (GitHub Actions/Kubernetes паттерн)
- **etcd/Consul** для Registry (Kubernetes Service Discovery паттерн)

См: [docs/principles/READY_MADE_FIRST.md](../../principles/READY_MADE_FIRST.md)

### ✅ Specification-Driven Development
- **SSOT спецификации** определяют структуры данных ПЕРЕД кодом:
  - [NODE_PASSPORT_SPEC_v1.0.md](../../SSOT/NODE_PASSPORT_SPEC_v1.0.md)
  - [NODE_REGISTRY_SPEC_v1.0.md](../../SSOT/NODE_REGISTRY_SPEC_v1.0.md)
  - [PROCESS_CARD_SPEC_v1.0.md](../../SSOT/PROCESS_CARD_SPEC_v1.0.md)
- **Pydantic models** = SSOT в коде (автоматическая валидация)
- **OpenAPI specs** автогенерируются из FastAPI

### ✅ Zero Hardcoding
- **YAML файлы** для всех параметров
- **Environment variables** для секретов
- **Process Cards** для бизнес-логики (не в коде!)
- **Node Passport** для конфигурации узлов

### ✅ Observability
- **W3C Trace Context** сквозная трассировка через все компоненты
- **OpenTelemetry** трассирует все операции
- **Prometheus** собирает метрики
- **Grafana** визуализирует

### ✅ Resilience
- **RabbitMQ** гарантирует доставку сообщений
- **Priority Queues** для экстренных команд (STOP)
- **LiteLLM** fallbacks при ошибках API
- **PostgreSQL** ACID транзакции

### ✅ Modularity (LEGO-принцип)
Каждый компонент = независимый модуль с стандартным интерфейсом

**Примеры замены блоков:**
- RabbitMQ → NATS / Kafka / Redis Streams
- PostgreSQL → MySQL / MongoDB / SQLite
- etcd → Consul / Zookeeper
- MinIO → AWS S3 / Google Cloud Storage
- FastAPI → Flask / Django REST / gRPC

Замена компонента не требует переписывания всей системы благодаря стандартным интерфейсам.

---

## Эволюционный план (Roadmap)

### Фаза 1: MVP (1-2 недели)
```
Python 3.11+
    ↓
FastAPI (API gateway)
    ↓
┌──────────────────────────────────────┐
│ Orchestrator v2.1                    │
│   Temporal (durable execution)       │
│   + LangGraph (AI planning) [Phase2] │
└──────────────────────────────────────┘
    ↓
Node Registry (etcd/Consul)
    ↓
MindBus (RabbitMQ + CloudEvents)
    ↓
Nodes (Agents с Node Passport)
    ↓
SQLite + fsspec (MVP storage)
    ↓
LiteLLM (unified LLM interface)
    ↓
Docker Compose (контейнеры)
```

**Минимальный стек:**
- API, **Temporal Server**, RabbitMQ, SQLite, etcd, Redis (для кеша LLM)
- Узлы (агенты) регистрируются в Node Registry
- Process Cards описывают процессы
- **Temporal UI** для мониторинга workflows
- Логи с trace_id (базовый observability)
- **Стоимость**: $0 (всё open source, работает на ноутбуке)

**MVP Phase 1 (Temporal Core):** ProcessCardWorkflow + execute_step Activity
**MVP Phase 2 (LangGraph):** collaborative_planning + quality_loop Activities

### Фаза 2: Ранние пользователи (1-3 месяца)
```
+ MinIO (для больших артефактов)
+ Prometheus (метрики)
+ Grafana (визуализация)
+ Расширенная схема БД (3-5 таблиц)
```

### Фаза 3: Production (6+ месяцев)
```
+ Kubernetes (масштабирование)
+ OpenTelemetry + Jaeger/Tempo (distributed tracing)
+ Multi-region (опционально)
```

---

## Стоимость (Cost Analysis)

### Development (Разработка)
**$0/месяц**
- Все open source
- Работает на ноутбуке
- Нет облачных затрат

### Production MVP (Боевой MVP)
**$40-60/месяц**
- VPS (Hetzner 16GB): ~$30/мес
- ИЛИ DigitalOcean Kubernetes: ~$50/мес
- Redis Cloud (опционально): ~$10/мес

### LLM Costs (Переменные)
**Зависит от использования**
- GPT-4: ~$0.03 за задачу
- Claude: ~$0.02 за задачу
- Контролируется бюджетными лимитами в config/llm.yaml

---

## Альтернативы, которые рассматривались

### ❌ Отклонено: Custom протокол вместо RabbitMQ
**Почему НЕТ:**
- Reinventing wheel (7-10 недель разработки vs 1-2 недели интеграции)
- RabbitMQ покрывает 95% требований
- ISO/IEC стандарт с 15+ годами в production

### ❌ Отклонено: Redis Streams вместо RabbitMQ
**Почему НЕТ:**
- Нет встроенных Priority Queues (критично для STOP команд)
- Менее зрелый для enterprise messaging
- RabbitMQ более надёжный для critical paths

**НО:** 🔄 LEGO-принцип позволяет заменить RabbitMQ на Redis Streams если требования изменятся

### ✅ ПРИНЯТО: Temporal для Orchestrator (ADR-001)

**Статус**: ✅ УТВЕРЖДЕНО (2025-12-20)

**Решение:** Использовать Temporal как Execution Layer для Orchestrator.

**Важное уточнение:**
- Temporal = **workflow engine для Orchestrator**, НЕ замена MindBus
- MindBus = **message bus для агентов** (остаётся без изменений)
- Разные задачи, работают вместе

**Экономия:** 15-21 неделя разработки (вместо custom state machine)

См: [ADR-001: Temporal + LangGraph](../../concepts/ADR-001_TEMPORAL_LANGGRAPH.md)

### ✅ ПРИНЯТО: LangGraph для AI-логики Orchestrator (ADR-001)

**Статус**: ✅ УТВЕРЖДЕНО (2025-12-20)

**Что используем в Orchestrator:**
- LangGraph для **Meeting Protocol** (коллаборативное планирование)
- LangGraph для **Quality Loop** (циклы улучшения)
- LangGraph для **Conflict Resolution** (разрешение конфликтов)

**Ключевой принцип:**
> LangGraph работает ВНУТРИ Temporal Activity (не наоборот!)

**Что НЕ изменилось:**
- ❌ LangGraph НЕ заменяет нашу архитектуру (Node Passport, MindBus, Registry)
- ✅ LangGraph дополняет Orchestrator AI-логикой

### ❌ Отклонено: LangGraph/CrewAI как единственный СИСТЕМНЫЙ фреймворк

**Почему НЕТ как замена архитектуре:**
- Не совместимо с концепцией Node Passport + capability-based routing
- Не заменяет MindBus, Registry, наши принципы
- Мы используем свою архитектуру узлов с единым интерфейсом

**НО:** ✅ Используем LangGraph В ДВУХ МЕСТАХ:
1. **Внутри агентов** — для Agent Loop (ReAct паттерн)
2. **Внутри Orchestrator** — для Meeting Protocol, Quality Loop (ADR-001)

---

## Для нетехнических пользователей

**Простыми словами:**

> AI_TEAM построен на технологиях, которые используют банки, телекоммуникационные компании и крупные корпорации. Это надёжные решения с десятилетиями опыта production использования.

**Что это значит на практике:**
- Ваши задачи передаются через систему, которую банки используют для миллионов транзакций (RabbitMQ)
- Данные хранятся в той же БД, которую Instagram использует для миллиардов пользователей (PostgreSQL)
- Система следит за собой как приборная панель автомобиля (Prometheus + Grafana)
- Всё настраивается через простые YAML файлы
- Можно начать на ноутбуке и вырасти до облачных серверов

---

## Следующие шаги

**Для изучения деталей:**
1. Читайте файлы в `docs/technical/stack/` по порядку (01, 02, 03...)
2. Каждый файл объясняет ЗАЧЕМ выбрана технология
3. Приводятся примеры кода и конфигураций
4. Описываются альтернативы и почему они отклонены

**Для начала разработки:**
1. Установить Python 3.11+
2. Установить Docker Desktop
3. Клонировать репозиторий
4. Запустить `docker-compose up`

---

## 📦 Архив устаревших документов

Файлы, написанные ДО утверждения финальной архитектуры, перемещены в [`archive_2025-12-15/`](archive_2025-12-15/):
- `02_message_bus.md` — устаревший (описывал Redis вместо RabbitMQ)
- `03_agent_framework.md` — устаревший (не учитывал Node Passport/Registry)
- `06_orchestrator.md` — устаревший (не учитывал Process Cards)
- `README.md` (старая версия) — устаревший

Эти файлы сохранены для истории, но **НЕ соответствуют** текущей архитектуре.

---

**Статус:** ✅ УТВЕРЖДЕНО
**Последнее обновление**: 2025-12-20
