# Технический стек AI_TEAM

## Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     User Interface                           │
│                    (Web / API / CLI)                         │
└───────────────────────────┬─────────────────────────────────┘
                            │
                  ┌─────────▼─────────┐
                  │   API Gateway     │
                  │    (FastAPI)      │
                  └─────────┬─────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
┌───────▼────────┐  ┌──────▼───────┐  ┌───────▼────────┐
│  Orchestrator  │  │   Monitor    │  │    Storage     │
│   (Custom)     │  │  (Observer)  │  │  (PostgreSQL)  │
└───────┬────────┘  └──────────────┘  └────────────────┘
        │
        │ Commands/Results
        │
┌───────▼──────────────────────────────────────────────┐
│              MindBus (Redis Streams)                  │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐              │
│  │ COMMAND │  │ RESULT  │  │  EVENT  │              │
│  └─────────┘  └─────────┘  └─────────┘              │
└───────┬──────────────────────────────────────────────┘
        │
        │ Subscribe
        │
┌───────┴────────────────────────────────────────────────┐
│                    Agents                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │  Writer  │  │  Critic  │  │  Editor  │   ...       │
│  └──────────┘  └──────────┘  └──────────┘             │
│                                                         │
│  ┌────────────────────────────────────────┐            │
│  │    LLM Integration (LiteLLM)           │            │
│  │  OpenAI │ Anthropic │ Google │ Local   │            │
│  └────────────────────────────────────────┘            │
└─────────────────────────────────────────────────────────┘
```

---

## Полный стек (Stack Overview)

| Компонент | Технология | Для чего | Файл с деталями |
|-----------|-----------|----------|----------------|
| **Язык программирования** | Python 3.11+ | Основной язык разработки | [01_programming_language.md](01_programming_language.md) |
| **Workflow Engine** | Temporal | Управление процессами и коммуникацией | [06_orchestrator.md](06_orchestrator.md) |
| **Кеш & Rate Limiting** | Redis | Кеширование LLM, очереди | [02_message_bus.md](02_message_bus.md) |
| **Фреймворк агентов** | Hybrid (готовый для MVP → кастом) | Логика AI-агентов | [03_agent_framework.md](03_agent_framework.md) |
| **Интеграция с LLM** | LiteLLM | Вызовы AI-моделей | [04_llm_integration.md](04_llm_integration.md) |
| **База данных & Хранилище** | PostgreSQL (упрощённая схема) + опционально MinIO | Задачи, состояние, артефакты | [05_database_storage.md](05_database_storage.md) |
| **API фреймворк** | FastAPI | HTTP API для пользователей | [07_api_framework.md](07_api_framework.md) |
| **Мониторинг** | OpenTelemetry + Prometheus + Grafana | Наблюдаемость системы | [08_monitoring_observability.md](08_monitoring_observability.md) |
| **Развертывание** | Docker Compose → Kubernetes | Запуск и масштабирование | [09_deployment.md](09_deployment.md) |
| **Конфигурация** | YAML + Pydantic | Настройки системы | [10_configuration_management.md](10_configuration_management.md) |
| **Тестирование** | pytest + Ruff + mypy | Качество кода | [11_testing_quality.md](11_testing_quality.md) |

---

## Ключевые решения (TL;DR)

### 1. Python 3.11+ — стандарт для AI/ML
- ✅ Все LLM SDK написаны для Python
- ✅ Быстрая разработка MVP
- ✅ Огромное сообщество
- 🔄 **Модульность**: При необходимости можно переписать критичные компоненты на Go/Rust

### 2. Temporal — workflow engine с первого дня
- ✅ State persistence (автоматическое восстановление)
- ✅ Встроенная коммуникация между агентами (вместо отдельного MindBus)
- ✅ Retry logic, версионирование, визуализация
- 🔄 **Модульность**: Можно заменить на Airflow/Prefect если нужны другие паттерны

### 3. Redis — кеш и очереди
- ✅ Кеширование LLM вызовов = экономия денег
- ✅ Rate limiting для API
- ✅ Простая настройка
- 🔄 **Модульность**: Можно заменить на Memcached, Valkey или in-memory кеш

### 4. Hybrid Agent Framework — готовый для MVP, кастом потом
- ✅ Старт с LangGraph/CrewAI для быстрого прототипа
- ✅ Постепенная замена компонентов на кастомные
- ✅ LangChain utilities для промптов/парсинга
- 🔄 **Модульность**: Каждый агент = независимый модуль

### 5. LiteLLM — один интерфейс для всех AI-провайдеров
- ✅ OpenAI, Anthropic, Google, локальные модели — одним API
- ✅ Автоматический подсчет стоимости
- ✅ Fallback и retry из коробки
- 🔄 **Модульность**: Легко добавить новые модели или заменить на прямые SDK

### 6. PostgreSQL — надежная БД с упрощённой схемой
- ✅ 30+ лет в production (Apple, Instagram, Spotify)
- ✅ ACID гарантии для состояния задач
- ✅ JSONB для гибкости (начинаем с 1 таблицей)
- 🔄 **Модульность**: Можно заменить на MySQL, MongoDB, или даже SQLite для разработки

---

## Эволюционный план (Roadmap)

### Фаза 1: MVP (1-2 недели)
```
Python 3.11+
    ↓
FastAPI (API gateway)
    ↓
Temporal (workflows) + Redis (кеш)
    ↓
PostgreSQL (1 таблица tasks)
    ↓
LiteLLM (unified LLM interface)
    ↓
Docker Compose (5 контейнеров)
```

**Минимальный стек:**
- API, Temporal Server, Temporal Worker, PostgreSQL, Redis
- Агенты = Temporal Activities (LangGraph для быстрого старта)
- Логи с trace_id (без полного observability стека)
- Файлы в PostgreSQL BYTEA или локальной FS
- **Стоимость**: $0 (всё open source, работает на ноутбуке)

### Фаза 2: Ранние пользователи (1-3 месяца)
```
+ MinIO (для больших артефактов)
+ Prometheus (метрики)
+ Custom Agent Components (постепенная замена LangGraph)
+ Упрощённая схема БД (2-3 таблицы)
```

**Что добавляется:**
- Хранение больших файлов (PDF, изображения) в MinIO
- Метрики производительности (Prometheus)
- Кастомные агенты для killer features
- Базовый мониторинг

### Фаза 3: Production (6+ месяцев)
```
+ Kubernetes (масштабирование)
+ OpenTelemetry + Grafana (полный observability стек)
+ Полностью кастомные агенты
+ Расширенная схема БД
+ Multi-region (опционально)
```

**Что добавляется:**
- Высокая доступность и auto-scaling
- Distributed tracing (Jaeger/Tempo)
- Визуализация в Grafana
- Глобальное развёртывание (если нужно)

---

## Соответствие принципам проекта

### ✅ Specification-Driven Development
- **Pydantic schemas** = SSOT для данных
- **OpenAPI specs** автогенерируются из FastAPI
- Все конфиги валидируются перед использованием

### ✅ Zero Hardcoding
- **YAML файлы** для всех параметров
- **Переменные окружения** для секретов
- **LiteLLM** для провайдер-агностичных вызовов

### ✅ Observability
- **OpenTelemetry** трассирует все операции
- **Prometheus** собирает метрики
- **Grafana** визуализирует
- **Redis MONITOR** для отладки

### ✅ Resilience
- **Redis** retry logic
- **LiteLLM** fallbacks
- **PostgreSQL** ACID
- **Kubernetes** auto-healing (позже)

### ✅ Modularity (LEGO-принцип)
- Каждый компонент = Docker контейнер с стандартным интерфейсом
- **Примеры замены блоков:**
  - PostgreSQL → MySQL / MongoDB / SQLite
  - Temporal → Airflow / Prefect / Custom
  - Redis → Memcached / Valkey / In-memory
  - MinIO → AWS S3 / Google Cloud Storage / Filesystem
  - LangGraph → CrewAI / AutoGen / Custom framework
  - FastAPI → Flask / Django REST / gRPC
- Замена компонента не требует переписывания всей системы
- Стандартные интерфейсы: HTTP, SQL, Message Queue, Object Storage API

### ✅ Proven Architectures
- Каждая технология: 10+ лет в production
- Нет bleeding-edge экспериментов
- Стандартные паттерны (REST, message queue, SQL)

---

## Стоимость (Cost Analysis)

### Development (Разработка)
**$0/месяц**
- Все open source
- Работает на ноутбуке
- Нет облачных затрат

### Production MVP (Боевой MVP)
**$40-60/месяц**
- VPS (Hetzner 16GB): ~$30/мес
- ИЛИ DigitalOcean Kubernetes: ~$50/мес
- Redis Cloud (опционально): ~$10/мес

### LLM Costs (Переменные)
**Зависит от использования**
- GPT-4: ~$0.03 за задачу
- Claude: ~$0.02 за задачу
- Контролируется бюджетными лимитами

---

## Альтернативы, которые рассматривались

### Отклонено: Node.js
**Почему НЕТ:**
- Слабая AI/ML экосистема
- Async сложность для данного use case
- Меньше LLM библиотек

### Отклонено: RabbitMQ как MindBus
**Почему НЕТ для MVP:**
- Более сложная настройка vs Redis
- Тяжелее по ресурсам
- Temporal уже обеспечивает messaging между агентами
**НО:** Можно легко заменить Redis на RabbitMQ если понадобится (LEGO-принцип)

### Отклонено: CrewAI/AutoGen как единственный фреймворк
**Почему НЕТ как permanent решение:**
- Фиксированные workflows (Sequential/Hierarchical)
- Наши killer features требуют dynamic workflows
**НО:** Используем для быстрого MVP, постепенно заменяем кастомными компонентами

### Отклонено: Custom Orchestrator вместо Temporal
**Почему НЕТ:**
- Reinventing wheel (state persistence, retry, versioning)
- Двойная работа: Custom → потом миграция на Temporal
- Temporal НЕ сложнее чем Custom для наших задач

### Отклонено: MongoDB
**Почему НЕТ:**
- Нет строгой консистентности
- Состояние задач требует ACID
- PostgreSQL лучше для наших данных

### Отклонено: Serverless (Lambda)
**Почему НЕТ:**
- Долгоживущие задачи не подходят
- Cold start проблемы
- Сложнее отладка

---

## Реальные примеры использования (Real-World Evidence)

### Похожие проекты используют этот стек:

**LangChain:**
- Python + Redis + PostgreSQL

**Prefect (workflow orchestration):**
- Python + PostgreSQL + FastAPI

**Ray (distributed computing):**
- Python + Redis

**Modal (serverless Python):**
- Python + FastAPI + OpenTelemetry

### Battle-tested at scale:

**Redis:**
- Twitter, GitHub, Stack Overflow

**PostgreSQL:**
- Apple, Instagram, Spotify

**FastAPI:**
- Microsoft, Uber, Netflix (ML APIs)

**Kubernetes:**
- Google, Amazon, все Fortune 500

---

## Для нетехнических пользователей

**Простыми словами:**

> AI_TEAM построен на технологиях, которые используют Netflix, Uber и Instagram. Вам не нужно понимать технические детали — всё просто работает надежно.

**Что это значит на практике:**
- Ваши задачи хранятся в той же БД, которую Instagram использует для миллиардов пользователей
- Сообщения между агентами идут через ту же систему, что использует GitHub
- Система следит за собой как приборная панель автомобиля
- Всё настраивается через простые файлы (как заполнение формы)
- Можно начать на ноутбуке и вырасти до облачных серверов

---

## Следующие шаги

**Для изучения деталей:**
1. Читайте файлы в `docs/technical/stack/` по порядку (01, 02, 03...)
2. Каждый файл объясняет ЗАЧЕМ выбрана технология
3. Приводятся примеры кода
4. Описываются альтернативы и почему они отклонены

**Для начала разработки:**
1. Установить Python 3.11+
2. Установить Docker Desktop
3. Клонировать репозиторий
4. Запустить `docker-compose up`

---

# ✅ СТАТУС ДОКУМЕНТА

**УТВЕРЖДЕНО для реализации**

Этот технический стек **прошёл критическую оценку и утверждён** для разработки AI_TEAM.

**Ключевой принцип: LEGO-модульность**
> Каждый компонент можно заменить другим без перестройки всей системы. Как в конструкторе LEGO — вынул один блок, вставил другой.

**Примеры замены блоков:**
- PostgreSQL → MySQL / MongoDB
- Redis → NATS / RabbitMQ
- Temporal → Airflow / Prefect
- MinIO → AWS S3 / Google Cloud Storage
- FastAPI → Flask / Django REST

Стандартные интерфейсы (HTTP, Message Queue, SQL) позволяют легко менять реализацию без изменения архитектуры.

**Последнее обновление**: 2025-12-13
**Статус**: ✅ УТВЕРЖДЕНО
