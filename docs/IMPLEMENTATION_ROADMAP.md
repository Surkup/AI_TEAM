# План реализации AI_TEAM

**Принцип**: Сначала "материнская плата и провода", потом "процессор и память", потом "софт и логика"

**Метафора**: Мы собираем компьютер — начинаем с материнской платы (MindBus), потом добавляем компоненты один за одним.

---

## 🎯 Этап 0: Концепция (✅ ЗАВЕРШЁН)

**Статус**: ✅ Готово

**Что сделано**:
- ✅ Основная концепция системы ([README.md](../README.md))
- ✅ Концепция MindBus ([docs/MINDBUS_README.md](MINDBUS_README.md))
- ✅ Список вопросов для проработки ([QUESTIONS.md](../QUESTIONS.md))
- ✅ Ключевые принципы (Observability, Resilience, Proven Architectures)

**Результат**: Понимание "ЧТО мы строим" и "ЗАЧЕМ"

---

## 🔌 Этап 1: Материнская плата (MindBus + Monitor + Dummy Agent)

**Статус**: 🔄 Текущий этап

**Цель**: Создать "провода" и убедиться, что "по ним бегут сигналы"

### Шаг 1.1: Определить формат Message (⏭️ NEXT)

**Задача**: Зафиксировать структуру сообщения на концептуальном уровне

**Документ**: `docs/SPEC_MESSAGE_FORMAT.md`

**Что определить**:
- Обязательные поля: `id`, `type`, `from`, `to`, `trace_id`, `payload`
- Опциональные поля: `priority`, `created_at`, `parent_message_id`
- Типы сообщений для MVP: `COMMAND`, `RESULT`, `EVENT`
- Формат `payload` (произвольный JSON объект)

**Критерий готовности**: Есть документ с примерами сообщений всех трёх типов

**Время**: 1-2 часа

---

### Шаг 1.2: Реализовать MindBus Core v0.1

**Задача**: Написать минимальную рабочую шину

**Функционал**:
- `send_message(message)` — отправить сообщение в шину
- `subscribe(filter, callback)` — подписаться на сообщения
  - Фильтр по `type` (например, только `COMMAND`)
  - Фильтр по `to` (например, только для `agent_1`)
- Доставка сообщений всем подписчикам

**Технологии**: (будет определено на этапе реализации)

**Критерий готовности**: Можно отправить сообщение и получить его в callback

**Время**: 3-4 часа

---

### Шаг 1.3: Реализовать Monitor v0.1

**Задача**: Создать "индикатор питания" — видим, что по шине что-то летает

**Функционал**:
- Подписывается на ВСЕ сообщения (без фильтров)
- Выводит в консоль: `[timestamp] type: from→to (trace_id: xxx)`
- Опционально: сохраняет в файл для последующего анализа

**Критерий готовности**: При отправке сообщения видим его в логе Monitor

**Время**: 1-2 часа

---

### Шаг 1.4: Реализовать DummyAgent v0.1

**Задача**: Создать агента-заглушку, который умеет принимать COMMAND и отвечать RESULT

**Функционал**:
- Подписывается на сообщения: `type=COMMAND` и `to=dummy_agent`
- При получении COMMAND:
  - Ждёт 1 секунду (имитация работы)
  - Отправляет RESULT обратно: `{"status": "SUCCESS", "result": "Done"}`
  - Копирует `trace_id` из входящего сообщения

**Критерий готовности**: Отправили COMMAND → через 1 сек получили RESULT

**Время**: 1-2 часа

---

### Шаг 1.5: Сквозной тест Этапа 1

**Задача**: Убедиться, что "провода работают"

**Сценарий**:
1. Запустить MindBus
2. Запустить Monitor
3. Запустить DummyAgent
4. Отправить вручную (через скрипт) COMMAND для `dummy_agent`
5. Посмотреть в Monitor:
   - Видим COMMAND: `user→dummy_agent`
   - Видим RESULT: `dummy_agent→user`
6. Убедиться, что `trace_id` один и тот же в обоих сообщениях

**Критерий успеха Этапа 1**:
✅ "Мы можем кинуть сообщение и увидеть в Monitor, как оно прошло через систему"

**Время**: 1 час

---

## 🧠 Этап 2: Добавляем мозг (Task + Orchestrator)

**Статус**: ⏭️ Следующий этап

**Цель**: Превратить "абстрактные сообщения" в "живую задачу с координацией"

### Шаг 2.1: Определить структуру Task

**Документ**: `docs/SPEC_TASK.md`

**Что определить**:
- Поля Task: `id`, `description`, `status`, `result`, `created_at`, `completed_at`
- Статусы: `CREATED`, `IN_PROGRESS`, `COMPLETED`, `FAILED`

**Время**: 30 минут

---

### Шаг 2.2: Реализовать Orchestrator v0.1

**Функционал**:
- Подписывается на EVENT с типом `task_created`
- При получении:
  - Создаёт COMMAND для DummyAgent
  - Ждёт RESULT от агента
  - Публикует EVENT `task_completed`

**Критерий готовности**: Человек создаёт Task → Orchestrator координирует → Task завершён

**Время**: 4-5 часов

---

### Шаг 2.3: API Gateway (точка входа для человека)

**Функционал**:
- HTTP эндпоинт: `POST /tasks` — создать задачу
- Генерирует EVENT `task_created` в MindBus
- Возвращает `task_id` человеку

**Критерий готовности**: Можно через curl создать задачу

**Время**: 2-3 часа

---

### Шаг 2.4: Сквозной тест Этапа 2

**Сценарий**:
1. Человек отправляет POST запрос: "Сделай что-нибудь"
2. API Gateway публикует EVENT `task_created`
3. Orchestrator получает событие
4. Orchestrator отправляет COMMAND → DummyAgent
5. DummyAgent отвечает RESULT
6. Orchestrator публикует EVENT `task_completed`
7. Monitor показывает всю цепочку
8. Человек получает результат

**Критерий успеха Этапа 2**:
✅ "У нас есть сквозной поток: Человек → Task → Orchestrator → Agent → Результат"

---

## 🤖 Этап 3: Настоящий AI-агент

**Статус**: ⏭️ Будущий этап

**Цель**: Заменить DummyAgent на Agent с реальным LLM

### Шаг 3.1: Реализовать AIAgent v0.1

**Функционал**:
- Принимает COMMAND с `payload.prompt`
- Вызывает LLM API (OpenAI / Anthropic / локальная модель)
- Возвращает RESULT с текстом от LLM

**Критерий готовности**: Задача "Напиши стих про кот" → получаем стих

**Время**: 3-4 часа

---

### Шаг 3.2: Сквозной тест Этапа 3

**Сценарий**: Тот же, что Этап 2, но агент теперь "умный"

**Критерий успеха Этапа 3**:
✅ "Система генерирует осмысленные ответы через LLM"

---

## 📋 Этап 4: Process Cards (инструкции для Orchestrator)

**Статус**: ⏭️ Будущий этап

**Цель**: Дать Orchestrator возможность выполнять многошаговые процессы по инструкциям

### Шаг 4.1: Разработать DSL для Process Cards

**Документ**: `docs/SPEC_PROCESS_CARD_DSL.md`

**Что определить**:
- Синтаксис карточки (YAML / JSON)
- Описание шагов процесса
- Условия переходов между шагами
- Роли агентов

**Время**: 4-6 часов

---

### Шаг 4.2: Реализовать интерпретатор Process Cards

**Функционал**:
- Orchestrator читает Process Card
- Выполняет шаги по очереди
- Отправляет COMMAND нужным агентам
- Обрабатывает RESULT и переходит к следующему шагу

**Критерий готовности**: Process Card с 3 шагами выполняется автоматически

**Время**: 6-8 часов

---

### Шаг 4.3: Сквозной тест Этапа 4

**Сценарий**:
- Process Card: "Сгенерируй текст → Проверь качество → Сохрани результат"
- Orchestrator координирует 3 агентов
- Весь процесс виден в Monitor

**Критерий успеха Этапа 4**:
✅ "Система выполняет сложные многошаговые процессы по инструкциям"

---

## 💾 Этап 5: Storage (хранение данных)

**Статус**: ⏭️ Будущий этап

**Цель**: Добавить постоянное хранение для задач, артефактов, истории

### Шаг 5.1: Реализовать Storage Adapter

**Функционал**:
- Принимает COMMAND `store_artifact`
- Сохраняет данные (in-memory / файл / БД)
- Возвращает RESULT с `artifact_id`

**Время**: 4-5 часов

---

### Шаг 5.2: Интегрировать с Orchestrator

**Функционал**:
- После завершения задачи Orchestrator сохраняет результат в Storage
- Monitor может запросить историю задач из Storage

**Критерий успеха Этапа 5**:
✅ "Результаты задач сохраняются и доступны для просмотра"

---

## 📊 Этап 6: Улучшенный Monitor (Web UI)

**Статус**: ⏭️ Будущий этап

**Цель**: Создать удобный интерфейс для наблюдения за системой

### Шаг 6.1: Web-интерфейс для Monitor

**Функционал**:
- Показывает live stream сообщений
- Фильтр по `trace_id` (показать всю цепочку одной задачи)
- Визуализация: кто кому отправил сообщение

**Критерий успеха Этапа 6**:
✅ "Можно в браузере видеть, как система работает в реальном времени"

---

## 🎭 Этап 7: Несколько специализированных агентов

**Статус**: ⏭️ Будущий этап

**Цель**: Добавить разных агентов: Writer, Coder, Critic, Researcher

### Шаг 7.1: Реализовать специализированных агентов

**Агенты**:
- **Writer Agent**: Генерация текстов
- **Coder Agent**: Написание кода
- **Critic Agent**: Оценка качества
- **Researcher Agent**: Поиск информации

**Критерий успеха Этапа 7**:
✅ "Orchestrator может координировать разных специалистов для сложных задач"

---

## 🔄 Этап 8: Цикл улучшения (Human-in-the-loop)

**Статус**: ⏭️ Будущий этап

**Цель**: Добавить возможность человеку вмешиваться в процесс

### Шаг 8.1: Notification для человека

**Функционал**:
- Orchestrator может отправить NOTIFICATION человеку
- Человек принимает решение (approve / reject / edit)
- Процесс продолжается с учётом решения

**Критерий успеха Этапа 8**:
✅ "Человек может контролировать критичные моменты процесса"

---

## 📈 Дальнейшие этапы

- Этап 9: Knowledge Base (векторное хранилище для контекста)
- Этап 10: Advanced Process Cards (циклы, условия, параллельность)
- Этап 11: Метрики и аналитика
- Этап 12: Масштабирование (распределённая шина)

---

## 🎯 Текущий фокус

**Сейчас мы на**: 🔄 Этап 1, Шаг 1.1

**Следующий шаг**: Создать `docs/SPEC_MESSAGE_FORMAT.md`

**Цель**: К концу Этапа 1 у нас будет работающая MindBus + Monitor + DummyAgent, через которые можно прогнать тестовое сообщение.

---

## 📝 Принципы работы с этим планом

1. **Последовательно**: Не переходим к следующему этапу, пока не завершён текущий
2. **Инкрементально**: Каждый шаг добавляет небольшой кусок функционала
3. **Тестируемо**: Каждый этап завершается сквозным тестом
4. **Гибко**: План может корректироваться по мере получения опыта

---

**Последнее обновление**: 2025-12-12
