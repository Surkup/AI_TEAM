# Orchestrator: Функциональные требования

**Статус документа**: 🚧 **ЧЕРНОВИК / WORK-IN-PROGRESS**

**Версия**: v0.1 (DRAFT)
**Дата создания**: 2025-12-15
**Авторы**: User + Claude Sonnet 4.5

---

## ⚠️ ВАЖНО: Это концептуальный черновик

**Этот документ**:
- ✅ Описывает **ЧТО** должен уметь Orchestrator (не КАК)
- ✅ Фиксирует первичные мысли о функциональности
- ✅ Служит основой для планирования версий
- ⚠️ **ТРЕБУЕТ детальной проработки и проверки**
- ⚠️ **МОЖЕТ содержать ошибки и неточности**
- ⚠️ **НЕ является финальной спецификацией**

**Цель документа**: Зафиксировать полный список функций, чтобы спланировать поэтапную разработку Orchestrator по принципу "скелет → наращивание возможностей".

**Философия**: Технология-независимое описание. Любая реализация Orchestrator (вариант A/B/C из [orchestrator_architectures.md](orchestrator_architectures.md)) должна покрывать эти функции.

**Связанные документы**:
- [orchestrator_architectures.md](orchestrator_architectures.md) - КАК реализовать (архитектурные варианты)
- Этот документ - ЧТО реализовать (функциональные требования)

---

## 📋 Содержание

1. [Введение](#введение)
2. [Функциональные требования](#функциональные-требования)
3. [Приоритизация функций](#приоритизация-функций)
4. [План версионирования Orchestrator](#план-версионирования-orchestrator)
5. [Use Cases (примеры использования)](#use-cases-примеры-использования)
6. [Граничные случаи](#граничные-случаи)
7. [Связь с SSOT спецификациями](#связь-с-ssot-спецификациями)
8. [Non-Functional Requirements](#non-functional-requirements)

---

## Введение

### Что такое Orchestrator?

**Orchestrator** — центральный интеллектуальный узел системы AI_TEAM, который:
- Понимает цели пользователя
- Проектирует и исполняет процессы
- Анализирует возможности всех узлов через Node Registry и Node Passport
- Делегирует задачи агентам
- Управляет временем, приоритетами, ошибками
- Обеспечивает прозрачность и безопасность
- Эволюционирует, не нарушая архитектурных контрактов

### Принцип: "ЧТО, а не КАК"

Этот документ описывает **функции** Orchestrator, но **НЕ** способ их реализации.

**Пример**:
- ✅ "Orchestrator должен уметь выбирать исполнителя на основе capabilities" - **ЧТО**
- ❌ "Orchestrator использует LLM для выбора агента" - **КАК** (это в orchestrator_architectures.md)

### Связь с архитектурными вариантами

В [orchestrator_architectures.md](orchestrator_architectures.md) описаны 3 варианта реализации:
- **Вариант A**: Deterministic Workflow Engine
- **Вариант B**: LLM-Director
- **Вариант C**: Policy-Governed Hybrid

Все три варианта должны покрывать функции из этого документа, но **по-разному**:

| Функция | Вариант A | Вариант B | Вариант C |
|---------|-----------|-----------|-----------|
| 3.2. Выбор исполнителя | Детерминистический из YAML | LLM reasoning | LLM + Policy check |
| 6.2. Объяснение решений | Просто: "в YAML написано" | Сложно: LLM explain trace | Средне: Policy logs + LLM reasoning |

---

### Архитектурные принципы устойчивости

> Эти принципы обязательны для ЛЮБОГО варианта реализации (A/B/C).

#### Принцип 1: Orchestrator — stateless

**Состояние процессов хранится в Process State Store (PostgreSQL), НЕ в памяти Orchestrator.**

Если Orchestrator:
- упал
- был перезапущен
- заменён другой версией

→ новый экземпляр поднимает процессы с того же места.

**Это обеспечивает**:
- Устранение Single Point of Failure
- Hot-swap (замена без остановки системы)
- Подготовку к горизонтальному масштабированию

#### Принцип 2: Event-driven модель

**Orchestrator НЕ ждёт синхронно ответа от агента.**

Цикл работы:
1. Отправил COMMAND
2. Сохранил state в БД
3. Переключился на обработку других событий
4. Получил RESULT/ERROR → реакция

**Это обеспечивает**:
- Отсутствие блокировок
- Параллельное управление множеством процессов
- Устойчивость к медленным агентам

#### Принцип 3: Контракты важнее интеллекта

**Даже если Orchestrator LLM-based — его свобода заканчивается там, где начинаются контракты системы.**

Orchestrator НИКОГДА не имеет права:
- Нарушать формат сообщений (MESSAGE_FORMAT)
- Игнорировать Process Card
- "Догадываться" вместо соблюдения протоколов
- Обходить Policy Layer

**Это обеспечивает**:
- Предсказуемость
- Отладку
- Доверие

---

### Эволюционный путь Orchestrator

> От простого к сложному. Каждый этап — стабильная точка.

| Версия | Название | Фокус | Ключевые функции |
|--------|----------|-------|------------------|
| v0.1 | Runtime Manager | Один процесс работает | Чтение Process Card, вызов агентов, приём результатов |
| v0.2 | Resilient Runtime | Устойчивость | Retries, TTL, Circuit Breaker, state recovery |
| v0.3 | Observable Runtime | Наблюдаемость | CONTROL-сообщения, мониторинг, трассировка |
| v0.4 | Policy Manager | Безопасность | Бюджеты, ограничения, Policy Layer |
| v0.5 | Intelligent Planner | LLM-помощник | Помощь в создании Process Cards, адаптация стратегий |
| v1.0 | Strategic Leader | Зрелый продукт | Самоанализ, оптимизация, долгосрочное планирование |

**Принцип**: Ядро Orchestrator не переписывается — оно обрастает возможностями.

---

## Функциональные требования

### 1. Взаимодействие с системой (базовые способности)

#### 1.1. Подключение к MindBus

**Orchestrator должен уметь**:
- Публиковать сообщения всех допустимых типов (COMMAND / EVENT / CONTROL)
- Подписываться на RESULT / ERROR / EVENT
- Работать с приоритетами, TTL, correlation_id, reply_to
- Обеспечивать трассировку (traceparent согласно W3C Trace Context)

**Связь с SSOT**: [mindbus_protocol_v1.md](../SSOT/mindbus_protocol_v1.md)

**Пример**:
```python
# Pseudo-code
orchestrator.publish(
    message_type="COMMAND",
    data={"action": "write_chapter", ...},
    priority=5,
    ttl=300,
    traceparent="00-0af7651916cd43dd8448eb211c80319c-b7ad6b7169203331-01"
)
```

---

#### 1.2. Работа с реестром узлов (Node Registry)

**Orchestrator обязан уметь**:
- Получать актуальный список всех зарегистрированных узлов
- Различать типы узлов (agent / tool / storage / monitor / external_service)
- Понимать статус узла (ready / busy / degraded / offline)
- Реагировать на появление и исчезновение узлов

**Связь с SSOT**: [NODE_REGISTRY_SPEC_v1.0.md](../SSOT/NODE_REGISTRY_SPEC_v1.0.md)

**Пример**:
```
Запрос к Node Registry:
→ Получаем: [agent-1 (ready), agent-2 (busy), agent-3 (offline)]
→ Решение: "Агент agent-3 сейчас offline — исключаем из планирования"
```

**Граничный случай**:
- Что если ВСЕ агенты offline? → См. раздел "Граничные случаи"

---

#### 1.3. Чтение и интерпретация Node Passport

**Orchestrator должен**:
- Читать паспорт узла (Node Passport)
- Понимать его capabilities (что умеет)
- Понимать подключённые инструменты (tools)
- Учитывать ограничения (resource limits, environment requirements, security policies)
- Сопоставлять требования задачи с возможностями узлов

**Связь с SSOT**: [NODE_PASSPORT_SPEC_v1.0.md](../SSOT/NODE_PASSPORT_SPEC_v1.0.md)

**Пример**:
```yaml
# Задача требует:
requirements:
  capabilities: ["python_execution"]
  environment: "sandbox"

# Паспорт agent-1:
capabilities:
  - name: "python_execution"
    version: "1.0"
security:
  sandbox: true

# Паспорт agent-2:
capabilities:
  - name: "python_execution"
    version: "1.0"
security:
  sandbox: false

→ Решение: "Выбираем agent-1 (sandbox=true подходит)"
```

---

### 2. Управление процессами и целями

#### 2.1. Приём и интерпретация целей (Goal Intake)

**Orchestrator должен уметь**:
- Принимать цель в свободной форме (natural language)
- Уточнять цель через диалог (если необходимо)
- Фиксировать критерии успеха
- Понимать, является ли цель одношаговой или многошаговой

**Пример**:
```
User: "Напиши книгу про AI"

Orchestrator уточняет:
- "Сколько глав?"
- "Какой стиль (академический / популярный)?"
- "Нужна ли иллюстрация?"

→ Фиксирует цель:
goal: "Написать 10-главную популярную книгу про AI с иллюстрациями"
success_criteria:
  - 10 chapters
  - popular style
  - illustrations included
```

---

#### 2.2. Проектирование процессов

**Orchestrator должен уметь**:
- Создавать Process Card с нуля
- Читать и валидировать существующие Process Cards
- Модифицировать карточки по ходу выполнения (если стратегия это позволяет)
- Версионировать карточки (draft → active → archived)

**Связь с SSOT**: [PROCESS_CARD_SPEC_v1.0.md](../SSOT/PROCESS_CARD_SPEC_v1.0.md)

**Пример**:
```yaml
# Orchestrator создаёт Process Card:
apiVersion: process/v1
kind: ProcessCard
metadata:
  name: "book-ai-001"
  status: "draft"
spec:
  goal: "Написать книгу про AI"
  steps:
    - id: research
      action: web_search
    - id: outline
      action: create_outline
    - id: write-chapters
      action: write_chapter
      repeat: 10
```

---

#### 2.3. Управление состоянием процессов

**Orchestrator обязан**:
- Знать, какие процессы активны в данный момент
- Знать текущий шаг каждого процесса
- Хранить и восстанавливать контекст процесса (при сбоях/перезапусках)
- Поддерживать переменные процесса (счётчики, флаги, промежуточные результаты)

**Пример**:
```
Process: book-ai-001
Status: in_progress
Current step: 12/25 (write-chapter)
Variables:
  - current_chapter: 12
  - chapters_completed: 11
  - total_words: 45000
Context:
  - research_data: {...}
  - outline: {...}
  - chapter_11_text: "..."
```

**Граничный случай**:
- Orchestrator упал на шаге 12. После перезапуска должен продолжить с шага 12, а не начинать сначала.

---

### 3. Назначение и делегирование задач

#### 3.1. Формирование задач (Commands)

**Orchestrator должен**:
- Формировать корректные COMMAND-сообщения (согласно MESSAGE_FORMAT)
- Задавать требования (requirements: capabilities, constraints)
- Указывать приоритеты, TTL, idempotency_key
- Привязывать команду к процессу/шагу (context.process_id, context.step)

**Связь с SSOT**: [MESSAGE_FORMAT_v1.1.md](../SSOT/MESSAGE_FORMAT_v1.1.md) - COMMAND type

**Пример**:
```json
{
  "type": "COMMAND",
  "data": {
    "action": "write_chapter",
    "params": {
      "chapter_number": 12,
      "topic": "AI Safety"
    },
    "requirements": {
      "capabilities": ["text_generation"],
      "constraints": {
        "max_words": 5000
      }
    },
    "context": {
      "process_id": "book-ai-001",
      "step": "write-chapter-12"
    }
  }
}
```

---

#### 3.2. Выбор исполнителя

**Orchestrator должен**:
- Выбирать исполнителя на основе:
  - Capabilities (совпадение с requirements)
  - Статуса (ready / busy)
  - Загрузки (количество активных задач)
  - Истории успеха (опционально, для v1.0+)
- Поддерживать стратегии:
  - **Point-to-point**: конкретный агент по node_id
  - **Role-based**: любой агент с нужными capabilities
  - **Fallback**: если первый агент недоступен, выбрать второго

**Реализация зависит от архитектурного варианта**:

| Вариант | Как выбирается исполнитель |
|---------|----------------------------|
| A (Workflow) | Детерминистический из Process Card: `agent: "agent-1"` |
| B (LLM-Director) | LLM reasoning: "Выберу agent-2, т.к. у него лучшая история" |
| C (Policy-Governed) | LLM предлагает → Policy Layer проверяет capabilities |

**Пример**:
```
Задача: write_chapter (requires: text_generation)

Node Registry:
- agent-1: capabilities=[text_generation], status=ready, load=2
- agent-2: capabilities=[text_generation], status=ready, load=5
- agent-3: capabilities=[code_generation], status=ready, load=0

→ Решение:
  - agent-3 НЕ подходит (нет text_generation)
  - agent-1 лучше agent-2 (меньше загрузка)
  - Выбираем agent-1
```

---

#### 3.3. Перераспределение задач

**Orchestrator обязан**:
- Менять исполнителя при ошибках (если агент упал / timeout)
- Эскалировать приоритет (если задача критична)
- Делить задачу на подзадачи (если задача слишком сложная)
- Отменять задачи (CONTROL.STOP)

**Пример**:
```
1. Отправили COMMAND agent-1
2. Получили ERROR (timeout)
3. Orchestrator решает:
   - Повторить на том же агенте? Нет (timeout обычно не retryable на том же узле)
   - Выбрать другого агента? Да
4. Отправляет COMMAND agent-2
```

---

### 4. Обработка результатов и ошибок

#### 4.1. Приём результатов (RESULT)

**Orchestrator должен**:
- Сопоставлять результат с исходной командой (correlation_id)
- Извлекать полезные данные (output)
- Обновлять контекст процесса (сохранять промежуточные результаты)
- Решать, что делать дальше (следующий шаг / завершение процесса)

**Связь с SSOT**: [MESSAGE_FORMAT_v1.1.md](../SSOT/MESSAGE_FORMAT_v1.1.md) - RESULT type

**Пример**:
```json
// Получили RESULT:
{
  "type": "RESULT",
  "data": {
    "status": "SUCCESS",
    "output": {
      "chapter_text": "AI Safety is crucial because...",
      "word_count": 4850
    }
  },
  "correlation_id": "cmd-12345"
}

// Orchestrator:
→ Сопоставляет с COMMAND (correlation_id)
→ Сохраняет chapter_text в контекст
→ Обновляет переменную: chapters_completed = 12
→ Переходит к следующему шагу: write-chapter-13
```

---

#### 4.2. Обработка ошибок (ERROR)

**Orchestrator обязан**:
- Классифицировать ошибки:
  - **Retryable** (временная проблема, можно повторить)
  - **Fatal** (невозможно выполнить, нужно остановить процесс)
  - **Unknown** (неизвестная ошибка, требует анализа)
- Принимать решение о повторе
- Менять стратегию (другой агент / другой подход)
- Корректно завершать процесс при необходимости

**Связь с SSOT**: [MESSAGE_FORMAT_v1.1.md](../SSOT/MESSAGE_FORMAT_v1.1.md) - ERROR type, google.rpc.Code

**Классификация ошибок по google.rpc.Code**:

| Error Code | Retryable? | Действие Orchestrator |
|------------|------------|-----------------------|
| DEADLINE_EXCEEDED (timeout) | ⚠️ Иногда | Retry на другом агенте / увеличить TTL |
| RESOURCE_EXHAUSTED | ⚠️ Иногда | Retry позже / выбрать другого агента |
| UNAVAILABLE | ✅ Да | Retry через backoff |
| INVALID_ARGUMENT | ❌ Нет | Исправить COMMAND / escalate to human |
| PERMISSION_DENIED | ❌ Нет | Выбрать другого агента / escalate |
| INTERNAL | ⚠️ Иногда | Retry 1-2 раза, потом fatal |
| UNIMPLEMENTED | ❌ Нет | Выбрать другого агента |

**Пример**:
```json
// Получили ERROR:
{
  "type": "ERROR",
  "data": {
    "code": "DEADLINE_EXCEEDED",
    "message": "Timeout after 300s"
  }
}

// Orchestrator:
→ Классифицирует: DEADLINE_EXCEEDED = retryable на другом агенте
→ Выбирает agent-2
→ Увеличивает TTL до 600s
→ Повторяет COMMAND
```

---

#### 4.3. Управление повторами (Retry & Backoff)

**Orchestrator должен**:
- Ограничивать количество повторов (max_retries)
- Поддерживать задержки между попытками (backoff)
- Избегать бесконечных циклов
- Логировать все попытки для observability

**Пример retry policy**:
```yaml
retry_policy:
  max_attempts: 3
  backoff: exponential  # 1s, 2s, 4s, 8s...
  timeout_increase: 1.5x  # каждый раз увеличиваем TTL
```

**Пример**:
```
Попытка 1: agent-1, TTL=300s → ERROR (timeout)
Ждём 1s
Попытка 2: agent-2, TTL=450s → ERROR (timeout)
Ждём 2s
Попытка 3: agent-3, TTL=675s → SUCCESS
```

---

### 5. Управление временем, приоритетами и ресурсами

#### 5.1. Управление временем

**Orchestrator обязан**:
- Учитывать TTL задач (не отправлять просроченные команды)
- Понимать дедлайны процессов
- Прекращать бессмысленные действия при истечении времени
- Логировать задержки для анализа

**Пример**:
```
Process deadline: 2025-12-15 18:00
Current time: 2025-12-15 17:55
Remaining steps: 5 (estimated 30 min)

→ Orchestrator: "Не успеем. Escalate to human."
```

---

#### 5.2. Управление приоритетами

**Orchestrator должен**:
- Назначать приоритеты задач (0-9, где 9 = highest)
- Динамически менять приоритеты (если процесс срочный)
- Реагировать на экстренные CONTROL-команды

**Пример**:
```
Процесс A (обычный): priority=5
Процесс B (срочный): priority=9

→ Все задачи процесса B выполняются раньше задач A
```

---

#### 5.3. Управление бюджетами

**⚠️ Концептуально** (не для MVP, для v1.0+)

**Orchestrator должен**:
- Учитывать ограничения по стоимости, токенам, времени
- Выбирать стратегии "дешевле / быстрее / качественнее"
- Останавливать процесс при превышении бюджета

**Пример**:
```yaml
budget:
  max_cost_usd: 10.0
  max_tokens: 1000000
  max_time_minutes: 120

current_usage:
  cost_usd: 8.5
  tokens: 850000
  time_minutes: 95

→ Orchestrator: "Осталось $1.5, будьте осторожны с выбором агентов"
```

---

### 6. Наблюдаемость и объяснимость

#### 6.1. Трассировка и логирование

**Orchestrator обязан**:
- Обеспечивать traceability всех действий (W3C Trace Context)
- Публиковать события о ключевых решениях (EVENT type)
- Поддерживать диагностику и отладку
- Логировать все COMMAND / RESULT / ERROR с timestamps

**Связь с SSOT**: [mindbus_protocol_v1.md](../SSOT/mindbus_protocol_v1.md) - W3C Trace Context

**Пример события**:
```json
{
  "type": "EVENT",
  "data": {
    "event_type": "orchestrator.decision",
    "details": {
      "decision": "selected_agent",
      "agent_id": "agent-1",
      "reason": "best_match_capabilities",
      "alternatives": ["agent-2", "agent-3"]
    }
  },
  "traceparent": "00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01"
}
```

---

#### 6.2. Объяснение решений (Explainability)

**Orchestrator должен**:
- Уметь ответить (хотя бы логически):
  - "Почему был выбран этот агент?"
  - "Почему этот шаг выполняется сейчас?"
  - "Почему процесс был остановлен?"
- Сохранять reasoning-артефакты (не обязательно идеальные для MVP)

**Реализация зависит от архитектурного варианта**:

| Вариант | Как объясняются решения |
|---------|-------------------------|
| A (Workflow) | Просто: "В Process Card указан agent-1 на шаге 5" |
| B (LLM-Director) | Сложно: LLM explain trace ("Выбрал agent-1, потому что...") |
| C (Policy-Governed) | Средне: Policy logs + LLM reasoning |

**Пример (Вариант C)**:
```
User: "Почему выбрали agent-1?"

Orchestrator:
1. Policy check log:
   - agent-1: capabilities ✅, status ✅, load=2
   - agent-2: capabilities ✅, status ✅, load=5

2. LLM reasoning:
   "Выбрал agent-1, т.к. у него меньше загрузка (2 vs 5)"
```

---

### 7. Управление системой и безопасностью

#### 7.1. Контроль доступа

**Orchestrator должен**:
- Уважать права узлов (не отправлять команды, если нет разрешения)
- Не отправлять запрещённые команды (forbidden_actions в Policy)
- Действовать в рамках политик безопасности
- Валидировать все входящие команды

**Пример**:
```yaml
# Policy:
forbidden_actions:
  - delete_data
  - external_api_call

# Orchestrator получает цель: "Удали все файлы"
→ Orchestrator: "❌ Запрещено политикой безопасности"
```

---

#### 7.2. Реакция на внешние сигналы

**Orchestrator обязан**:
- Принимать команды от человека (UI / Monitoring)
- Реагировать на CONTROL сообщения:
  - STOP - остановить процесс
  - PAUSE - приостановить процесс
  - RESUME - возобновить процесс
- Корректно останавливать процессы (graceful shutdown)

**Связь с SSOT**: [MESSAGE_FORMAT_v1.1.md](../SSOT/MESSAGE_FORMAT_v1.1.md) - CONTROL type

**Пример**:
```json
// Human отправляет CONTROL:
{
  "type": "CONTROL",
  "data": {
    "control_type": "STOP",
    "target": {
      "process_id": "book-ai-001"
    }
  }
}

// Orchestrator:
→ Останавливает процесс book-ai-001
→ Отменяет все активные COMMAND этого процесса
→ Сохраняет текущее состояние (для возможного RESUME)
→ Отправляет EVENT: "process_stopped"
```

---

### 8. Эволюция и обучение (стратегически)

**⚠️ Эти функции для v1.0+, не для MVP**

#### 8.1. Самоанализ

**Orchestrator должен**:
- Анализировать успешность процессов (completion rate, execution time)
- Выявлять паттерны ошибок (какие агенты чаще падают)
- Улучшать стратегии планирования (на основе истории)

**Пример**:
```
Статистика за неделю:
- agent-1: 95% success rate, avg time 120s
- agent-2: 70% success rate, avg time 90s

→ Orchestrator: "Предпочитаю agent-1 для критичных задач"
```

---

#### 8.2. Обновляемость

**Orchestrator обязан**:
- Переживать обновления моделей (если используется LLM)
- Сохранять совместимость с существующими процессами
- Позволять замену "процессора" без ломки системы

**Пример**:
```
Orchestrator v0.1 → v0.2:
- Добавлена функция budget management
- Старые Process Cards работают как прежде
- Новые Process Cards могут использовать budget
```

---

## Приоритизация функций

### Легенда приоритетов

- 🔴 **КРИТИЧНО для MVP** - без этого система не работает вообще
- 🟡 **ВАЖНО для v0.2-v0.3** - нужно быстро, но не блокирует запуск
- 🟢 **СТРАТЕГИЧЕСКИ (v1.0+)** - эволюция системы, долгосрочное развитие

---

### Приоритизация по функциям

| # | Функция | Приоритет | Комментарий |
|---|---------|-----------|-------------|
| **1. Взаимодействие с системой** |
| 1.1 | Подключение к MindBus | 🔴 КРИТИЧНО | Без этого система мертва |
| 1.2 | Работа с Node Registry | 🔴 КРИТИЧНО | Нужно знать, кто доступен |
| 1.3 | Чтение Node Passport | 🔴 КРИТИЧНО | Нужно понимать capabilities |
| **2. Управление процессами** |
| 2.1 | Приём целей | 🔴 КРИТИЧНО | Нужно понимать, что делать |
| 2.2 | Проектирование процессов | 🔴 КРИТИЧНО | Создание/чтение Process Card |
| 2.3 | Управление состоянием | 🟡 ВАЖНО (v0.2) | Для MVP можно in-memory, позже - persistence |
| **3. Назначение задач** |
| 3.1 | Формирование COMMAND | 🔴 КРИТИЧНО | Основное действие Orchestrator |
| 3.2 | Выбор исполнителя | 🔴 КРИТИЧНО | Базовый алгоритм (capability match) |
| 3.3 | Перераспределение задач | 🟡 ВАЖНО (v0.2) | Для MVP можно без retry |
| **4. Обработка результатов** |
| 4.1 | Приём RESULT | 🔴 КРИТИЧНО | Без этого нет обратной связи |
| 4.2 | Обработка ERROR | 🔴 КРИТИЧНО | Минимальная обработка для MVP |
| 4.3 | Retry & Backoff | 🟡 ВАЖНО (v0.2) | Для MVP можно без retry |
| **5. Время, приоритеты, ресурсы** |
| 5.1 | Управление временем | 🟡 ВАЖНО (v0.2) | Для MVP можно без TTL |
| 5.2 | Управление приоритетами | 🟡 ВАЖНО (v0.3) | Для MVP все задачи = priority 5 |
| 5.3 | Управление бюджетами | 🟢 СТРАТЕГИЯ (v1.0+) | Сложная функция, не для MVP |
| **6. Наблюдаемость** |
| 6.1 | Трассировка и логирование | 🔴 КРИТИЧНО | Без этого невозможно отладить |
| 6.2 | Объяснение решений | 🟡 ВАЖНО (v0.3) | Для MVP хотя бы базовые логи |
| **7. Безопасность** |
| 7.1 | Контроль доступа | 🟡 ВАЖНО (v0.2) | Для MVP можно без Policy Layer |
| 7.2 | Реакция на CONTROL | 🟡 ВАЖНО (v0.3) | Для MVP хотя бы STOP |
| **8. Эволюция** |
| 8.1 | Самоанализ | 🟢 СТРАТЕГИЯ (v1.0+) | Очень интересно, но не критично |
| 8.2 | Обновляемость | 🟢 СТРАТЕГИЯ (v1.0+) | Думать с самого начала, реализовывать позже |

---

## План версионирования Orchestrator

### Философия: Скелет → Наращивание возможностей

**Идея**: Сделать минимальный рабочий скелет Orchestrator, который можно **расширять как плагинами**, добавляя функциональность версия за версией.

**Преимущества**:
- ✅ Быстрый старт (v0.1 за 1-2 недели)
- ✅ Постепенное усложнение (не overwhelming)
- ✅ Тестирование на каждом этапе
- ✅ Возможность замены Orchestrator в будущем (plug-and-play через MindBus)

---

### v0.1 (MVP Skeleton) - "Hello World" Orchestrator

**Цель**: Минимальный рабочий Orchestrator, который может выполнить простейший процесс.

**Функции** (только 🔴 КРИТИЧНЫЕ):

✅ **1.1. Подключение к MindBus**
- Публиковать COMMAND
- Подписываться на RESULT / ERROR
- Базовая трассировка (traceparent)

✅ **1.2. Работа с Node Registry**
- Получить список узлов
- Фильтровать по статусу (ready / offline)

✅ **1.3. Чтение Node Passport**
- Читать capabilities
- Базовое сопоставление (exact match)

✅ **2.1. Приём целей**
- Принимать цель как строку
- Фиксировать (без диалога)

✅ **2.2. Проектирование процессов**
- Читать готовый Process Card (YAML)
- Валидация базовая

✅ **3.1. Формирование COMMAND**
- Создавать корректные COMMAND согласно MESSAGE_FORMAT
- Базовые поля (action, params)

✅ **3.2. Выбор исполнителя**
- Детерминистический выбор (первый подходящий)
- Capability matching (exact match)

✅ **4.1. Приём RESULT**
- Сопоставление по correlation_id
- Извлечение output

✅ **4.2. Обработка ERROR**
- Логирование ошибки
- Останов процесса (без retry)

✅ **6.1. Трассировка и логирование**
- Логи всех COMMAND / RESULT / ERROR
- W3C Trace Context

**Что НЕ входит в v0.1**:
- ❌ Retry / Backoff
- ❌ Перераспределение задач
- ❌ Управление временем (TTL)
- ❌ Управление приоритетами
- ❌ Policy Layer
- ❌ LLM reasoning
- ❌ CONTROL сообщения
- ❌ Persistence (state в памяти)

**Пример Process Card для v0.1**:
```yaml
apiVersion: process/v1
kind: ProcessCard
metadata:
  name: "hello-world"
spec:
  steps:
    - id: step-1
      action: echo
      params:
        message: "Hello from Orchestrator!"
      agent: "dummy-agent-1"  # явное указание агента
```

**Критерий успеха v0.1**:
- Orchestrator читает Process Card
- Отправляет COMMAND dummy-agent-1
- Получает RESULT
- Логирует успех
- Процесс завершён

**Срок реализации**: 1-2 недели (Step 1.8.4 → Etap 2.1)

---

### v0.2 (Basic Resilience) - "Устойчивость к ошибкам"

**Цель**: Добавить базовую обработку ошибок и retry.

**Добавленные функции**:

✅ **2.3. Управление состоянием процессов**
- Persistence (сохранение в файл/БД)
- Восстановление после сбоя

✅ **3.3. Перераспределение задач**
- Fallback на другого агента при ошибке

✅ **4.3. Retry & Backoff**
- Retry policy (max 3 попытки)
- Exponential backoff

✅ **5.1. Управление временем**
- TTL для задач
- Timeout обработка

**Критерий успеха v0.2**:
- Orchestrator пережил сбой и продолжил процесс
- Агент упал → Orchestrator выбрал другого
- Timeout → Retry с увеличенным TTL

**Срок реализации**: 1 неделя (после v0.1)

---

### v0.3 (Observability & Control) - "Прозрачность и управление"

**Цель**: Добавить возможность управления процессами извне.

**Добавленные функции**:

✅ **5.2. Управление приоритетами**
- Динамические приоритеты
- Приоритетные очереди

✅ **6.2. Объяснение решений (базовое)**
- Логи с reasoning
- EVENT: orchestrator.decision

✅ **7.2. Реакция на CONTROL**
- STOP / PAUSE / RESUME

**Критерий успеха v0.3**:
- Человек может остановить процесс через CONTROL.STOP
- Orchestrator публикует события о решениях
- Приоритетные процессы выполняются раньше

**Срок реализации**: 1 неделя (после v0.2)

---

### v0.4 (Security & Policies) - "Безопасность и политики"

**Цель**: Добавить Policy Layer для безопасности.

**Добавленные функции**:

✅ **7.1. Контроль доступа**
- Policy Layer
- Forbidden actions
- Capability validation

✅ **2.2. Модификация Process Cards**
- Изменение по ходу выполнения (Policy-controlled)

**Критерий успеха v0.4**:
- Policy Layer блокирует запрещённые действия
- Orchestrator не может отправить COMMAND на удаление данных

**Срок реализации**: 1-2 недели (после v0.3)

---

### v0.5 (LLM Integration) - "Интеллектуальное планирование"

**Цель**: Добавить LLM для адаптивного планирования.

**Добавленные функции**:

✅ **2.1. Приём целей с диалогом**
- LLM уточняет цель
- Извлечение критериев успеха

✅ **2.2. Создание Process Cards**
- LLM генерирует Process Card из цели

✅ **3.2. Выбор исполнителя (умный)**
- LLM reasoning для выбора агента
- Учёт истории, контекста

✅ **6.2. Объяснение решений (полное)**
- LLM explain traces

**Критерий успеха v0.5**:
- Orchestrator принимает цель "Напиши книгу"
- LLM задаёт уточняющие вопросы
- LLM генерирует Process Card с 10 шагами
- LLM выбирает лучших агентов для каждого шага

**Срок реализации**: 2-3 недели (после v0.4)

---

### v1.0 (Production-Ready) - "Масштабирование и эволюция"

**Цель**: Production-grade система с полным функционалом.

**Добавленные функции**:

✅ **5.3. Управление бюджетами**
- Cost tracking
- Token limits
- Time budgets

✅ **8.1. Самоанализ**
- Статистика процессов
- Паттерны ошибок
- Улучшение стратегий

✅ **8.2. Обновляемость**
- Hot-swap Orchestrator
- Миграция процессов
- Версионность API

**Критерий успеха v1.0**:
- Orchestrator работает в production
- 99% uptime
- Автоматическое улучшение стратегий
- Поддержка бюджетов

**Срок реализации**: 1-2 месяца (после v0.5)

---

### Сводная таблица версий

| Версия | Фокус | Ключевые функции | Срок | Критерий успеха |
|--------|-------|------------------|------|-----------------|
| **v0.1** | MVP Skeleton | MindBus + Registry + COMMAND/RESULT | 1-2 нед | Hello World процесс |
| **v0.2** | Resilience | Retry, Fallback, TTL, Persistence | 1 нед | Пережил сбой |
| **v0.3** | Observability | Priorities, CONTROL, Events | 1 нед | Human control |
| **v0.4** | Security | Policy Layer, Access control | 1-2 нед | Blocked forbidden action |
| **v0.5** | LLM Integration | Goal intake, Smart planning, Explain | 2-3 нед | LLM создал Process Card |
| **v1.0** | Production | Budgets, Self-analysis, Hot-swap | 1-2 мес | 99% uptime |

**Общий срок**: ~2-3 месяца от v0.1 до v1.0

---

### Архитектурная эволюция

| Версия | Архитектурный вариант |
|--------|-----------------------|
| v0.1-v0.3 | **Вариант A** (Deterministic Workflow) - простой старт |
| v0.4 | **Переход к Варианту C** (добавляется Policy Layer) |
| v0.5 | **Вариант C** (Policy-Governed Hybrid) - LLM + Policies |
| v1.0+ | **Вариант C** + опциональный **Вариант B** (для экспериментов) |

---

## Use Cases (примеры использования)

### Use Case 1: Simple Single-Step Task (v0.1)

**Цель**: "Выполни echo команду"

**Process Card**:
```yaml
spec:
  steps:
    - id: echo
      action: echo
      params:
        message: "Hello"
      agent: "dummy-agent-1"
```

**Flow**:
1. Orchestrator читает Process Card
2. Формирует COMMAND:
   ```json
   {"type": "COMMAND", "data": {"action": "echo", "params": {"message": "Hello"}}}
   ```
3. Выбирает агента (dummy-agent-1 явно указан)
4. Отправляет COMMAND в MindBus
5. Получает RESULT: `{"output": {"echo": "Hello"}}`
6. Логирует успех
7. Процесс завершён ✅

---

### Use Case 2: Multi-Step Process with Dependency (v0.1)

**Цель**: "Найди информацию про AI, потом напиши summary"

**Process Card**:
```yaml
spec:
  steps:
    - id: research
      action: web_search
      params:
        query: "AI trends 2025"
      outputs:
        - research_data

    - id: summarize
      action: summarize
      depends_on: [research]
      params:
        text: "{{ steps.research.outputs.research_data }}"
```

**Flow**:
1. Orchestrator начинает с шага `research`
2. Отправляет COMMAND (action=web_search)
3. Получает RESULT с research_data
4. Сохраняет research_data в контекст
5. Переходит к шагу `summarize`
6. Подставляет research_data в params
7. Отправляет COMMAND (action=summarize)
8. Получает RESULT с summary
9. Процесс завершён ✅

---

### Use Case 3: Agent Failure and Retry (v0.2)

**Цель**: "Напиши главу книги"

**Scenario**:
1. Orchestrator отправляет COMMAND agent-1
2. Получает ERROR (DEADLINE_EXCEEDED)
3. **v0.2 функция**: Retry с другим агентом
4. Выбирает agent-2
5. Увеличивает TTL (300s → 450s)
6. Отправляет COMMAND agent-2
7. Получает SUCCESS ✅

**Без v0.2**: процесс завершился бы с ошибкой на шаге 2.

---

### Use Case 4: Human Stops Process (v0.3)

**Цель**: "Напиши 10 глав книги"

**Scenario**:
1. Orchestrator выполняет главы 1-5
2. Human видит, что качество низкое
3. Human отправляет CONTROL.STOP
4. **v0.3 функция**: Orchestrator останавливает процесс
5. Сохраняет текущее состояние (5/10 глав готово)
6. Публикует EVENT: "process_stopped_by_human"
7. Процесс приостановлен ⏸️

---

### Use Case 5: Policy Blocks Forbidden Action (v0.4)

**Цель**: "Удали все файлы в проекте"

**Scenario**:
1. LLM Planner: "Нужно отправить COMMAND с action=delete_files"
2. **v0.4 функция**: Policy Layer проверяет
3. Policy: "❌ delete_files в списке forbidden_actions"
4. Orchestrator блокирует команду
5. Публикует ERROR: "PERMISSION_DENIED: Forbidden action"
6. Процесс остановлен (безопасно) 🛡️

**Без v0.4**: команда была бы отправлена, файлы удалены.

---

### Use Case 6: LLM Creates Process Card (v0.5)

**Цель**: "Напиши книгу про AI" (без готового Process Card)

**Scenario**:
1. User: "Напиши книгу про AI"
2. **v0.5 функция**: Orchestrator использует LLM
3. LLM уточняет:
   - "Сколько глав?" → 10
   - "Какой стиль?" → Popular
4. LLM генерирует Process Card:
   ```yaml
   spec:
     steps:
       - id: research
       - id: outline
       - id: write-chapter-1
       ...
       - id: write-chapter-10
       - id: review
   ```
5. Orchestrator выполняет сгенерированный Process Card
6. Книга готова ✅

**Без v0.5**: пользователь должен был сам написать YAML.

---

### Use Case 7: Budget Exceeded (v1.0)

**Цель**: "Напиши 100-главную книгу"

**Scenario**:
1. User задаёт бюджет: $50
2. Orchestrator выполняет главы 1-40
3. Cost tracker: $48 потрачено
4. **v1.0 функция**: Orchestrator видит, что осталось $2
5. Рассчитывает: ещё 60 глав * $1.5/глава = $90 нужно
6. Orchestrator: "❌ Бюджет недостаточен"
7. Публикует ERROR: "RESOURCE_EXHAUSTED: Budget exceeded"
8. Процесс остановлен (контролируемо) 💰

---

## Граничные случаи

### Граничный случай 1: Все агенты offline

**Проблема**: Orchestrator не может найти подходящего агента.

**Сценарий**:
```
Задача требует: capabilities=["text_generation"]
Node Registry: Все агенты offline
```

**Решение**:

**v0.1 (MVP)**:
- Возвращает ERROR: "UNAVAILABLE: No agents available"
- Останавливает процесс

**v0.2+ (с retry)**:
- Ждёт с exponential backoff (1s, 2s, 4s...)
- Переспрашивает Node Registry
- Если через N попыток агенты не появились → ERROR

**v0.3+ (с приоритетами)**:
- Если процесс priority=9 (критичный):
  - Публикует EVENT: "waiting_for_agents"
  - Человек видит alert, запускает агентов
- Если процесс priority=1 (низкий):
  - Просто ждёт в очереди

---

### Граничный случай 2: Agent упал во время выполнения

**Проблема**: COMMAND отправлен, но агент не ответил (crash).

**Сценарий**:
```
1. Orchestrator отправил COMMAND agent-1
2. Agent-1 упал (crash)
3. Нет RESULT, нет ERROR
```

**Решение**:

**v0.1 (без TTL)**:
- Orchestrator ждёт бесконечно ❌ (плохо)

**v0.2+ (с TTL)**:
- TTL истекает через 300s
- Orchestrator: "Timeout, считаем это ERROR"
- Retry на другом агенте

**v0.3+ (с Node Registry heartbeat)**:
- Node Registry видит, что agent-1 не отвечает на heartbeat
- Меняет статус: ready → offline
- Orchestrator видит это, отменяет задачу на agent-1
- Выбирает agent-2

---

### Граничный случай 3: Ни один агент не подходит по capabilities

**Проблема**: Задача требует capability, которой нет ни у кого.

**Сценарий**:
```
Задача требует: capabilities=["quantum_computing"]
Node Registry:
  - agent-1: ["text_generation"]
  - agent-2: ["code_generation"]
→ Никто не умеет quantum_computing
```

**Решение**:

**v0.1-v0.4**:
- Возвращает ERROR: "FAILED_PRECONDITION: No agent with required capability"
- Останавливает процесс

**v0.5+ (с LLM)**:
- LLM анализирует: "Нет агента с quantum_computing"
- LLM предлагает альтернативу:
  - Упростить задачу?
  - Использовать external API?
  - Создать placeholder агента?
- Спрашивает у пользователя

---

### Граничный случай 4: Бесконечный цикл в Process Card

**Проблема**: Process Card содержит циклическую зависимость.

**Сценарий**:
```yaml
steps:
  - id: step-1
    depends_on: [step-2]
  - id: step-2
    depends_on: [step-1]
```

**Решение**:

**v0.1+ (валидация при чтении)**:
- Orchestrator проверяет DAG на циклы
- Если цикл найден → ERROR: "INVALID_ARGUMENT: Circular dependency detected"
- Отказ от выполнения

---

### Граничный случай 5: Process Card требует 100 агентов, доступно только 3

**Проблема**: Недостаточно ресурсов.

**Сценарий**:
```yaml
spec:
  parallelism: 100
  steps:
    - id: parallel-tasks
      repeat: 100
```

**Решение**:

**v0.1-v0.2**:
- Выполняет последовательно (медленно, но работает)

**v0.3+ (с очередями)**:
- Отправляет 3 задачи сразу (по количеству агентов)
- Остальные 97 задач в очередь
- По мере завершения запускает следующие

**v1.0+ (с budget)**:
- Анализирует: "100 задач * 60s = 6000s = 100 минут"
- Предупреждает пользователя: "Процесс займёт 100 минут"
- Пользователь решает: продолжать или масштабировать систему

---

## Связь с SSOT спецификациями

### Таблица соответствия

| Функция Orchestrator | SSOT Specification | Что используется |
|---------------------|-------------------|------------------|
| **1.1. Подключение к MindBus** | [mindbus_protocol_v1.md](../SSOT/mindbus_protocol_v1.md) | CloudEvents envelope, AMQP primitives, W3C Trace Context |
| **1.2. Работа с Node Registry** | [NODE_REGISTRY_SPEC_v1.0.md](../SSOT/NODE_REGISTRY_SPEC_v1.0.md) | API реестра, статусы узлов, heartbeat |
| **1.3. Чтение Node Passport** | [NODE_PASSPORT_SPEC_v1.0.md](../SSOT/NODE_PASSPORT_SPEC_v1.0.md) | Capabilities, constraints, security policies |
| **2.2. Проектирование процессов** | [PROCESS_CARD_SPEC_v1.0.md](../SSOT/PROCESS_CARD_SPEC_v1.0.md) | YAML структура, steps, dependencies |
| **3.1. Формирование COMMAND** | [MESSAGE_FORMAT_v1.1.md](../SSOT/MESSAGE_FORMAT_v1.1.md) | COMMAND type, action, params, requirements, context |
| **4.1. Приём RESULT** | [MESSAGE_FORMAT_v1.1.md](../SSOT/MESSAGE_FORMAT_v1.1.md) | RESULT type, status, output, execution_time_ms |
| **4.2. Обработка ERROR** | [MESSAGE_FORMAT_v1.1.md](../SSOT/MESSAGE_FORMAT_v1.1.md) | ERROR type, google.rpc.Code, error details |
| **6.1. Трассировка** | [mindbus_protocol_v1.md](../SSOT/mindbus_protocol_v1.md) | W3C Trace Context (traceparent) |
| **7.2. CONTROL сообщения** | [MESSAGE_FORMAT_v1.1.md](../SSOT/MESSAGE_FORMAT_v1.1.md) | CONTROL type, control_type (STOP/PAUSE/RESUME) |

### Валидация против SSOT

**Orchestrator ОБЯЗАН**:
- ✅ Валидировать все исходящие сообщения против MESSAGE_FORMAT v1.1.2
- ✅ Отклонять невалидные Process Cards (не соответствуют PROCESS_CARD_SPEC)
- ✅ Использовать только capabilities из NODE_PASSPORT
- ✅ Использовать только error codes из google.rpc.Code

**Пример валидации**:
```python
# Pseudo-code
def send_command(command_data):
    # Валидация против SSOT
    if not validate_against_schema(command_data, MESSAGE_FORMAT_COMMAND_SCHEMA):
        raise ValidationError("Invalid COMMAND format")

    # Отправка в MindBus
    mindbus.publish(command_data)
```

---

## Non-Functional Requirements

### Performance

**Требования**:
- Latency: < 100ms на формирование COMMAND (без LLM)
- Latency: < 3s на формирование COMMAND (с LLM в v0.5+)
- Throughput: 100+ процессов параллельно (v1.0)
- Memory: < 1GB RAM для v0.1, < 4GB для v1.0

---

### Scalability

**Требования**:
- Горизонтальное масштабирование (можно запустить несколько Orchestrator для разных процессов)
- Поддержка 1000+ узлов в Node Registry
- Поддержка 10000+ активных процессов (v1.0)

**Архитектурное решение**:
- Orchestrator = stateless (state в БД)
- Можно запустить N инстансов Orchestrator
- Load balancing через MindBus queues

---

### Reliability

**Требования**:
- Availability: 99% (v0.3), 99.9% (v1.0)
- Recovery: автоматическое восстановление после сбоя (v0.2+)
- Data persistence: все процессы сохраняются в БД (v0.2+)

**Архитектурное решение**:
- Persistence слой (PostgreSQL / etcd)
- Health checks
- Graceful shutdown

---

### Security

**Требования**:
- Authentication: MindBus AMQP auth (user/password или TLS)
- Authorization: Policy Layer (v0.4+)
- Audit: логи всех действий с timestamps

**Архитектурное решение**:
- Policy Layer как gatekeeper
- Все логи в структурированном формате (JSON)
- Интеграция с Security Controls (будущий документ)

---

### Observability

**Требования**:
- Logging: structured logs (JSON) всех событий
- Tracing: W3C Trace Context для всех сообщений
- Metrics: Prometheus-совместимые метрики
- Monitoring: интеграция с Monitor component

**Метрики**:
- `orchestrator_processes_total` (counter)
- `orchestrator_processes_active` (gauge)
- `orchestrator_command_sent_total` (counter)
- `orchestrator_errors_total` (counter by error_code)
- `orchestrator_latency_seconds` (histogram)

---

## Следующие шаги

### Краткосрочные (до создания ORCHESTRATOR_SPEC v1.0)

1. **Доработать этот документ**:
   - [ ] Добавить больше Use Cases
   - [ ] Проверить граничные случаи
   - [ ] Уточнить Non-Functional Requirements
   - [ ] Получить feedback от команды

2. **Провести ретроспективу Этапа 1** (Step 1.7):
   - [ ] Проанализировать практический опыт MindBus, Monitor, DummyAgent
   - [ ] Скорректировать функциональные требования на основе опыта
   - [ ] Зафиксировать в `docs/audits/ETAP_1_RETROSPECTIVE.md`

3. **Создать концепции компонентов** (Etap 1.8.1-1.8.3):
   - [ ] Agent Registration концепция
   - [ ] Quality Assessment концепция
   - [ ] Task Budget концепция

4. **Создать ORCHESTRATOR_SPEC_v1.0.md** (Step 1.8.4):
   - [ ] Финальная архитектура (скорее всего Policy-Governed Hybrid)
   - [ ] AsyncAPI спецификация
   - [ ] Pydantic схемы
   - [ ] Интеграция функций из этого документа

---

### Среднесрочные (Etap 2-3)

5. **Реализовать Orchestrator v0.1** (Etap 2.1):
   - [ ] Минимальный скелет (функции из таблицы приоритетов 🔴)
   - [ ] Интеграция с MindBus
   - [ ] Базовое чтение Process Cards
   - [ ] COMMAND/RESULT flow

6. **Тестировать на простых процессах** (Etap 2.2):
   - [ ] Hello World процесс
   - [ ] Multi-step процесс
   - [ ] Измерить метрики

7. **Итерация: v0.1 → v0.2 → v0.3** (Etap 3):
   - [ ] Добавлять функции по плану версионирования
   - [ ] Тестировать на каждом этапе
   - [ ] Собирать feedback

---

### Долгосрочные (Etap 4+)

8. **Эволюция до v1.0**:
   - [ ] LLM интеграция (v0.5)
   - [ ] Budgets, Self-analysis (v1.0)
   - [ ] Production deployment

9. **Возможность замены Orchestrator**:
   - [ ] Если v1.0 не устраивает, написать новый Orchestrator
   - [ ] Подключить через стандартный MindBus protocol
   - [ ] Существующие Process Cards работают без изменений

---

## Заключение

**Этот документ** - концептуальное ТЗ для Orchestrator, описывающее **ЧТО** он должен уметь.

**Ключевые идеи**:
1. ✅ **Полный список функций** - от базовых до стратегических
2. ✅ **Приоритизация** - 🔴 критично, 🟡 важно, 🟢 стратегия
3. ✅ **План версионирования** - v0.1 (скелет) → v1.0 (production)
4. ✅ **Граничные случаи** - что делать, когда всё идёт не так
5. ✅ **Связь с SSOT** - все функции опираются на существующие спецификации

**Философия разработки**:
- Начать с минимального скелета (v0.1)
- Наращивать функциональность как плагины
- Тестировать на каждом этапе
- Возможность замены Orchestrator в будущем (plug-and-play)

**Следующий шаг**: Ретроспектива Этапа 1 → доработка концепций → ORCHESTRATOR_SPEC_v1.0.md

---

**Статус**: 🚧 **ЧЕРНОВИК** - требует проверки, обсуждения и доработки

**Версия**: v0.1 (DRAFT)
**Дата последнего обновления**: 2025-12-15
**Авторы**: User + Claude Sonnet 4.5
