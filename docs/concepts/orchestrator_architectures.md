# Orchestrator Architectures: Концепции и анализ

**Статус документа**: 🚧 **ЧЕРНОВИК / WORK-IN-PROGRESS**

**Версия**: v0.1 (DRAFT)
**Дата создания**: 2025-12-15
**Автор концепций**: User + Claude Sonnet 4.5

---

## ⚠️ ВАЖНО: Это проект документа

**Этот документ**:
- ✅ Фиксирует начальные идеи и концепции Orchestrator
- ✅ Содержит критический анализ и оценку вариантов
- ⚠️ **ТРЕБУЕТ детальной доработки** перед реализацией
- ⚠️ **НЕ является финальной спецификацией**
- ⚠️ **НЕ готов к имплементации** без дальнейшей проработки

**Цель документа**: Сохранить идеи, чтобы не забыть, и служить основой для дальнейшего обсуждения и доработки.

**Следующие шаги**: Проверка, обсуждение, доработка → создание ORCHESTRATOR_SPEC_v1.0.md (после Step 1.7 - ретроспективы Этапа 1).

---

## 📋 Содержание

1. [Контекст и мотивация](#контекст-и-мотивация)
2. [Три архитектурных варианта](#три-архитектурных-варианта)
   - [Вариант A: Deterministic Workflow Engine](#вариант-a-deterministic-workflow-engine)
   - [Вариант B: LLM-Director](#вариант-b-llm-director)
   - [Вариант C: Policy-Governed Hybrid](#вариант-c-policy-governed-hybrid)
3. [Критический анализ](#критический-анализ)
4. [Сравнительная таблица](#сравнительная-таблица)
5. [Рекомендации](#рекомендации)
6. [Выявленные пробелы](#выявленные-пробелы)
7. [Следующие шаги](#следующие-шаги)

---

## Контекст и мотивация

**Проблема**: Нам нужен Orchestrator для управления процессами (Process Cards) и координации агентов.

**Стратегия реализации** (из IMPLEMENTATION_ROADMAP v2.0):
1. **Сначала практика** (Steps 1.2-1.7): MindBus, Monitor, DummyAgent, реальный AI-агент
2. **Ретроспектива** (Step 1.7): Анализ практического опыта
3. **Концепции** (Etap 1.8): Разработка концепций на основе опыта
4. **Спецификация** (Step 1.8.4): ORCHESTRATOR_SPEC_v1.0.md
5. **Реализация** (Etap 2): Orchestrator v0.1

**Текущий этап**: Etap 1.8 - концептуальная проработка (этот документ).

---

## Три архитектурных варианта

### Вариант A: Deterministic Workflow Engine

**Философия**: Process Card как программа, Orchestrator как runtime/interpreter.

#### Основные характеристики

**1. Process Card = Executable Program**

```yaml
spec:
  steps:
    - id: step-1
      action: research
      agent_requirements:
        capabilities: ["web_search", "summarization"]
      inputs:
        topic: "{{ variables.topic }}"
      outputs:
        result: research_data

    - id: step-2
      action: write_chapter
      agent_requirements:
        capabilities: ["text_generation"]
      inputs:
        research: "{{ steps.step-1.outputs.result }}"
        chapter_number: "{{ variables.chapter }}"
      outputs:
        result: chapter_text

    - id: step-3
      action: review
      agent_requirements:
        capabilities: ["code_review", "text_analysis"]
      inputs:
        text: "{{ steps.step-2.outputs.result }}"
```

**2. Orchestrator Role**

- **Интерпретация**: Парсинг Process Card YAML
- **Планирование**: Построение DAG из steps
- **Execution**: Пошаговое выполнение с учётом `depends_on`
- **Routing**: Capability matching → выбор агента из Node Registry
- **State Management**: Сохранение промежуточных outputs
- **Error Handling**: Retry policies из Process Card

**3. Deterministic Guarantees**

```yaml
spec:
  retry_policy:
    max_attempts: 3
    backoff: exponential
    timeout: 300s

  failure_handling:
    on_step_failure: retry_then_fail
    on_timeout: escalate_to_human
```

#### Сильные стороны

✅ **Полная детерминированность** - одинаковый Process Card → одинаковое выполнение
✅ **Прозрачность** - каждое решение Orchestrator явно описано в YAML
✅ **Тестируемость** - можно написать unit-тесты для каждого Process Card
✅ **Production-ready** - стабильно, предсказуемо, легко отлаживать
✅ **Observability** - каждый step логируется, понятно что происходит
✅ **Версионность** - Process Card в git → полная история изменений

#### Слабые стороны

❌ **Негибкость** - нужно заранее описать все сценарии в YAML
❌ **Сложность для динамических процессов** - если процесс зависит от результатов, нужны сложные conditions
❌ **Overhead на создание Process Cards** - каждый процесс требует ручного YAML
❌ **Ограниченная адаптивность** - не может "придумать" новый шаг на лету

#### Примеры использования

**Лучше всего для**:
- Повторяющиеся процессы (daily reports, code reviews)
- Критичные процессы с compliance требованиями
- Процессы, где важна воспроизводимость

**Хуже всего для**:
- Исследовательские задачи ("разберись в этом проекте")
- Творческие задачи с неопределённым результатом

---

### Вариант B: LLM-Director

**Философия**: Process Card как директива, LLM как мозг Orchestrator.

#### Основные характеристики

**1. Process Card = High-Level Directive**

```yaml
spec:
  goal: "Написать книгу про AI"

  constraints:
    - no_plagiarism
    - cite_sources
    - max_chapters: 10

  resources:
    max_agents: 5
    timeout: 24h
```

**2. Orchestrator Role**

- **Planning**: LLM разбивает цель на подзадачи динамически
- **Agent Selection**: LLM решает какой агент нужен, основываясь на capabilities
- **Adaptation**: Если шаг провалился, LLM придумывает альтернативный план
- **Context Management**: LLM сам решает какой контекст передавать следующему агенту

**3. Пример работы**

```
User Request: "Напиши книгу про AI"

LLM Orchestrator думает:
1. "Мне нужно сначала изучить тему"
   → Отправляет COMMAND research агенту с web_search
2. Получает результат research
   → "Теперь нужен план книги"
   → Отправляет COMMAND outline агенту с text_generation
3. Получает outline
   → "Отлично, теперь пишем главы по очереди"
   → Для каждой главы отправляет COMMAND write_chapter
4. После каждой главы
   → "Нужен review"
   → Отправляет COMMAND review агенту с code_review
```

#### Сильные стороны

✅ **Максимальная гибкость** - LLM адаптируется к любой ситуации
✅ **Простота для пользователя** - минимальный Process Card (только цель)
✅ **Адаптивность** - если план не работает, LLM меняет стратегию
✅ **Творческий подход** - LLM может "придумать" нестандартное решение
✅ **Естественный язык** - Process Card близок к human intent

#### Слабые стороны

❌ **Непредсказуемость** - одна и та же задача может выполниться по-разному
❌ **Сложность отладки** - "почему LLM выбрал этого агента?" → unclear
❌ **Риск зацикливания** - LLM может застрять в retry loop
❌ **Latency** - каждое решение требует LLM inference (~1-3s)
❌ **Cost** - LLM inference дорого при больших объёмах
❌ **Observability** - трудно понять "что думает Orchestrator"
❌ **Безопасность** - LLM может сделать неожиданное (отправить лишние команды)

#### Примеры использования

**Лучше всего для**:
- Исследовательские задачи ("разберись в этом проекте и предложи улучшения")
- Творческие задачи ("придумай архитектуру для этой системы")
- Прототипирование (MVP, эксперименты)

**Хуже всего для**:
- Production-критичные процессы (нет гарантий детерминизма)
- Процессы с compliance требованиями (трудно аудитировать)

---

### Вариант C: Policy-Governed Hybrid

**Философия**: Лучшее из обоих миров - Policy Layer (детерминизм) + LLM (гибкость).

#### Основные характеристики

**1. Process Card = Goals + Policies**

```yaml
spec:
  goal: "Написать книгу про AI"

  policies:
    - type: budget
      max_steps: 50
      max_agents: 5
      timeout: 24h

    - type: capability_matching
      required_capabilities:
        - web_search
        - text_generation
        - code_review

    - type: retry
      max_retries_per_step: 3
      backoff: exponential

    - type: safety
      forbidden_actions:
        - delete_data
        - external_api_call

      required_approvals:
        - action: publish
          approver: human

  strategy: "llm"  # или "deterministic" для fallback
```

**2. Orchestrator Architecture**

```
┌─────────────────────────────────────────────┐
│           Orchestrator Core                 │
├─────────────────────────────────────────────┤
│                                             │
│  ┌──────────────┐         ┌──────────────┐ │
│  │ Policy Layer │ ──────> │ LLM Planner  │ │
│  │ (Guardrails) │ <────── │ (Flexible)   │ │
│  └──────────────┘         └──────────────┘ │
│         │                        │         │
│         ├─ Budget enforcement    │         │
│         ├─ Safety checks         │         │
│         ├─ Capability validation │         │
│         └─ Retry limits          │         │
│                                  │         │
│                     ┌────────────┘         │
│                     ▼                      │
│         ┌──────────────────────┐          │
│         │  Execution Engine    │          │
│         │  (Deterministic)     │          │
│         └──────────────────────┘          │
│                     │                      │
└─────────────────────┼──────────────────────┘
                      ▼
              ┌──────────────┐
              │  MindBus     │
              │  (AMQP)      │
              └──────────────┘
```

**3. Пример работы**

```
1. User Request: "Напиши книгу про AI"

2. Orchestrator:
   - Policy Layer проверяет: budget ok, capabilities ok
   - LLM Planner: "Нужно сделать research → outline → chapters"

3. Step 1: Research
   - LLM: "Отправлю COMMAND research с action=web_search"
   - Policy Layer: "Проверяю capability web_search в Node Registry... ✅"
   - Policy Layer: "Проверяю budget (step 1/50)... ✅"
   - Execution Engine: отправляет COMMAND в MindBus

4. Agent отвечает ERROR (timeout)
   - LLM: "Попробую ещё раз"
   - Policy Layer: "Проверяю retry policy (attempt 1/3)... ✅"
   - Execution Engine: retry COMMAND

5. Agent отвечает SUCCESS
   - LLM: "Отлично, теперь нужен outline"
   - Policy Layer: "Проверяю budget (step 2/50)... ✅"

6. LLM хочет отправить action=delete_data
   - Policy Layer: "❌ ЗАПРЕЩЕНО политикой safety"
   - LLM: "Ок, пропущу этот шаг"
```

#### Сильные стороны

✅ **Баланс гибкости и безопасности** - LLM может адаптироваться, но в рамках политик
✅ **Observability** - Policy Layer логирует все проверки
✅ **Тестируемость** - можно тестировать Policy Layer отдельно
✅ **Контролируемость** - явные ограничения в Process Card
✅ **Fallback** - если LLM недоступен, можно переключиться на deterministic mode
✅ **Постепенная миграция** - можно начать с простых политик, усложнять по мере необходимости

#### Слабые стороны

⚠️ **Сложность архитектуры** - два компонента (Policy + LLM) вместо одного
⚠️ **Нужна балансировка** - слишком строгие политики → теряем гибкость
⚠️ **Latency** - Policy checks + LLM inference
⚠️ **Требует дизайна политик** - нужно продумать какие политики нужны

#### Примеры использования

**Лучше всего для**:
- **MVP и production одновременно** - начинаем с гибкости, постепенно добавляем политики
- **Процессы с частичной неопределённостью** - общая структура известна, детали гибкие
- **Постепенное ужесточение** - сначала экспериментируем, потом добавляем guardrails

**Универсальность**: Подходит для большинства сценариев.

---

## Критический анализ

### Метрики для оценки

Оценка по 5 критериям (каждый от 0 до 10):

1. **Determinism** - предсказуемость результата
2. **Safety** - защита от ошибок/нежелательных действий
3. **Speed of iteration** - как быстро можно создавать новые процессы
4. **Quality of decisions** - как хорошо Orchestrator выбирает агентов/стратегии
5. **Observability** - насколько легко понять что происходит

### Оценки

#### Вариант A: Deterministic Workflow Engine

| Метрика | Оценка | Обоснование |
|---------|--------|-------------|
| **Determinism** | 10/10 | Полная детерминированность - YAML как программа |
| **Safety** | 9/10 | Все действия явно описаны, легко аудитировать. -1 за человеческий фактор в YAML |
| **Speed of iteration** | 6/10 | Каждый процесс требует ручного YAML. Медленно для экспериментов |
| **Quality of decisions** | 8/10 | Качество = качество YAML. Хорошо для известных процессов |
| **Observability** | 10/10 | Идеальная прозрачность - каждый step явный |
| **ИТОГО** | **43/50 (86%)** | Отлично для production, тяжело для MVP |

**Вердикт**: Production-grade решение, но требует много работы на старте.

---

#### Вариант B: LLM-Director

| Метрика | Оценка | Обоснование |
|---------|--------|-------------|
| **Determinism** | 3/10 | Низкая предсказуемость - LLM может выбрать разные пути |
| **Safety** | 4/10 | Риск неожиданных действий. Нужны дополнительные guardrails |
| **Speed of iteration** | 10/10 | Максимальная скорость - минимальный Process Card |
| **Quality of decisions** | 8/10 | LLM хорош в адаптации, но может ошибаться |
| **Observability** | 5/10 | Сложно понять "почему LLM выбрал это". Нужны explain traces |
| **ИТОГО** | **30/50 (60%)** | Отлично для MVP, рискованно для production |

**Вердикт**: Идеален для быстрых экспериментов и исследовательских задач. Не подходит для критичных процессов без доработки.

---

#### Вариант C: Policy-Governed Hybrid

| Метрика | Оценка | Обоснование |
|---------|--------|-------------|
| **Determinism** | 7/10 | Policy Layer даёт гарантии, но LLM вносит вариативность |
| **Safety** | 9/10 | Policy Layer как guardrails - отличная защита |
| **Speed of iteration** | 8/10 | Быстрее A (минимальный YAML), медленнее B (нужны политики) |
| **Quality of decisions** | 9/10 | LLM адаптируется, Policy Layer корректирует |
| **Observability** | 8/10 | Policy logs + LLM reasoning traces = хорошая видимость |
| **ИТОГО** | **41/50 (82%)** | Практичный баланс для реального продукта |

**Вердикт**: Лучший баланс для MVP → Production. Можно начать с простых политик, усложнять постепенно.

---

## Сравнительная таблица

| Характеристика | A: Workflow Engine | B: LLM-Director | C: Policy-Governed |
|----------------|--------------------|-----------------|--------------------|
| **Детерминизм** | ✅ Полный | ❌ Низкий | ⚠️ Средний |
| **Безопасность** | ✅ Высокая | ❌ Требует guardrails | ✅ Высокая |
| **Скорость создания процессов** | ❌ Медленная | ✅ Быстрая | ⚠️ Средняя |
| **Адаптивность** | ❌ Низкая | ✅ Высокая | ✅ Высокая |
| **Observability** | ✅ Отличная | ⚠️ Сложная | ✅ Хорошая |
| **Подходит для MVP** | ❌ Нет (долго) | ✅ Да (быстро) | ✅ Да |
| **Подходит для Production** | ✅ Да (стабильно) | ❌ Нет (рискованно) | ✅ Да |
| **Complexity** | ⚠️ Средняя | ✅ Низкая | ⚠️ Высокая |
| **Cost (runtime)** | ✅ Низкая | ❌ Высокая (LLM) | ⚠️ Средняя |
| **Итоговый балл** | 43/50 (86%) | 30/50 (60%) | 41/50 (82%) |

---

## Рекомендации

### Итоговая рекомендация

**Согласен с рекомендацией пользователя**:

1. **Вариант C (Policy-Governed Hybrid) как базовая архитектура**
   - ✅ Лучший баланс для MVP → Production
   - ✅ Гибкость LLM + безопасность Policy Layer
   - ✅ Можно начать просто, усложнять постепенно

2. **Вариант B (LLM-Director) как экспериментальный режим**
   - ✅ Оставить возможность `strategy: "llm"` в Process Card
   - ✅ Использовать для исследовательских задач
   - ⚠️ С явным предупреждением о рисках

3. **Вариант A (Workflow Engine) как fallback**
   - ✅ Если LLM недоступен, использовать детерминистический mode
   - ✅ Для критичных процессов можно явно задать все steps в YAML

### Стратегия реализации

**Этап 1: Минимальная Policy-Governed архитектура (Step 1.8 → Etap 2)**

```yaml
# Начальные политики для MVP:
policies:
  - type: budget
    max_steps: 100

  - type: capability_matching
    strict: true  # требовать точное совпадение capabilities

  - type: retry
    max_retries_per_step: 3
```

**Этап 2: Постепенное усложнение (Etap 3-5)**

По мере накопления опыта добавлять:
- Safety policies (forbidden actions)
- Approval policies (human-in-the-loop)
- Cost policies (ограничение расходов на LLM)
- Quality policies (минимальные требования к результатам)

**Этап 3: LLM-Director как опция (Etap 6+)**

Для продвинутых пользователей:
```yaml
spec:
  strategy: "llm"  # использовать LLM без Policy Layer
  warning: "Experimental mode - no safety guarantees"
```

---

## Выявленные пробелы

### 1. Отсутствие конкретных примеров

**Проблема**: Все три варианта описаны абстрактно.

**Что нужно**:
- ✅ Минимум 2-3 полных примера Process Cards для каждого варианта
- ✅ Примеры для разных сценариев:
  - Простой процесс (1-2 шага)
  - Процесс с ветвлением (conditions)
  - Процесс с retry и error handling
  - Процесс с human approval

**Где добавить**: `examples/process_cards/` directory

---

### 2. Неполный список начальных политик для Варианта C

**Проблема**: Упоминается Policy Layer, но не ясно какие политики нужны для MVP.

**Что нужно**:
- ✅ Список ВСЕХ политик для MVP (budget, capability, retry, safety)
- ✅ Формат описания политик в Process Card
- ✅ Приоритизация политик (какие критичны, какие optional)

**Предложение для MVP**:
```yaml
# Минимальный набор политик для начала:
policies:
  - type: budget           # КРИТИЧНО - защита от зацикливания
  - type: capability       # КРИТИЧНО - выбор правильного агента
  - type: retry            # КРИТИЧНО - обработка ошибок
  - type: timeout          # ВАЖНО - защита от hang
  # НЕ НУЖНО для MVP:
  # - type: safety        # можно добавить позже
  # - type: approval      # можно добавить позже
  # - type: cost          # можно добавить позже
```

---

### 3. Отсутствие описания миграции между вариантами

**Проблема**: Не ясно как переключаться между A/B/C или как мигрировать от B к C.

**Что нужно**:
- ✅ Сценарий миграции: "Начали с B (быстрый MVP), хотим перейти на C (добавить безопасность)"
- ✅ Backward compatibility: как поддерживать старые Process Cards при изменении Orchestrator
- ✅ Versioning: как версионировать Orchestrator API

**Предложение**:
```yaml
# Process Card с указанием версии Orchestrator:
apiVersion: orchestrator/v1
kind: ProcessCard
spec:
  strategy: "policy-governed"  # или "llm" или "deterministic"
  orchestrator_version: ">=0.2.0"  # минимальная версия Orchestrator
```

---

### 4. Нет описания LLM Planner интерфейса

**Проблема**: Не ясно как LLM принимает решения в вариантах B и C.

**Что нужно**:
- ✅ Формат промпта для LLM Planner
- ✅ Структура reasoning trace (как LLM объясняет свои решения)
- ✅ Интеграция с Node Registry (как LLM видит доступных агентов)

**Пример промпта**:
```
You are Orchestrator LLM. Your goal: {goal from Process Card}

Available agents in Node Registry:
- agent-1: capabilities [web_search, summarization]
- agent-2: capabilities [text_generation]

Current state: {outputs from previous steps}

Policies enforced:
- Budget: {current_step}/{max_steps}
- Retry: {retry_count}/{max_retries}

Decide next action:
1. Which agent to call?
2. What COMMAND to send?
3. What inputs to provide?

Respond in JSON:
{
  "reasoning": "...",
  "next_action": {
    "agent": "agent-1",
    "command": {...}
  }
}
```

---

### 5. Отсутствие метрик успешности

**Проблема**: Как измерять "хорошо ли работает Orchestrator"?

**Что нужно**:
- ✅ KPI для каждого варианта:
  - A: % процессов завершённых успешно, среднее время создания Process Card
  - B: % процессов с неожиданным поведением, среднее время выполнения
  - C: % срабатываний Policy Layer, latency Policy checks

**Предложение**:
```yaml
# Метрики в Process Card:
metadata:
  success_criteria:
    - type: completion_rate
      target: ">95%"

    - type: execution_time
      target: "<30m"

    - type: policy_violations
      target: "0"
```

---

## Следующие шаги

### Краткосрочные (до создания ORCHESTRATOR_SPEC_v1.0.md)

1. **Доработать концепции** (этот документ):
   - [ ] Добавить 2-3 полных примера Process Cards
   - [ ] Определить минимальный набор политик для MVP
   - [ ] Описать LLM Planner интерфейс
   - [ ] Добавить метрики успешности

2. **Провести ретроспективу Этапа 1** (Step 1.7):
   - [ ] Проанализировать опыт работы с MindBus, Monitor, DummyAgent, реальным AI-агентом
   - [ ] Зафиксировать находки в `docs/audits/ETAP_1_RETROSPECTIVE.md`
   - [ ] Скорректировать концепции на основе практического опыта

3. **Создать концепции компонентов Orchestrator** (Etap 1.8.1-1.8.3):
   - [ ] Agent Registration (Step 1.8.1)
   - [ ] Quality Assessment (Step 1.8.2)
   - [ ] Task Budget (Step 1.8.3)

4. **Создать ORCHESTRATOR_SPEC_v1.0.md** (Step 1.8.4):
   - [ ] На основе этого документа + ретроспективы + концепций компонентов
   - [ ] С финальной архитектурой (скорее всего Policy-Governed Hybrid)
   - [ ] С AsyncAPI спецификацией
   - [ ] С Pydantic схемами

### Среднесрочные (Etap 2-3)

5. **Реализовать Orchestrator v0.1** (Etap 2):
   - [ ] Начать с минимальной Policy-Governed архитектуры
   - [ ] Только базовые политики: budget, capability, retry
   - [ ] Простой LLM Planner (без сложных reasoning chains)

6. **Тестировать на реальных процессах** (Etap 3):
   - [ ] Создать 5-10 тестовых Process Cards разной сложности
   - [ ] Измерить метрики (completion rate, execution time, policy violations)
   - [ ] Собрать feedback от пользователей

### Долгосрочные (Etap 4+)

7. **Усложнять постепенно**:
   - [ ] Добавлять новые политики по мере необходимости
   - [ ] Улучшать LLM Planner (более сложные reasoning chains)
   - [ ] Добавлять LLM-Director режим для экспериментов

---

## Заключение

**Этот документ** - стартовая точка для проектирования Orchestrator.

**Ключевые решения**:
1. ✅ **Вариант C (Policy-Governed Hybrid)** как базовая архитектура
2. ✅ **Вариант B (LLM-Director)** как экспериментальный режим
3. ✅ **Постепенное усложнение** - начать с минимальных политик, добавлять по мере необходимости

**Следующий шаг**: Доработка концепций → Ретроспектива Этапа 1 → ORCHESTRATOR_SPEC_v1.0.md

---

**Статус**: 🚧 **ЧЕРНОВИК** - требует проверки, обсуждения и доработки

**Версия**: v0.1 (DRAFT)
**Дата последнего обновления**: 2025-12-15
**Авторы**: User + Claude Sonnet 4.5
