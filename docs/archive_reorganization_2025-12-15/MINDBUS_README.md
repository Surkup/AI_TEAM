# MindBus - Концептуальная архитектура шины данных

**Статус документа**: Концептуальный черновик (Draft)
**Версия**: 0.2
**Дата**: 2025-01-12

---

## Оглавление

1. [Что такое MindBus](#что-такое-mindbus)
2. [Ключевая идея](#ключевая-идея)
3. [Фундаментальные принципы MindBus](#фундаментальные-принципы-mindbus)
4. [Архитектурные метафоры](#архитектурные-метафоры)
5. [Типы сообщений и канонический формат](#типы-сообщений-и-канонический-формат)
6. [Принцип универсального подключения](#принцип-универсального-подключения)
7. [Примеры структур данных](#примеры-структур-данных)
8. [Вопросы на будущее](#вопросы-на-будущее)
9. [Пояснения: Зачем нам эти аналогии](#пояснения-зачем-нам-эти-аналогии)

---

## Что такое MindBus

**MindBus** — это единая коммуникационная шина (скелет, нервная система) всей системы AI_TEAM.

Через MindBus взаимодействуют все компоненты системы:
- **Orchestrator** (Оркестратор-директор)
- **AI-агенты** (Исполнители задач)
- **Storage Layer** (Хранилище данных)
- **Monitoring Interface** (Интерфейс мониторинга)
- **Внешние сервисы** (API, базы данных, файловые хранилища)

**Главная цель MindBus**: обеспечить надёжную, прозрачную и универсальную коммуникацию между всеми компонентами системы, не заставляя их "знать друг о друге".

---

## Ключевая идея

### Компоненты не "договариваются друг с другом" — они подключаются к шине и говорят на едином языке.

Это значит:
- ✅ **Любой новый модуль** может быть добавлен без изменения существующих компонентов
- ✅ **Единый канонический формат сообщений** — все говорят на одном языке
- ✅ **Адаптеры/драйверы** для подключения компонентов со своей спецификой
- ✅ **Полная наблюдаемость** — все сообщения логируются и трассируются
- ✅ **Отказоустойчивость** — падение одного компонента не рушит систему

---

## Фундаментальные принципы MindBus

Эти принципы определяют философию работы шины и взяты из проверенных архитектур сетей и компьютеров.

### 1️⃣ **Принцип "Шина доставляет, но не думает"**

**Из архитектуры:** Роутер в сети, системная шина в компьютере.

MindBus отвечает **только** за:
- Доставку сообщений адресатам
- Маршрутизацию (кому отправить)
- Логирование и трассировку
- Обеспечение надёжности доставки

MindBus **НЕ отвечает** за:
- Принятие бизнес-решений (это делает Orchestrator)
- Хранение состояния процессов (это делает Storage Layer)
- Обработку данных (это делают Agents)
- Управление задачами (это делает Orchestrator)

**Метафора:** Роутер в сети не решает, что должен делать ваш компьютер — он только доставляет пакеты.

---

### 2️⃣ **Принцип "Всё через шину"**

**Из архитектуры:** Системная шина компьютера, корпоративная сеть.

Любое взаимодействие между компонентами системы идёт **только через MindBus**.

- ❌ **Нет прямых вызовов** "агент → база данных"
- ❌ **Нет обхода шины** для "быстрого доступа"
- ✅ **Все интеграции через адаптеры** и MindBus

**Что это даёт:**
- Полная наблюдаемость (все сообщения видны)
- Единая точка контроля безопасности
- Возможность замены компонентов без изменения других
- Простота тестирования (можно перехватить сообщения)

**Метафора:** В компьютере все устройства общаются через системную шину, а не напрямую друг с другом.

---

### 3️⃣ **Принцип "Единый канонический формат"**

**Из архитектуры:** TCP/IP протокол, USB стандарт, PCI Express.

Все сообщения в MindBus следуют **единому формату Message** (конверт + payload).

- Один стандарт для всех компонентов
- Изменения протокола — управляемые и обратно совместимые
- Валидация на входе/выходе

**Что это даёт:**
- Нет хаоса форматов данных
- Легко отлаживать (все сообщения одинаковой структуры)
- Простота интеграции новых модулей

**Метафора:** Как TCP/IP — все устройства в интернете говорят на одном протоколе, независимо от производителя.

---

### 4️⃣ **Принцип "Ссылки, а не данные"**

**Из архитектуры:** URL в Web, указатели в программировании, DMA в компьютере.

MindBus передаёт **ссылки на большие данные**, а не сами данные.

- Малые данные (метаданные, короткие тексты) — можно inline в payload
- Большие данные (файлы, изображения, датасеты) — только ссылка на Storage

**Что это даёт:**
- Шина остаётся быстрой и лёгкой
- Нет перегрузки сети
- Эффективное использование ресурсов

**Метафора:** В HTML вы не встраиваете картинку 10 МБ в код страницы — вы ставите `<img src="url">`.

---

### 5️⃣ **Принцип "Приоритеты и управление очередями"**

**Из архитектуры:** QoS (Quality of Service) в сетях, приоритеты прерываний в CPU, nice в Unix/Linux.

Сообщения имеют **приоритет**, который определяет порядок обработки.

- Критичные сообщения обрабатываются первыми
- Фоновые задачи не блокируют важные операции
- Человек может прерывать систему через высокоприоритетные команды

**Что это даёт:**
- Система реагирует на критичные события мгновенно
- Можно остановить/изменить процесс по команде человека
- Оптимизация использования ресурсов

**Метафора:** В сети VoIP-трафик (голос) имеет приоритет над скачиванием файлов, чтобы не было задержек в разговоре.

---

### 6️⃣ **Принцип "Идемпотентность и повторная доставка"**

**Из архитектуры:** TCP retransmission, HTTP PUT/DELETE, Kafka at-least-once delivery.

Компоненты системы должны быть **устойчивы к повторной доставке** одного и того же сообщения.

- Сообщение может быть доставлено дважды (при сбоях сети)
- Получатель должен обработать его корректно (не создавать дубликаты)
- Проверка "уже обработал ли я это сообщение?"

**Что это даёт:**
- Надёжность системы при сбоях
- Возможность retry без побочных эффектов
- Защита от дублирования операций

**Метафора:** TCP переотправляет потерянный пакет, но получатель игнорирует дубликаты по sequence number.

---

### 7️⃣ **Принцип "Изоляция и безопасность"**

**Из архитектуры:** VLAN в сетях, Unix permissions, Zero Trust Network, Pub/Sub топики.

Компоненты видят **только те сообщения, к которым у них есть доступ**.

- Агент подписан только на нужные топики/каналы
- Не может читать "чужие" сообщения
- Права доступа проверяются MindBus

**Что это даёт:**
- Безопасность системы (изоляция компонентов)
- Нет "случайных" утечек данных
- Защита от злоупотреблений

**Метафора:** В VLAN устройства из одной подсети не видят трафик другой подсети.

---

### 8️⃣ **Принцип "Версионирование и обратная совместимость"**

**Из архитектуры:** HTTP/1.1 → HTTP/2, IPv4 → IPv6, API versioning.

Протокол MindBus **эволюционирует, сохраняя совместимость** со старыми компонентами.

- Новые поля добавляются опционально
- Старые компоненты игнорируют неизвестные поля
- Критичные изменения — через новую версию протокола

**Что это даёт:**
- Система может обновляться постепенно
- Нет "большого взрыва" при изменениях
- Старые и новые компоненты работают вместе

**Метафора:** HTTP/2 клиент может работать с HTTP/1.1 сервером через fallback.

---

### 9️⃣ **Принцип "Наблюдаемость из коробки"**

**Из архитектуры:** Мониторинг сетевого трафика, логирование системных вызовов, трассировка в распределённых системах.

Все сообщения **автоматически логируются и трассируются**.

- Каждое сообщение имеет `trace_id` — связывает всю цепочку операций
- Метрики (количество сообщений, задержки, ошибки)
- Полный аудит (что, когда, кто, кому)

**Что это даёт:**
- Легко отлаживать проблемы
- Понятно, что происходит в системе
- Аудит для compliance

**Метафора:** В сети можно включить tcpdump и увидеть все пакеты, проходящие через роутер.

---

## Архитектурные метафоры

MindBus проектируется с опорой на две проверенные временем архитектуры:

### 1️⃣ Материнская плата компьютера (Motherboard + System Bus)

**Что берём из этой аналогии:**

- **Единый стандарт интерфейсов**: Как PCI/USB/SATA — любое устройство с правильным разъёмом подключается к материнке
- **Plug-and-play модульность**: Добавил видеокарту — система её распознала, установил драйвер — заработала
- **Драйверы/контроллеры**: Каждое устройство имеет драйвер, который переводит команды ОС в команды устройства
- **Единая шина данных**: Процессор, память, диск общаются через одну шину по общему протоколу

**Как это работает в MindBus:**

```
┌──────────────────────────────────────────────────────┐
│                     MindBus                          │
│            (Системная шина + протокол)               │
└─────┬────────┬────────┬────────┬─────────┬──────────┘
      │        │        │        │         │
      ▼        ▼        ▼        ▼         ▼
   [Agent] [Agent] [Orch] [Storage] [Monitor]
      │        │        │        │         │
   [Адаптер][Адаптер][Адаптер][Адаптер][Адаптер]
```

- **MindBus** = Материнская плата + шина
- **Подключаемые модули** = Устройства (процессор, память, диск, видеокарта)
- **Адаптеры** = Драйверы, переводящие канонический протокол в специфику модуля

**Преимущества:**
- ✅ Единый стандарт формата сообщений
- ✅ Модульность "из коробки"
- ✅ SSOT (Single Source of Truth) для протокола

**Ограничения:**
- ⚠️ Ассоциация с монолитом ("всё в одном корпусе")
- ⚠️ Скрывает распределённость системы

---

### 2️⃣ Корпоративная локальная сеть (Corporate LAN / Network)

**Что берём из этой аналогии:**

- **Распределённость узлов**: Компьютеры, принтеры, серверы, хранилища — все в одной сети, но физически раздельны
- **Роутер/коммутатор**: Центральный узел маршрутизации, который не "думает за устройства", а доставляет пакеты
- **Единый сетевой протокол**: TCP/IP — все устройства говорят на одном языке
- **Независимость узлов**: Узел может быть offline, перезагружаться, обновляться — сеть продолжает работать
- **Наблюдаемость**: Мониторинг трафика, логи маршрутов, метрики сети
- **Безопасность**: Сегментация сети, права доступа, изоляция узлов

**Как это работает в MindBus:**

```
                    ┌─────────────┐
                    │  MindBus    │
                    │  (Router +  │
                    │  Protocol)  │
                    └──────┬──────┘
                           │
       ┌───────────────────┼───────────────────┐
       │                   │                   │
       ▼                   ▼                   ▼
   ┌────────┐         ┌────────┐         ┌────────┐
   │ Agent  │         │Orch    │         │Storage │
   │ (Node) │         │(Node)  │         │(Node)  │
   └────────┘         └────────┘         └────────┘
```

- **MindBus** = Роутер + сетевой протокол (IP/TCP аналог)
- **Узлы сети** = Агенты, Оркестратор, Storage, Monitor
- **Пакеты** = Сообщения (Message)
- **Адресация** = Имена/ID компонентов (как IP-адреса)
- **Trace ID** = Session ID / Request ID в распределённых системах

**Преимущества:**
- ✅ Естественная распределённость
- ✅ Отказоустойчивость (узел упал — сеть работает)
- ✅ Независимость компонентов
- ✅ Наблюдаемость трафика и маршрутов
- ✅ Безопасность и права доступа

**Ограничения:**
- ⚠️ Риск "роутер = мозг" (MindBus должен быть лёгким, не принимать бизнес-решения)
- ⚠️ Может потерять "единый стандарт", если не зафиксировать протокол

---

### 🎯 Гибридный подход: "Корпоративная сеть с единым протоколом"

**Мы комбинируем лучшее из обоих миров:**

| Аспект | Что берём |
|--------|-----------|
| **Форма жизни системы** | Сеть (узлы независимы, распределены, могут падать и восстанавливаться) |
| **Стандарт коммуникации** | Материнка (единый протокол, единый формат Message, SSOT) |
| **Подключение модулей** | Материнка (драйверы/адаптеры для универсального подключения) |
| **Маршрутизация** | Сеть (роутер доставляет сообщения, не принимает бизнес-решения) |
| **Наблюдаемость** | Сеть (логи трафика, метрики, трассировка) |
| **Безопасность** | Сеть (сегментация, права доступа, изоляция) |

**Итоговая формула:**

> **MindBus = Роутер (маршрутизация) + Протокол (единый язык) + Адаптеры (универсальное подключение)**

---

## Типы сообщений и канонический формат

### Принцип "канонический конверт + смысловой payload"

MindBus переносит не "задачу" и не "ответ" напрямую, а универсальное **Сообщение (Message)**:

```
Message = Конверт (метаданные) + Payload (смысловая нагрузка)
```

Это как в компьютерной сети:
- **Шина переносит "пакеты"** по правилам
- **Содержимое пакетов** бывает разным

---

### Типизация сообщений

**Принцип**: Каждое сообщение имеет **тип (type)**, который определяет его смысловое назначение.

Типы нужны для:
- **Ясности** — сразу понятно, что это: команда, ответ или уведомление
- **Маршрутизации** — роутер может обрабатывать разные типы по-разному
- **Приоритезации** — критичные сообщения могут иметь приоритет
- **Валидации** — каждый тип может иметь свою схему payload

#### Примеры возможных типов:

**Command** — "Сделай"
- Поручение выполнить действие
- Примеры: `generate_draft`, `store_artifact`, `run_process_card`

**Result** — "Вот результат"
- Результат исполнения команды
- Статусы: `SUCCESS`, `FAILED`, `PARTIAL`

**Event** — "Произошло"
- Уведомление о факте
- Примеры: `task_created`, `step_finished`, `agent_online`

**Query** — "Дай информацию"
- Запрос данных без изменения состояния
- Примеры: `get_task_history`, `list_agents`

**Error** — "Ошибка"
- Специализированное сообщение об ошибке
- Отличается от Result с FAILED более детальной диагностикой

**Control** — "Системное управление"
- Управление системой
- Примеры: `pause_agent`, `resume_process`, `enable_module`

**Notification** — "Уведомление пользователю"
- Сообщения для человека (не для обработки агентами)
- Примеры: требуется вмешательство, критичная ситуация

**Metric** — "Метрика"
- Данные телеметрии и мониторинга
- Примеры: performance stats, resource usage

**Log** — "Лог"
- Журналирование для отладки и аудита
- Структурированные логи с уровнями важности

**Stream** — "Поток данных"
- Потоковая передача больших данных
- Чанки файлов, real-time данные

---

### Ключевые решения:

> **Точное количество типов, их финальные названия и техническая реализация (например, 1 байт = 256 возможных типов) будут определены на этапе технической спецификации.**

На уровне концепции важно:
- Сообщения **имеют тип** для ясности и маршрутизации
- Возможна **иерархия Type + Subtype** для гибкости (например, `COMMAND.STORE`, `COMMAND.RETRIEVE`)
- Система **расширяема** — можно добавлять новые типы без изменения базового протокола

> **Command управляет действием, Result завершает действие, Event описывает жизнь системы.**

Это базовая тройка, которая покрывает большинство сценариев взаимодействия.

---

## Принцип универсального подключения

### Принцип "всё через MindBus"

Любая интеграция (даже обращение к диску или БД) в концепции выглядит так:

1. **Orchestrator/Agent** формирует **Command** в каноническом виде:
   - "Сохрани артефакт"
   - "Прочитай запись"
   - "Обнови статус"
   - "Найди похожее"

2. **Адаптер-хранилища** принимает команду и делает конкретные действия в своей "вселенной" (PostgreSQL, MongoDB, S3, файловая система).

3. **Возвращает Result** (и при необходимости генерирует Event).

**Никаких прямых вызовов "в обход шины".**

---

### Адаптеры/драйверы для подключения модулей

Если мы хотим подключить **новый компонент** (новую БД, файловое хранилище, агента, внешний сервис), мы **не меняем MindBus** и **не меняем существующие модули**.

Вместо этого создаём **Адаптер (Controller/Driver/Bridge)**, который:

1. Принимает сообщения MindBus в каноническом формате
2. Преобразует их в понятные операции конкретного устройства/сервиса
3. Возвращает результат обратно в каноническом формате MindBus

**Так мы достигаем:**
- ✅ **Плагинности** — подключение новых модулей без "перепайки материнки"
- ✅ **Стабильности языка** общения
- ✅ **Масштабируемости** — много разных устройств, один общий язык
- ✅ **Управляемости** — единая наблюдаемость и трассировка

---

### Пример: подключение новой базы данных

```
┌─────────────────────────────────────────────────────┐
│ Orchestrator: Command "store_artifact"              │
│ (канонический формат MindBus)                       │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │      MindBus         │
            │   (маршрутизация)    │
            └──────────┬───────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │  PostgreSQL Adapter  │
            │  - принимает Command │
            │  - переводит в SQL   │
            │  - возвращает Result │
            └──────────┬───────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │   PostgreSQL DB      │
            │   (реальное хранилище)│
            └──────────────────────┘
```

**Мы не меняем:**
- Orchestrator
- Агенты
- Monitor
- Общий язык сообщений

**Меняем только:**
- Драйвер для нового устройства (как в компьютере)

---

## Примеры структур данных

Ниже приведены **примеры канонических структур** (черновик, не финальная версия).

### 1️⃣ Канонический Message (конверт)

```yaml
Message:
  id: <UUID>                    # Уникальный идентификатор сообщения
  type: COMMAND | RESULT | EVENT | QUERY | CONTROL | ARTIFACT
  from: <string>                # Отправитель (компонент/роль/агент)
  to: <string>                  # Получатель (компонент/роль/канал/группа)
  created_at: <timestamp>       # Время создания

  priority: <int>               # Приоритет сообщения (выше = важнее)
                                # Определяет порядок обработки в очередях
                                # Аналогия: QoS в сетях, nice в Unix

  context:                      # Контекст для трассировки
    trace_id: <UUID>            # Единая нить "жизни" задачи
    task_id: <UUID>             # Идентификатор задачи (если применимо)
    process_id: <UUID>          # Идентификатор процесса (если применимо)
    step_id: <UUID>             # Идентификатор шага (если применимо)
    parent_message_id: <UUID>   # Связь с предыдущим сообщением

  payload: <object>             # Содержимое (зависит от type)
```

**Важно:**
- `context` — это не "память процесса", а **маркировка и связка сообщений** в единую историю.
- `priority` — определяет порядок обработки (аналог QoS в сетях). Значения зависят от реализации, но концептуально: чем выше число, тем важнее сообщение.

---

### 2️⃣ Payload: Command (Task)

```yaml
CommandPayload:
  name: <string>                # Каноническое действие (generate_draft, store_artifact, etc.)
  intent: <string>              # Зачем это делается (смысл/цель)

  input:                        # Входные данные
    data: <object>              # Малые данные (inline)
    refs: <array>               # Ссылки на артефакты/документы (если большие)

  constraints:                  # Ограничения (опционально)
    time_budget: <duration>     # Ограничение по времени
    cost_budget: <float>        # Ограничение по бюджету
    quality_target: <string>    # Ожидаемое качество/критерий

  expected_output:              # Ожидаемый результат
    kind: text | file | structure | link | report
```

**Принцип "ссылки, а не данные":**
- Если входные данные большие (файл, изображение, датасет) — передаём ссылку в `refs`
- Если малые (параметры, короткий текст) — можно inline в `data`

---

### 3️⃣ Payload: Result (Response)

```yaml
ResultPayload:
  status: SUCCESS | FAILED | PARTIAL
  for_command_id: <UUID>        # Ссылка на исходную команду

  output:                       # Результат
    data: <object>              # Результат (малый, inline)
    refs: <array>               # Ссылки на артефакты (большой результат)

  metrics:                      # Метрики выполнения (опционально)
    duration: <duration>        # Сколько заняло
    cost: <float>               # Сколько "стоило" (если учитывается)
    quality: <float>            # Оценка качества (если есть)

  error:                        # Ошибка (если FAILED)
    code: <string>              # Тип ошибки
    message: <string>           # Описание
    recoverable: <boolean>      # Можно ли повторить/исправить
```

**Принцип идемпотентности:**
- Если команда была выполнена дважды (retry), Result должен быть тот же
- Получатель может кэшировать Result по `for_command_id`

---

### 4️⃣ Payload: Event (событие)

```yaml
EventPayload:
  name: <string>                # Что произошло (task_created, step_started, etc.)

  entity:                       # Сущность, связанная с событием
    kind: TASK | PROCESS | STEP | AGENT | STORAGE | SYSTEM
    id: <UUID>                  # Идентификатор сущности

  state:                        # Состояние (если применимо)
    from: <string>              # Прежнее состояние
    to: <string>                # Новое состояние

  details: <object>             # Пояснение/параметры события
```

---

### 5️⃣ Payload: Query (запрос)

```yaml
QueryPayload:
  name: <string>                # Что хотим узнать
  filters: <object>             # Условия поиска/выборки
  expected: <string>            # Что вернуть (краткое описание)
```

---

### 6️⃣ Payload: Control/Admin (управление)

```yaml
ControlPayload:
  action: enable | disable | pause | resume | reconfigure
  target: <string>              # Кто/что управляется (agent/process/node)
  reason: <string>              # Зачем (человекочитаемо)
```

---

### 7️⃣ Payload: Artifact/Data (данные или ссылка)

```yaml
ArtifactPayload:
  kind: text | file | image | dataset | report
  inline_data: <any>            # Если маленькое

  ref:                          # Ссылка (если большое)
    uri: <string>               # URI/ID в Storage
    checksum: <string>          # Контрольная сумма
    version: <string>           # Версия

  metadata:                     # Метаданные
    title: <string>
    description: <string>
    tags: <array>
```

---

## Вопросы на будущее

Ниже — список вопросов, которые **не обязательно решать сейчас**, но важно помнить при детальном проектировании.

### 1. Смысловая модель и "словарь"
- Какие сущности считаются каноническими (Task, Process, Step, Agent, Artifact…)?
- Какие поля обязательно едины по всему проекту?
- Как управлять изменениями языка данных (совместимость/версии)?

### 2. Маршрутизация и адресация
- Как адресуются получатели: конкретный компонент, роль, группа, "канал"?
- Как работать с "несколькими агентами одной роли" (pool)?
- Поддержка pub/sub топиков или только point-to-point?

### 3. Надёжность и повторяемость
- Что считается "доставлено"?
- Что делать при повторной доставке?
- Какая философия повторов (retry) и "дедлайнов"?
- Как гарантировать at-least-once / exactly-once delivery?

### 4. Порядок и согласование событий
- Нужно ли гарантировать порядок сообщений в пределах trace_id?
- Как быть с параллельными шагами?

### 5. Наблюдаемость и аудит
- Какие события считаются "обязательными" для фиксации?
- Нужен ли "журнал решений" оркестратора (почему выбрал такой путь)?
- Какие метрики здоровья системы важны (нагрузка, ошибки, очереди)?

### 6. Безопасность и права
- Какой модуль имеет право отправлять какие команды?
- Как ограничивать опасные команды (например, доступ к внешним ресурсам)?
- Как различать "бизнес-сообщения" и "админку"?
- Реализация топиков/каналов с правами доступа?

### 7. Артефакты и большие данные
- Какой порог размера для "inline vs ссылка"?
- Как версионировать артефакты концептуально?
- Как гарантировать, что ссылка ещё "жива"?

### 8. Мульти-проекты и мульти-пользователи
- Будет ли один пользователь (CEO) или много?
- Нужно ли разделение по проектам/командам (изоляция)?

### 9. Эволюция типов сообщений
- Достаточно ли базовых типов (Command/Result/Event) для MVP?
- Какие новые типы появятся позже (например, "Proposal/Approval" для governance)?

### 10. Приоритеты сообщений
- Сколько уровней приоритетов достаточно?
- Кто присваивает приоритет (отправитель, MindBus, адаптер)?
- Как реализовать preemption (прерывание низкоприоритетных задач)?

---

## Пояснения: Зачем нам эти аналогии

### 🎯 Главные задачи, которые помогают решить архитектурные метафоры

#### 1. **Модульность и универсальность**

**Проблема:** Как сделать систему, куда можно легко добавлять новые компоненты (агенты, хранилища, сервисы) без переделки существующих?

**Решение из архитектуры компьютера:**
- Материнская плата имеет **стандартные разъёмы** (PCI, USB, SATA)
- Любое устройство с правильным разъёмом подключается без изменения материнки
- Драйвер переводит команды ОС в команды устройства

**Что мы берём:**
- MindBus = материнская плата с единым протоколом
- Адаптеры = драйверы для каждого модуля
- Новый модуль = новое устройство, нужен только драйвер

**Результат:** Можем подключать любые базы данных, любые хранилища, любые AI-провайдеры через адаптеры, не меняя ядро системы.

---

#### 2. **Распределённость и отказоустойчивость**

**Проблема:** Как сделать систему, где компоненты могут быть на разных серверах, падать и восстанавливаться, не роняя всю систему?

**Решение из архитектуры сети:**
- Локальная сеть объединяет **независимые узлы**
- Роутер маршрутизирует пакеты, но не "думает за узлы"
- Узел может быть offline — сеть продолжает работать
- Сетевой протокол (TCP/IP) гарантирует доставку

**Что мы берём:**
- MindBus = роутер + протокол
- Компоненты = узлы сети (могут быть на разных машинах)
- Сообщения = пакеты с адресацией
- Независимость узлов = агент упал, система работает

**Результат:** Система может масштабироваться горизонтально, компоненты могут быть распределены, отказ одного компонента не рушит систему.

---

#### 3. **Единый источник правды (SSOT) для протокола**

**Проблема:** Как избежать хаоса, когда каждый компонент придумывает свой формат данных?

**Решение из обеих аналогий:**
- Материнка задаёт **единый стандарт сигналов** на шине
- Сеть требует **единого протокола** (TCP/IP), иначе устройства не поймут друг друга

**Что мы берём:**
- Канонический формат Message — единый для всей системы
- Все компоненты говорят на одном языке
- Изменения протокола — управляемые, совместимые

**Результат:** Нет путаницы в форматах, все компоненты понимают друг друга, легко отлаживать.

---

#### 4. **Наблюдаемость и трассировка**

**Проблема:** Как понять, что происходит в распределённой системе? Как отладить цепочку действий?

**Решение из архитектуры сети:**
- Мониторинг трафика (кто кому что отправил)
- Логи маршрутов (как пакет прошёл через сеть)
- Метрики (загрузка, ошибки, задержки)
- Trace ID / Session ID для связывания распределённых операций

**Что мы берём:**
- Каждое сообщение имеет `trace_id` — связывает всю цепочку
- MindBus логирует все сообщения
- Можем визуализировать путь задачи через систему
- Метрики "здоровья" системы (нагрузка, очереди, ошибки)

**Результат:** Полная прозрачность системы, легко отлаживать, легко понять "что пошло не так".

---

#### 5. **Безопасность и изоляция**

**Проблема:** Как защитить систему от ошибок и злоупотреблений? Как изолировать компоненты?

**Решение из архитектуры сети:**
- Сегментация сети (разные подсети для разных уровней доступа)
- Права доступа (кто может обращаться к кому)
- Изоляция узлов (узел не может "полезть" в соседний)
- Firewall / Access Control

**Что мы берём:**
- Адаптеры изолируют компоненты (агент не "лезет" напрямую в БД)
- Права доступа (какой компонент может отправлять какие команды)
- MindBus контролирует маршрутизацию
- Топики/каналы с подписками (как в Pub/Sub)

**Результат:** Безопасность на уровне архитектуры, изоляция компонентов, контроль доступа.

---

#### 6. **Приоритизация и управление**

**Проблема:** Как система реагирует на критичные события? Как человек может немедленно вмешаться?

**Решение из архитектуры сетей и ОС:**
- QoS (Quality of Service) в сетях — критичный трафик обгоняет обычный
- Приоритеты прерываний в CPU — важные события обрабатываются первыми
- Nice в Unix — процессы с высоким приоритетом получают CPU раньше

**Что мы берём:**
- Поле `priority` в сообщениях
- Очереди с приоритетами
- Критичные команды (STOP, APPROVE) обрабатываются мгновенно

**Результат:** Система реагирует на критичные события, человек может управлять процессом в реальном времени.

---

#### 7. **Эффективность и производительность**

**Проблема:** Как не перегружать сеть/шину большими данными?

**Решение из архитектуры Web и компьютера:**
- URL вместо inline картинок
- Указатели вместо копирования данных
- DMA (Direct Memory Access) — данные пишутся напрямую, шина передаёт адреса

**Что мы берём:**
- Малые данные inline
- Большие данные — только ссылка на Storage
- Шина остаётся лёгкой и быстрой

**Результат:** Высокая производительность, нет узких мест, эффективное использование ресурсов.

---

#### 8. **Надёжность через идемпотентность**

**Проблема:** Как гарантировать корректность при сбоях и повторных попытках?

**Решение из архитектуры сетей и баз данных:**
- TCP retransmission — пакет отправляется снова, получатель игнорирует дубликаты
- HTTP PUT/DELETE — идемпотентные операции
- Kafka at-least-once — сообщение может прийти дважды

**Что мы берём:**
- Компоненты проверяют "обработал ли я это сообщение?"
- Кэширование результатов по `message_id`
- Повторная доставка не создаёт побочных эффектов

**Результат:** Надёжность при сбоях, безопасные retry, защита от дублирования.

---

#### 9. **Эволюция без разрушения**

**Проблема:** Как развивать систему, не ломая существующие компоненты?

**Решение из архитектуры протоколов:**
- HTTP/1.1 → HTTP/2 → HTTP/3
- IPv4 → IPv6
- API v1 → v2
- Protocol Buffers (новые поля игнорируются старыми версиями)

**Что мы берём:**
- Версионирование протокола
- Новые поля опциональны
- Старые компоненты игнорируют неизвестное
- Обратная совместимость

**Результат:** Система эволюционирует плавно, без "большого взрыва" при обновлениях.

---

### 📚 Что конкретно берём "за образец"

| Аспект | Образец | Что берём |
|--------|---------|-----------|
| **Модульность** | Материнская плата компьютера | Стандартные разъёмы, plug-and-play, драйверы |
| **Протокол** | TCP/IP, USB, PCI | Единый формат Message, канонические типы |
| **Распределённость** | Корпоративная LAN | Независимые узлы, роутер, маршрутизация |
| **Отказоустойчивость** | Сеть Интернет | Retry, таймауты, узел может быть offline |
| **Наблюдаемость** | Мониторинг сети | Логи трафика, метрики, trace ID |
| **Безопасность** | VLAN, Firewall ACL | Права доступа, изоляция, топики/каналы |
| **Приоритеты** | QoS в сетях, nice в ОС | Поле priority, очереди с приоритетами |
| **Эффективность** | URL в Web, указатели в C | Ссылки вместо данных |
| **Надёжность** | TCP retransmission | Идемпотентность, кэширование результатов |
| **Эволюция** | HTTP/API versioning | Версионирование протокола, обратная совместимость |

---

### 🔑 Ключевые принципы MindBus (итого)

1. **Минимализм шины**: MindBus доставляет, маршрутизирует, обеспечивает контекст. Он НЕ хранит бизнес-состояние, НЕ "думает" за оркестратора, НЕ становится базой данных.

2. **Единый язык**: Канонический формат Message — единый источник правды. Все компоненты говорят на одном языке.

3. **Всё через шину**: Никаких прямых вызовов "в обход". Любая интеграция идёт через MindBus.

4. **Адаптеры для универсальности**: Новый модуль = новый адаптер. Не меняем ядро системы.

5. **Наблюдаемость из коробки**: Все сообщения логируются, trace_id связывает операции, метрики доступны.

6. **Отказоустойчивость**: Узел может упасть — система продолжает работать.

7. **Приоритеты и управление**: Критичные сообщения обрабатываются первыми, человек может вмешаться в реальном времени.

8. **Ссылки, а не данные**: Шина передаёт ссылки на большие данные, остаётся лёгкой и быстрой.

9. **Идемпотентность**: Компоненты устойчивы к повторной доставке, безопасные retry.

10. **Эволюция с совместимостью**: Протокол версионируется, старые компоненты работают с новыми.

---

## Итоговая мысль

**MindBus передаёт универсальный Message с единым конвертом и расширяемой системой смысловых payload:**

- **Command** — поручение
- **Result** — результат
- **Event** — жизнь системы
- **Query, Control, Artifact** — по мере необходимости

А подключение любых новых систем реализуется через **контроллеры/драйверы/адаптеры**, как в компьютере: новое устройство не ломает архитектуру — оно просто получает "драйвер" под шину.

**Метафоры "материнской платы" и "корпоративной сети" помогают нам проектировать MindBus как:**
- **Модульную** (легко добавлять компоненты)
- **Универсальную** (любые технологии через адаптеры)
- **Распределённую** (компоненты независимы)
- **Отказоустойчивую** (узел упал — система работает)
- **Наблюдаемую** (полная прозрачность)
- **Безопасную** (изоляция и права доступа)
- **Управляемую** (приоритеты и реакция на критичные события)
- **Эффективную** (ссылки вместо данных)
- **Надёжную** (идемпотентность и retry)
- **Эволюционирующую** (версионирование и совместимость)

---

**Этот документ — живой черновик.** Мы будем возвращаться к нему, уточнять, дополнять по мере проработки деталей.

**Следующий шаг:** Детальная проработка канонического формата Message и типов payload в отдельном документе (Data Model Specification).

---

**Статус**: 🟡 Draft v0.2 (Черновик)
**Следующая версия**: После проработки конкретных структур данных и маршрутизации
