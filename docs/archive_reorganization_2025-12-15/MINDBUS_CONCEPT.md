# MindBus: Концепция и принципы

## Что такое MindBus

**MindBus** — это "нервная система" AI_TEAM, единая коммуникационная шина через которую все компоненты системы общаются друг с другом.

### Метафора: Нервная система

Как в живом организме нервы передают сигналы между мозгом и органами, так MindBus передаёт сообщения между Orchestrator и агентами.

**Ключевые свойства нервной системы:**
- 🔄 **Двусторонняя связь** — сигналы идут в обе стороны
- ⚡ **Быстрая передача** — реакция должна быть мгновенной
- 🛡 **Надёжность** — критичные сигналы не должны теряться
- 🔍 **Наблюдаемость** — можно отследить путь каждого сигнала
- 🧬 **Самовосстановление** — система продолжает работать даже при частичных повреждениях

---

## Роль MindBus в системе

```
┌────────────────────────────────────────────────┐
│              Orchestrator                       │
│         (Мозг системы)                         │
└───────────────────┬────────────────────────────┘
                    │
                    │ Команды и результаты
                    │
        ┌───────────▼──────────────────┐
        │        MindBus                │
        │    (Нервная система)          │
        └───────────┬──────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
┌───────▼────────┐     ┌───────▼────────┐
│  Writer Agent  │     │  Critic Agent  │
│  (Орган #1)    │     │  (Орган #2)    │
└────────────────┘     └────────────────┘
```

**MindBus отвечает за:**
1. Доставку сообщений от отправителя к получателю
2. Сохранение истории коммуникаций
3. Гарантии доставки
4. Обнаружение "мёртвых" компонентов
5. Трассировку всех операций

---

## Воплощение принципов живучести

MindBus является **критическим компонентом** для реализации всех 10 принципов устойчивости.

### 1. Самообнаружение и переподключение

**Принцип:** Компоненты сами находят друг друга после перезапуска.

**Роль MindBus:**
- Агент не знает адрес других агентов
- Агент подключается к MindBus
- MindBus становится "точкой встречи" для всех компонентов
- После перезапуска агент снова подключается к MindBus
- Orchestrator видит нового агента через MindBus

**Метафора:** Площадь в центре города — все знают где она, и встречаются там.

**Антипаттерн (без MindBus):**
```
❌ Writer должен знать IP адрес Orchestrator
❌ Orchestrator должен знать IP всех агентов
❌ При перезапуске нужно обновлять конфиги
❌ Добавление нового агента требует изменений везде
```

**С MindBus:**
```
✅ Все подключаются к MindBus
✅ IP адреса нужны только для MindBus
✅ Перезапуск = переподключение к MindBus
✅ Новый агент просто подключается к MindBus
```

---

### 2. Контроль активности (Liveness)

**Принцип:** Система знает кто жив, а кто упал.

**Роль MindBus:**
- Агенты периодически "дышат" (heartbeat) через MindBus
- MindBus отслеживает последний "вдох" каждого агента
- Если агент не "дышал" N секунд — считается мёртвым
- Orchestrator спрашивает у MindBus: "Кто жив?"
- MindBus отвечает списком активных агентов

**Метафора:** Врач проверяет пульс пациента.

**Пример:**
```
Ситуация:
1. Writer-1 подключился к MindBus
2. Writer-1 шлёт heartbeat каждые 10 секунд
3. Writer-1 упал (crash)
4. 30 секунд нет heartbeat
5. MindBus помечает Writer-1 как "мёртвый"
6. Orchestrator спрашивает: "Кто может писать статьи?"
7. MindBus отвечает: "Writer-2 жив, Writer-1 мёртв"
8. Orchestrator назначает задачу Writer-2
```

---

### 3. Защита от дубликатов

**Принцип:** Одна задача не должна выполняться дважды.

**Роль MindBus:**
- Каждое сообщение имеет уникальный ID
- MindBus запоминает какие сообщения уже обработаны
- Если сообщение пришло повторно — MindBus его игнорирует
- Гарантия "exactly-once delivery" (или "at-least-once" с идемпотентностью)

**Метафора:** Секретарь не передаёт босу одно письмо дважды.

**Пример:**
```
Ситуация:
1. Orchestrator отправляет: "Writer, напиши статью task-123"
2. Сеть глючит
3. Сообщение доставлено дважды
4. MindBus видит дубликат (тот же message_id)
5. Второе сообщение отбрасывается
6. Writer получает задачу только один раз
```

---

### 4. Восстановление состояния

**Принцип:** Незавершённые задачи не теряются при сбоях.

**Роль MindBus:**
- MindBus сохраняет все сообщения в лог
- При перезапуске компонент может спросить: "Что я делал?"
- MindBus отвечает историей сообщений
- Компонент восстанавливает контекст

**Метафора:** Дневник, который помогает вспомнить что ты делал вчера.

**Пример:**
```
Ситуация:
1. Orchestrator назначил задачу task-123 Writer
2. Writer начал работу
3. Writer упал
4. Writer перезапустился
5. Writer спрашивает MindBus: "Какие задачи мне назначали?"
6. MindBus отвечает: "task-123, начата но не завершена"
7. Writer продолжает ИЛИ сообщает что не может продолжить
```

---

### 5. Идемпотентность

**Принцип:** Операции безопасны к повторному выполнению.

**Роль MindBus:**
- MindBus может доставлять сообщения повторно (если не уверен что доставил)
- Агенты проектируются так что повторная обработка безопасна
- MindBus предоставляет механизмы для защиты от дублирования

**Метафора:** Кнопка "сохранить" в редакторе — можно нажать сколько угодно раз.

**Пример:**
```
Сообщение: "Установи quality_score=8 для task-123"

Без идемпотентности:
❌ "Добавь +1 к quality_score" → если выполнится дважды, будет неправильно

С идемпотентностью:
✅ "Установи quality_score=8" → можно выполнить сколько угодно раз
```

---

### 6. Управляемая деградация

**Принцип:** Отказ одного компонента не ломает всю систему.

**Роль MindBus:**
- MindBus продолжает работать если один агент упал
- Сообщения для упавшего агента помещаются в очередь
- Когда агент восстанавливается — получает накопившиеся сообщения
- Orchestrator видит какие агенты доступны и работает с ними

**Метафора:** Почтовый ящик — письма накапливаются пока вас нет.

**Пример:**
```
Ситуация:
1. Critic Agent упал
2. Orchestrator хочет отправить задачу Critic
3. MindBus сообщает: "Critic недоступен"
4. Orchestrator решает:
   - Подождать восстановления Critic ИЛИ
   - Продолжить без критики (с предупреждением) ИЛИ
   - Назначить другого Critic если есть
5. Система продолжает работать
```

---

### 7. Таймауты и корректное завершение

**Принцип:** Операции имеют ограничения по времени.

**Роль MindBus:**
- Сообщения могут иметь TTL (время жизни)
- Если сообщение не обработано за N времени — считается expired
- При остановке компонент отправляет через MindBus: "Я завершаюсь"
- Orchestrator получает уведомление и переназначает задачи

**Метафора:** Срок годности продуктов — старые выбрасываются.

**Пример:**
```
Ситуация:
1. Orchestrator отправляет задачу Writer (timeout: 5 минут)
2. Writer начал работу
3. Прошло 6 минут, ответа нет
4. MindBus помечает сообщение как expired
5. Orchestrator видит таймаут
6. Orchestrator переназначает задачу другому Writer
```

---

### 8. Нет "зомби"

**Принцип:** Устаревшие компоненты не влияют на систему.

**Роль MindBus:**
- Каждый компонент имеет уникальный instance_id
- Если компонент перерегистрировался с новым instance_id — старый считается "зомби"
- Сообщения от "зомби" игнорируются
- Только активный экземпляр получает задачи

**Метафора:** Пропуск в офис аннулируется при увольнении.

**Пример:**
```
Ситуация:
1. Writer-1 (instance: abc-123) работает
2. Сеть глючит
3. MindBus думает что Writer-1 упал
4. Запускается Writer-1 заново (instance: def-456)
5. Сеть восстанавливается
6. Старый Writer-1 (abc-123) пытается отправить результат
7. MindBus проверяет: "abc-123 больше не активен"
8. Сообщение отклоняется
9. Только def-456 может работать
```

---

### 9. Наблюдаемость

**Принцип:** Все события жизненного цикла видны.

**Роль MindBus:**
- MindBus логирует все сообщения
- Каждое сообщение имеет trace_id
- Можно проследить всю цепочку: задача → команды → результаты
- MindBus предоставляет метрики:
  - Сколько сообщений отправлено
  - Сколько доставлено
  - Сколько lost/expired
  - Латентность доставки

**Метафора:** Трек-номер посылки — можно отследить весь путь.

**Пример:**
```
Пользователь: "Где моя статья?"

Orchestrator смотрит в MindBus:
✅ 12:00 - Отправил команду Writer (trace_id: task-123)
✅ 12:01 - Writer получил команду
✅ 12:05 - Writer отправил результат
✅ 12:06 - Orchestrator получил результат
✅ 12:06 - Отправил на критику Critic
✅ 12:08 - Critic получил
⏳ 12:10 - Critic ещё работает...

Ответ: "Статья написана, сейчас проходит критику"
```

---

### 10. Эволюция и совместимость

**Принцип:** Компоненты разных версий могут работать вместе.

**Роль MindBus:**
- Сообщения имеют версию формата
- MindBus может трансформировать сообщения между версиями
- Старые агенты получают сообщения в старом формате
- Новые агенты получают в новом
- Постепенная миграция без остановки системы

**Метафора:** Переводчик между людьми говорящими на разных языках.

**Пример:**
```
Ситуация:
1. Обновили Orchestrator до v2.0
2. Writer всё ещё v1.0
3. Orchestrator отправляет сообщение v2 формата
4. MindBus видит что Writer понимает только v1
5. MindBus конвертирует сообщение в v1 формат
6. Writer получает понятное сообщение
7. Система продолжает работать
```

---

## Ключевые свойства MindBus

### 1. Централизованная точка коммуникации

**Все общаются через MindBus:**
- Orchestrator ↔ MindBus ↔ Agents
- Agents ↔ MindBus ↔ Agents (если нужно)
- Monitor ↔ MindBus (для наблюдения)

**Преимущества:**
- Не нужно знать адреса друг друга
- Легко добавлять новые компоненты
- Одна точка для мониторинга

### 2. Асинхронная коммуникация

**Отправитель не ждёт получателя:**
- Orchestrator отправил команду → продолжил работу
- Writer получит когда будет готов
- MindBus гарантирует доставку

**Преимущества:**
- Компоненты не блокируют друг друга
- Можно обрабатывать тысячи задач параллельно
- Устойчивость к временным недоступностям

### 3. Персистентность (сохранение истории)

**MindBus — это не просто "труба", это "журнал":**
- Все сообщения сохраняются
- Можно перечитать историю
- Можно воспроизвести события

**Преимущества:**
- Восстановление после сбоев
- Аудит всех операций
- Debugging и анализ

### 4. Гарантии доставки

**MindBus обещает:**
- Сообщение будет доставлено (at-least-once)
- Или вы узнаете что не доставлено
- История не потеряется

**Преимущества:**
- Надёжность системы
- Нет "потерянных" задач
- Предсказуемое поведение

---

## Типы сообщений

### COMMAND — Команда

**Orchestrator → Agent**

"Сделай это"

```
{
  "type": "COMMAND",
  "from": "orchestrator",
  "to": "writer",
  "trace_id": "task-123",
  "payload": {
    "action": "write_article",
    "topic": "AI trends",
    "quality_threshold": 8.0
  }
}
```

### RESULT — Результат

**Agent → Orchestrator**

"Вот что получилось"

```
{
  "type": "RESULT",
  "from": "writer",
  "to": "orchestrator",
  "trace_id": "task-123",
  "payload": {
    "status": "completed",
    "artifact_url": "storage://task-123/article.txt",
    "metadata": {
      "quality_score": 7.5,
      "tokens_used": 1500
    }
  }
}
```

### EVENT — Событие

**Любой → Все заинтересованные**

"Произошло это"

```
{
  "type": "EVENT",
  "from": "writer",
  "to": "monitor",
  "trace_id": "task-123",
  "payload": {
    "event": "task_started",
    "timestamp": "2025-12-13T12:00:00Z"
  }
}
```

---

## Концептуальные требования к реализации

**Когда будем выбирать техническую реализацию MindBus, она ДОЛЖНА поддерживать:**

### ✅ Обязательные свойства:

1. **Publish/Subscribe паттерн**
   - Отправитель публикует сообщение
   - Получатель подписывается на сообщения

2. **Персистентность**
   - Сообщения сохраняются на диск
   - Не теряются при перезапуске

3. **Consumer Groups**
   - Несколько экземпляров Writer могут обрабатывать сообщения параллельно
   - Каждое сообщение получит только один экземпляр

4. **Acknowledge mechanism**
   - Получатель подтверждает обработку
   - Если не подтвердил — сообщение переназначается

5. **Message ordering (в рамках одного trace_id)**
   - Сообщения для task-123 обрабатываются по порядку

6. **Backlog (история сообщений)**
   - Можно перечитать старые сообщения
   - Нужно для восстановления состояния

7. **TTL (Time To Live)**
   - Сообщения могут устаревать
   - Старые сообщения автоматически удаляются

8. **Monitoring и метрики**
   - Сколько сообщений в очереди
   - Latency доставки
   - Dead letter queue для проблемных сообщений

---

## Связь с другими компонентами

### MindBus ↔ Orchestrator

**Orchestrator использует MindBus для:**
- Отправки команд агентам
- Получения результатов
- Проверки кто из агентов жив
- Восстановления состояния задач

### MindBus ↔ Agents

**Агенты используют MindBus для:**
- Получения команд
- Отправки результатов
- Heartbeat (сигнал жив)
- Отправки событий (логи, метрики)

### MindBus ↔ Monitor

**Monitor использует MindBus для:**
- Чтения всех сообщений (read-only)
- Построения визуализации потоков
- Анализа узких мест

### MindBus ↔ Storage

**MindBus опирается на Storage для:**
- Сохранения сообщений
- Индексации по trace_id
- Ретеншен политики (удаление старых сообщений)

---

## Метафоры для понимания

### MindBus = Площадь в центре города

- Все знают где находится площадь
- Все приходят на площадь чтобы встретиться
- На площади есть доски объявлений (сообщения)
- Каждый может оставить объявление
- Каждый может прочитать объявления для себя

### MindBus = Почтовое отделение

- Отправитель приносит письмо
- Почта сохраняет и доставляет
- Получатель забирает из ящика
- Если получателя нет — письмо ждёт
- История всех писем сохраняется

### MindBus = Нервная система

- Нейроны передают сигналы
- Двусторонняя связь
- Быстрая передача
- Работает даже при частичных повреждениях
- Критичные сигналы имеют приоритет

---

## Принципы проектирования MindBus

### 1. Simple but powerful

**Простой интерфейс:**
- `publish(message)` — отправить
- `subscribe(pattern)` — подписаться
- `acknowledge(message_id)` — подтвердить

**Мощная функциональность:**
- Персистентность
- Гарантии доставки
- Трассировка
- Восстановление

### 2. MindBus не знает бизнес-логику

**MindBus — это транспорт, не процессор:**
- Не интерпретирует содержимое сообщений
- Не принимает решений
- Просто доставляет надёжно и быстро

**Бизнес-логика — в компонентах:**
- Orchestrator решает что делать
- Агенты выполняют
- MindBus только доставляет

### 3. Observability by design

**Каждое сообщение содержит:**
- `trace_id` — для сквозной трассировки
- `timestamp` — когда отправлено
- `from` / `to` — кто и кому
- `message_id` — уникальный идентификатор

**Можно ответить на вопросы:**
- Где задача task-123 сейчас?
- Почему Writer не ответил?
- Сколько времени заняла доставка?

### 4. Fail-safe

**MindBus проектируется на отказ:**
- Если сомневаешься — сохрани сообщение
- Если не уверен что доставил — доставь ещё раз (с идемпотентностью)
- Лучше задержка чем потеря

---

## Следующие шаги

Этот концептуальный документ — **фундамент** для технической спецификации MindBus.

**При выборе технологии реализации проверим:**
- ✅ Поддерживает ли pub/sub?
- ✅ Есть ли персистентность?
- ✅ Есть ли consumer groups?
- ✅ Есть ли acknowledge?
- ✅ Сохраняет ли порядок сообщений?
- ✅ Есть ли backlog?
- ✅ Поддерживает ли TTL?
- ✅ Какие метрики доступны?

**Если технология НЕ соответствует требованиям — она не подходит для MindBus.**

---

## Связь с принципами проекта

MindBus воплощает все базовые принципы AI_TEAM:

| Принцип | Как MindBus реализует |
|---------|----------------------|
| **Specification-Driven** | Формат сообщений = спецификация, валидируется |
| **Zero Hardcoding** | Компоненты не знают адреса друг друга |
| **Observability** | Каждое сообщение трассируется через trace_id |
| **Resilience** | Все 10 принципов живучести реализованы через MindBus |
| **Modularity** | MindBus = независимый компонент, легко заменяемый |
| **Proven Architectures** | Используем проверенные паттерны message bus |

---

**Последнее обновление:** 2025-12-13
**Статус:** ✅ Утверждённая концепция
**Следующий шаг:** Техническая спецификация с выбором конкретной реализации
